diff --git a/pi-tui/src/apps/git/git-tui.ts b/pi-tui-zh/src/apps/git/git-tui.ts
index 9e0411a..645eb16 100644
--- a/pi-tui/src/apps/git/git-tui.ts
+++ b/pi-tui-zh/src/apps/git/git-tui.ts
@@ -340,7 +340,7 @@ class GitTuiComponent implements Component {
 			if (this.#pendingDiscard !== key) {
 				this.#pendingDiscard = key;
 				this.#setStatus(
-					theme.fg("warning", `Discard changes to ${action.selection.label}? Press delete again to confirm`),
+					theme.fg("warning", `丢弃对 ${action.selection.label} 的更改？再次按 delete 确认`),
 				);
 				return;
 			}
@@ -352,7 +352,7 @@ class GitTuiComponent implements Component {
 			switch (action.type) {
 				case "discard":
 					await this.#model.discard(action.selection.files);
-					this.#setStatus(theme.fg("success", `Discarded ${action.selection.label}`));
+					this.#setStatus(theme.fg("success", `已丢弃 ${action.selection.label}`));
 					break;
 				case "stage":
 					await this.#model.stage(action.selection?.files);
@@ -768,8 +768,8 @@ class GitTuiComponent implements Component {
 			theme.fg(
 				"dim",
 				this.#focus === "diff"
-					? "alt+↓/↑ hunk · ]/[ file · shift+↑/↓ select · s/u stage · x/del discard · v view · c commit · q quit"
-					: "↑/↓ move · ←/→ fold · space stage · del discard · enter open · alt+↓/↑ hunk · c commit · t tree · q quit",
+					? "alt+↓/↑ 块 · ]/[ 文件 · shift+↑/↓ 选择 · s/u 暂存 · x/del 丢弃 · v 查看 · c 提交 · q 退出"
+					: "↑/↓ 移动 · ←/→ 折叠 · space 暂存 · del 丢弃 · enter 打开 · alt+↓/↑ 块 · c 提交 · t 树 · q 退出",
 			);
 		const free = width - row.width - right.width - 1;
 		const middleText = free > visibleWidth(middle) + 4 ? middle : truncateToWidth(middle, Math.max(0, free - 4));
diff --git a/pi-tui/src/chat/served-model-marker.ts b/pi-tui-zh/src/chat/served-model-marker.ts
index 52fd00f..e55ffa5 100644
--- a/pi-tui/src/chat/served-model-marker.ts
+++ b/pi-tui-zh/src/chat/served-model-marker.ts
@@ -81,7 +81,7 @@ export class ServedModelMarkerComponent extends MessageDividerComponent {
 			label: () => {
 				const dot = theme.sep.dot.trim();
 				const via = info.upstreamProvider ? `${info.provider}/${info.upstreamProvider}` : info.provider;
-				return `${theme.status.warning} served ${info.served} ${dot} requested ${info.requested} ${dot} via ${via}`;
+				return `${theme.status.warning} 已服务 ${info.served} ${dot} 请求 ${info.requested} ${dot} 经由 ${via}`;
 			},
 			labelColor: "warning",
 			ruleColor: "dim",
diff --git a/pi-tui/src/hotkeys-markdown.ts b/pi-tui-zh/src/hotkeys-markdown.ts
index a776f26..c55e946 100644
--- a/pi-tui/src/hotkeys-markdown.ts
+++ b/pi-tui-zh/src/hotkeys-markdown.ts
@@ -37,64 +37,64 @@ export function buildHotkeysMarkdown(bindings: HotkeysMarkdownBindings): string
 	const exitRows: string[] = [];
 	if (deletingExitKeys.length > 0) {
 		exitRows.push(
-			`| \`${formatKeyHints(deletingExitKeys)}\` | Delete char forward (with draft) / exit (empty prompt) |`,
+			`| \`${formatKeyHints(deletingExitKeys)}\` | 前向删除字符（有草稿时）/ 退出（提示为空时） |`,
 		);
 	}
 	// An unbound exit action still gets its row, mirroring the `Disabled` hint every other row uses.
 	if (quittingExitKeys.length > 0 || deletingExitKeys.length === 0) {
-		exitRows.push(`| \`${formatKeyHints(quittingExitKeys) || "Disabled"}\` | Exit |`);
+		exitRows.push(`| \`${formatKeyHints(quittingExitKeys) || "Disabled"}\` | 退出 |`);
 	}
 	return [
-		"**Navigation**",
-		"| Key | Action |",
+		"**导航**",
+		"| 按键 | 操作 |",
 		"|-----|--------|",
-		"| `Arrow keys` | Move cursor / browse history (Up when empty) |",
+		"| `方向键` | 移动光标 / 浏览历史(输入为空时) |",
 		`| \`${alt}+Left/Right\` | Move by word |`,
 		isMac ? `| \`Ctrl+A\` / \`Home\` / \`${cmd}+Left\` | Start of line |` : "| `Ctrl+A` / `Home` | Start of line |",
 		isMac ? `| \`Ctrl+E\` / \`End\` / \`${cmd}+Right\` | End of line |` : "| `Ctrl+E` / `End` | End of line |",
 		"",
-		"**Editing**",
-		"| Key | Action |",
+		"**编辑**",
+		"| 按键 | 操作 |",
 		"|-----|--------|",
-		"| `Enter` | Send message |",
+		"| `Enter` | 发送消息 |",
 		`| \`Shift+Enter\` / \`${alt}+Enter\` | New line |`,
 		`| \`Ctrl+W\` / \`${alt}+Backspace\` | Delete word backwards |`,
-		"| `Ctrl+U` | Delete to start of line |",
-		"| `Ctrl+K` | Delete to end of line |",
-		`| \`${hotkeyLabel(bindings, "app.clipboard.copyLine")}\` | Copy current line |`,
-		`| \`${hotkeyLabel(bindings, "app.clipboard.copyPrompt")}\` | Copy whole prompt |`,
+		"| `Ctrl+U` | 删除到行首 |",
+		"| `Ctrl+K` | 删除到行尾 |",
+		`| \`${hotkeyLabel(bindings, "app.clipboard.copyLine")}\` | 复制当前行 |`,
+		`| \`${hotkeyLabel(bindings, "app.clipboard.copyPrompt")}\` | 复制整个提示 |`,
 		"",
-		"**Other**",
-		"| Key | Action |",
+		"**其他**",
+		"| 按键 | 操作 |",
 		"|-----|--------|",
-		"| `Tab` | Path completion / accept autocomplete |",
-		`| \`${hotkeyLabel(bindings, "app.interrupt")}\` | Cancel autocomplete / interrupt active work |`,
-		`| \`${hotkeyLabel(bindings, "app.clear")}\` | Clear editor (first) / exit (second) |`,
+		"| `Tab` | 路径补全 / 接受自动补全 |",
+		`| \`${hotkeyLabel(bindings, "app.interrupt")}\` | 取消自动补全 / 中断进行中的工作 |`,
+		`| \`${hotkeyLabel(bindings, "app.clear")}\` | 清空编辑器（第一次）/ 退出（第二次） |`,
 		...exitRows,
-		`| \`${hotkeyLabel(bindings, "app.suspend")}\` | Suspend to background |`,
-		`| \`${hotkeyLabel(bindings, "app.display.reset")}\` | Reset terminal display |`,
-		`| \`${hotkeyLabel(bindings, "app.thinking.cycle")}\` | Cycle thinking level |`,
-		`| \`${hotkeyLabel(bindings, "app.model.cycleForward")}\` | Cycle role models (slow/default/smol) |`,
-		`| \`${hotkeyLabel(bindings, "app.model.cycleBackward")}\` | Cycle role models (backward) |`,
-		`| \`${hotkeyLabel(bindings, "app.model.selectTemporary")}\` | Select model (temporary) |`,
-		`| \`${hotkeyLabel(bindings, "app.model.select")}\` | Select model (set roles) |`,
-		`| \`${hotkeyLabel(bindings, "app.plan.toggle")}\` | Toggle plan mode |`,
-		`| \`${hotkeyLabel(bindings, "app.history.search")}\` | Search prompt history |`,
-		`| \`${hotkeyLabel(bindings, "app.tools.expand")}\` | Toggle tool output expansion |`,
-		`| \`${hotkeyLabel(bindings, "app.tools.toggleVisibility")}\` | Toggle tool activity visibility |`,
-		`| \`${hotkeyLabel(bindings, "app.thinking.toggle")}\` | Toggle thinking block visibility |`,
-		`| \`${hotkeyLabel(bindings, "app.editor.external")}\` | Edit message in external editor |`,
-		`| \`${hotkeyLabel(bindings, "app.retry")}\` | Retry last failed assistant turn |`,
-		`| \`${hotkeyLabel(bindings, "app.clipboard.pasteImage")}\` | Paste image or text from clipboard |`,
-		"| Hold `Space` | Speech-to-text (push-to-talk): hold to record, release to transcribe |",
-		`| \`${hotkeyLabel(bindings, "app.live.toggle")}\` | Start/stop live voice mode (/live) |`,
-		`| \`${hotkeyLabel(bindings, "app.agents.hub")}\` / \`${hotkeyLabel(bindings, "app.session.observe")}\` / double-tap \`←\` (empty editor) | Open the agent hub |`,
-		"| `#<number>` | GitHub issue/PR reference (e.g. `#3164` → `pr://`/`issue://`) |",
-		"| `#` / `#<text>` | Prompt actions (copy / undo / move cursor) |",
-		"| `/` | Slash commands |",
-		"| `!` | Run bash command |",
-		"| `!!` | Run bash command (excluded from context) |",
-		"| `$` | Run Python in shared kernel |",
-		"| `$$` | Run Python (excluded from context) |",
+		`| \`${hotkeyLabel(bindings, "app.suspend")}\` | 挂起到后台 |`,
+		`| \`${hotkeyLabel(bindings, "app.display.reset")}\` | 重置终端显示 |`,
+		`| \`${hotkeyLabel(bindings, "app.thinking.cycle")}\` | 循环切换思考级别 |`,
+		`| \`${hotkeyLabel(bindings, "app.model.cycleForward")}\` | 循环切换角色模型（slow/default/smol） |`,
+		`| \`${hotkeyLabel(bindings, "app.model.cycleBackward")}\` | 反向循环角色模型 |`,
+		`| \`${hotkeyLabel(bindings, "app.model.selectTemporary")}\` | 选择模型（临时） |`,
+		`| \`${hotkeyLabel(bindings, "app.model.select")}\` | 选择模型（设置角色） |`,
+		`| \`${hotkeyLabel(bindings, "app.plan.toggle")}\` | 切换规划模式 |`,
+		`| \`${hotkeyLabel(bindings, "app.history.search")}\` | 搜索提示词历史 |`,
+		`| \`${hotkeyLabel(bindings, "app.tools.expand")}\` | 展开/折叠工具输出 |`,
+		`| \`${hotkeyLabel(bindings, "app.tools.toggleVisibility")}\` | 切换工具活动可见性 |`,
+		`| \`${hotkeyLabel(bindings, "app.thinking.toggle")}\` | 切换思考块可见性 |`,
+		`| \`${hotkeyLabel(bindings, "app.editor.external")}\` | 在外部编辑器中编辑消息 |`,
+		`| \`${hotkeyLabel(bindings, "app.retry")}\` | 重试上一次失败的助手回合 |`,
+		`| \`${hotkeyLabel(bindings, "app.clipboard.pasteImage")}\` | 从剪贴板粘贴图片或文本 |`,
+		"| 按住 `Space` | 语音转文字(按住说话):按住录音,松开转写 |",
+		`| \`${hotkeyLabel(bindings, "app.live.toggle")}\` | 开始/停止实时语音模式（/live） |`,
+		`| \`${hotkeyLabel(bindings, "app.agents.hub")}\` / \`${hotkeyLabel(bindings, "app.session.observe")}\` / 双击 \`←\`（空编辑器） | 打开代理中心 |`,
+		"| `#<数字>` | GitHub issue/PR 引用(如 `#3164` → `pr://`/`issue://`) |",
+		"| `#` / `#<文本>` | 提示词操作(复制 / 撤销 / 移动光标) |",
+		"| `/` | 斜杠命令 |",
+		"| `!` | 运行 bash 命令 |",
+		"| `!!` | 运行 bash 命令(不写入上下文) |",
+		"| `$` | 在共享内核中运行 Python |",
+		"| `$$` | 运行 Python(不写入上下文) |",
 	].join("\n");
 }
diff --git a/pi-tui/src/overlays/advisor-config.ts b/pi-tui-zh/src/overlays/advisor-config.ts
index cf12471..006476f 100644
--- a/pi-tui/src/overlays/advisor-config.ts
+++ b/pi-tui-zh/src/overlays/advisor-config.ts
@@ -367,7 +367,7 @@ export class AdvisorConfigOverlayComponent implements Component {
 		// until a successful save rewrites the file without them.
 		const warnings = this.#doc.warnings?.length
 			? [
-					theme.fg("warning", "⚠ Config problems — dropped while loading:"),
+					theme.fg("warning", "⚠ 配置问题 —— 加载时已丢弃："),
 					...sanitizeDisplayWarnings(this.#doc.warnings).flatMap(warning =>
 						wrap(warning, bodyWidth).map(line => theme.fg("warning", line)),
 					),
@@ -647,7 +647,7 @@ export class AdvisorConfigOverlayComponent implements Component {
 			this.#showDetail(index);
 		};
 		input.onEscape = () => this.#showDetail(index);
-		this.#setScreen("name", input, "Type a name · Enter save · Esc cancel");
+		this.#setScreen("name", input, "输入名称 · Enter 保存 · Esc 取消");
 	}
 
 	#showModelPicker(index: number): void {
@@ -680,7 +680,7 @@ export class AdvisorConfigOverlayComponent implements Component {
 			}
 		};
 		picker.onCancel = () => this.#showDetail(index);
-		this.#setScreen("model", picker, "Type to search · Enter / click twice picks · Esc back");
+		this.#setScreen("model", picker, "输入搜索 · Enter / 双击选择 · Esc 返回");
 	}
 
 	#showThinkingPicker(index: number, selector: string, efforts: readonly string[]): void {
@@ -729,7 +729,7 @@ export class AdvisorConfigOverlayComponent implements Component {
 		this.#setScreen(
 			"tools",
 			list,
-			"Enter / click toggle · select Done or Esc to apply (empty = no tools; read/grep/glob = default)",
+			"Enter / 点击切换 · 选 Done 或 Esc 应用(空 = 无工具;read/grep/glob = 默认)",
 		);
 	}
 
diff --git a/pi-tui/src/overlays/agent-hub.ts b/pi-tui-zh/src/overlays/agent-hub.ts
index aa0261d..aa31e74 100644
--- a/pi-tui/src/overlays/agent-hub.ts
+++ b/pi-tui-zh/src/overlays/agent-hub.ts
@@ -1014,6 +1014,7 @@ export class AgentHubOverlayComponent<TRecord extends AgentRecordLike = AgentRec
 
 	#statusSummary(): string {
 		const parts: string[] = [];
+		const labels: Record<string, string> = { running: "运行中", idle: "空闲", parked: "驻留", aborted: "已中止" };
 		for (const status of ["running", "idle", "parked", "aborted"] as const) {
 			const count = this.#statusCounts[status];
 			if (count > 0) parts.push(`${statusGlyph(status)} ${statusText(status, `${count} ${status}`)}`);
@@ -1123,7 +1124,7 @@ export class AgentHubOverlayComponent<TRecord extends AgentRecordLike = AgentRec
 		const artifacts = ref.history;
 		if (artifacts?.outputPath) addWrapped(`Output ${shortenPath(artifacts.outputPath)}`);
 		if (artifacts?.patchPath) addWrapped(`Patch ${shortenPath(artifacts.patchPath)}`);
-		for (const nestedPath of artifacts?.nestedPatchPaths ?? []) addWrapped(`Nested patch ${shortenPath(nestedPath)}`);
+		for (const nestedPath of artifacts?.nestedPatchPaths ?? []) addWrapped(`嵌套补丁 ${shortenPath(nestedPath)}`);
 		if (artifacts?.branchName) addWrapped(`Worktree branch ${artifacts.branchName}`);
 
 		if (lines.length < rows) add();
diff --git a/pi-tui/src/overlays/agent-transcript-viewer.ts b/pi-tui-zh/src/overlays/agent-transcript-viewer.ts
index f8f0b65..cdee246 100644
--- a/pi-tui/src/overlays/agent-transcript-viewer.ts
+++ b/pi-tui-zh/src/overlays/agent-transcript-viewer.ts
@@ -660,10 +660,10 @@ export class AgentTranscriptViewer implements Component {
 	#placeholder(maxWidth: number): string {
 		if (this.#deps.remote) {
 			if (this.#remoteError) return sanitizeErrorLine(this.#remoteError, maxWidth);
-			if (this.#remoteUnavailable) return "Transcript lives on the host — not available.";
-			return this.#hasRemoteData ? "No messages yet." : "Loading transcript from host…";
+			if (this.#remoteUnavailable) return "记录在主机上——不可用。";
+			return this.#hasRemoteData ? "暂无消息。" : "正在从主机加载记录…";
 		}
-		if (!this.#deps.registry.get(this.#deps.agentId)?.sessionFile) return "No session file available yet.";
-		return "No messages yet.";
+		if (!this.#deps.registry.get(this.#deps.agentId)?.sessionFile) return "暂无会话文件。";
+		return "暂无消息。";
 	}
 }
diff --git a/pi-tui/src/overlays/agents-hub.ts b/pi-tui-zh/src/overlays/agents-hub.ts
index 8f6180f..98f5ffc 100644
--- a/pi-tui/src/overlays/agents-hub.ts
+++ b/pi-tui-zh/src/overlays/agents-hub.ts
@@ -291,7 +291,7 @@ export class AgentsHubComponent implements Component {
 		const counts: Record<AgentSource, number> = { project: 0, user: 0, bundled: 0 };
 		for (const agent of this.#allAgents) counts[agent.source]++;
 		const entries: SidebarEntry[] = [
-			{ id: "all", kind: "all", label: "All agents", annotation: String(this.#allAgents.length) },
+			{ id: "all", kind: "all", label: "全部代理", annotation: String(this.#allAgents.length) },
 		];
 		const sources = (["project", "user", "bundled"] as const).filter(source => counts[source] > 0);
 		if (sources.length > 0) {
@@ -307,7 +307,7 @@ export class AgentsHubComponent implements Component {
 			}
 		}
 		entries.push({ id: "sep:actions", kind: "separator", label: "" });
-		entries.push({ id: "new", kind: "new", label: "New agent" });
+		entries.push({ id: "new", kind: "new", label: "新建代理" });
 		this.#entries = entries;
 		if (!entries.some(entry => entry.id === this.#activeEntryId)) this.#activeEntryId = "all";
 	}
@@ -397,11 +397,11 @@ export class AgentsHubComponent implements Component {
 			}
 			case "prewalk": {
 				const pattern = this.#deps.effectivePrewalkPattern(agent);
-				return `${agent.name} prewalk: ${pattern ? `on (${pattern})` : "off"}`;
+				return `${agent.name} prewalk: ${pattern ? `on (${pattern})` : "关"}`;
 			}
 			case "advisor": {
 				const pattern = this.#deps.effectiveAdvisorPattern(agent);
-				return `${agent.name} advisor: ${pattern ? `on (${pattern})` : "off"}`;
+				return `${agent.name} advisor: ${pattern ? `on (${pattern})` : "关"}`;
 			}
 		}
 	}
@@ -416,11 +416,11 @@ export class AgentsHubComponent implements Component {
 				return agent.overrideModel ?? "auto";
 			case "prewalk": {
 				const pattern = this.#deps.effectivePrewalkPattern(agent);
-				return pattern ?? "off";
+				return pattern ?? "关";
 			}
 			case "advisor": {
 				const pattern = this.#deps.effectiveAdvisorPattern(agent);
-				return pattern ?? "off";
+				return pattern ?? "关";
 			}
 		}
 	}
@@ -458,46 +458,46 @@ export class AgentsHubComponent implements Component {
 			active ? theme.fg("accent", `${theme.status.enabled} ${label}`) : theme.fg(color, label);
 		if (property === "model") {
 			chips.push({
-				label: "pick model…",
-				styled: theme.fg("accent", "pick model…"),
+				label: "选择模型…",
+				styled: theme.fg("accent", "选择模型…"),
 				action: { kind: "pick", property },
 			});
 			chips.push({
-				label: "pattern…",
-				styled: theme.fg("muted", "pattern…"),
+				label: "模式…",
+				styled: theme.fg("muted", "模式…"),
 				action: { kind: "pattern", property },
 			});
 			if (agent.overrideModel) {
 				chips.push({
-					label: "clear override",
-					styled: theme.fg("warning", "clear override"),
+					label: "清除覆盖",
+					styled: theme.fg("warning", "清除覆盖"),
 					action: { kind: "set", property, value: undefined },
 				});
 			}
 		} else {
 			chips.push({
-				label: "agent default",
-				styled: mark("agent default", current === undefined),
+				label: "代理默认",
+				styled: mark("代理默认", current === undefined),
 				action: { kind: "set", property, value: undefined },
 			});
 			chips.push({
-				label: "on",
-				styled: mark("on", current === "on"),
-				action: { kind: "set", property, value: "on" },
+				label: "开",
+				styled: mark("开", current === "开"),
+				action: { kind: "set", property, value: "开" },
 			});
 			chips.push({
-				label: "off",
-				styled: mark("off", current === "off"),
-				action: { kind: "set", property, value: "off" },
+				label: "关",
+				styled: mark("关", current === "关"),
+				action: { kind: "set", property, value: "关" },
 			});
 			chips.push({
-				label: "pick model…",
-				styled: theme.fg("accent", "pick model…"),
+				label: "选择模型…",
+				styled: theme.fg("accent", "选择模型…"),
 				action: { kind: "pick", property },
 			});
 			chips.push({
-				label: "pattern…",
-				styled: theme.fg("muted", "pattern…"),
+				label: "模式…",
+				styled: theme.fg("muted", "模式…"),
 				action: { kind: "pattern", property },
 			});
 		}
@@ -989,7 +989,7 @@ export class AgentsHubComponent implements Component {
 		}
 		if (this.#notice) return truncateToWidth(theme.fg("success", ` ${this.#notice}`), width);
 		const entry = this.#activeEntry();
-		const scopeLabel = entry.kind === "source" ? `${entry.label} agents` : "All agents";
+		const scopeLabel = entry.kind === "source" ? `${entry.label} agents` : "全部代理";
 		const count = this.#rows.filter(rowDef => rowDef.kind === "agent").length;
 		return truncateToWidth(theme.fg("muted", ` ${scopeLabel} · ${count}`), width);
 	}
@@ -1071,8 +1071,8 @@ export class AgentsHubComponent implements Component {
 			const prewalk = this.#deps.effectivePrewalkPattern(agent);
 			const advisor = this.#deps.effectiveAdvisorPattern(agent);
 			const flagLine = [
-				`${theme.fg("muted", "prewalk:")} ${prewalk ? theme.fg("success", prewalk) : theme.fg("dim", "off")}`,
-				`${theme.fg("muted", "advisor:")} ${advisor ? theme.fg("success", advisor) : theme.fg("dim", "off")}`,
+				`${theme.fg("muted", "prewalk:")} ${prewalk ? theme.fg("success", prewalk) : theme.fg("dim", "关")}`,
+				`${theme.fg("muted", "advisor:")} ${advisor ? theme.fg("success", advisor) : theme.fg("dim", "关")}`,
 				agent.filePath ? theme.fg("dim", shortenPath(agent.filePath)) : "",
 			]
 				.filter(Boolean)
diff --git a/pi-tui/src/overlays/ask-dialog.ts b/pi-tui-zh/src/overlays/ask-dialog.ts
index 11daa08..8e94bf6 100644
--- a/pi-tui/src/overlays/ask-dialog.ts
+++ b/pi-tui-zh/src/overlays/ask-dialog.ts
@@ -777,7 +777,7 @@ export class AskDialogComponent implements Component {
 		const question = this.#questions[this.#currentQuestionIndex()];
 		// Enter advances in multi-question dialogs and submits single-question ones.
 		const enterAction = this.#questions.length > 1 ? "next" : "submit";
-		const action = question?.multi ? `Space toggle · Enter ${enterAction}` : "Enter select · n note";
+		const action = question?.multi ? `Space toggle · Enter ${enterAction}` : "Enter 选择 · n 备注";
 		const tabs = this.#hasSubmitTab() ? " · Tab/←/→" : "";
 		const expand = this.#expandHint();
 		if (this.#questionCanPage && indicator) {
diff --git a/pi-tui/src/overlays/btw-history-panel.ts b/pi-tui-zh/src/overlays/btw-history-panel.ts
index 538ae2f..48133b1 100644
--- a/pi-tui/src/overlays/btw-history-panel.ts
+++ b/pi-tui-zh/src/overlays/btw-history-panel.ts
@@ -60,11 +60,11 @@ interface RenderedTurn {
 }
 
 const STATUS: Record<BtwHistoryRecord["status"], { label: string; color: ThemeColor }> = {
-	running: { label: "Running", color: "accent" },
-	complete: { label: "Complete", color: "success" },
-	cancelled: { label: "Cancelled", color: "warning" },
-	error: { label: "Error", color: "error" },
-	interrupted: { label: "Interrupted", color: "warning" },
+	running: { label: "运行中", color: "accent" },
+	complete: { label: "已完成", color: "success" },
+	cancelled: { label: "已取消", color: "warning" },
+	error: { label: "出错", color: "error" },
+	interrupted: { label: "已中断", color: "warning" },
 };
 
 /** Session-local side questions. Selecting or copying never promotes them into chat. */
@@ -225,7 +225,7 @@ export class BtwHistoryPanel implements Component, Focusable {
 
 	#openComposer(record: BtwHistoryRecord): void {
 		const input = new Input();
-		input.prompt = theme.fg("accent", "Follow up: ");
+		input.prompt = theme.fg("accent", "追问：");
 		const composer: FollowUpComposer = { recordId: record.id, input, abortController: new AbortController() };
 		input.onEscape = () => {
 			composer.abortController.abort();
@@ -243,16 +243,16 @@ export class BtwHistoryPanel implements Component, Focusable {
 		if (this.#followUpPending || this.#composer !== composer) return;
 		const question = value.trim();
 		if (!question) {
-			composer.notice = "Enter a follow-up question.";
+			composer.notice = "输入追问内容。";
 			return;
 		}
 		const record = this.#records.find(record => record.id === composer.recordId);
 		if (!record || !this.#canFollowUp(record)) {
-			composer.notice = "A BTW request is busy. Try again when it finishes.";
+			composer.notice = "有 BTW 请求正在执行，请等它结束后重试。";
 			return;
 		}
 		this.#followUpPending = true;
-		composer.notice = "Starting follow-up…";
+		composer.notice = "正在开始追问…";
 		this.#options.requestRender();
 		try {
 			const accepted = await this.#options.onFollowUp!(record, question, composer.abortController.signal);
@@ -263,11 +263,11 @@ export class BtwHistoryPanel implements Component, Focusable {
 				this.#focus = "answer";
 				this.#followLatest = true;
 			} else {
-				composer.notice = "Follow-up was not started. Your draft is kept; Enter to retry.";
+				composer.notice = "追问未开始。草稿已保留；按 Enter 重试。";
 			}
 		} catch {
 			if (this.#composer === composer) {
-				composer.notice = "Could not start the follow-up. Your draft is kept; Enter to retry.";
+				composer.notice = "无法开始追问。草稿已保留；按 Enter 重试。";
 			}
 		} finally {
 			this.#followUpPending = false;
@@ -339,7 +339,7 @@ export class BtwHistoryPanel implements Component, Focusable {
 		const rowSpan = width < 36 && height >= 2 ? 2 : 1;
 		this.#listHeight = Math.max(1, Math.floor(height / rowSpan));
 		if (this.#records.length === 0) {
-			return new Text(theme.fg("muted", "No side questions yet.\n\nUse /btw QUESTION to start one."), 0, 0)
+			return new Text(theme.fg("muted", "还没有侧边提问。\n\n用 /btw QUESTION 开始一个。"), 0, 0)
 				.render(width)
 				.slice(0, height);
 		}
@@ -411,22 +411,22 @@ export class BtwHistoryPanel implements Component, Focusable {
 				width,
 			),
 			"",
-			theme.bold(theme.fg("accent", "Question")),
+			theme.bold(theme.fg("accent", "问题")),
 			...question.render(width),
 			"",
-			theme.bold(theme.fg("accent", "Answer")),
+			theme.bold(theme.fg("accent", "回答")),
 		];
 		if (turn.answer.trim()) lines.push(...answer.render(width));
 		else
 			lines.push(
 				...wrapTextWithAnsi(
-					theme.fg("dim", turn.status === "running" ? "Waiting for response…" : "No answer text."),
+					theme.fg("dim", turn.status === "running" ? "正在等待响应…" : "没有回答内容。"),
 					width,
 				),
 			);
 		if (turn.error) lines.push("", ...wrapTextWithAnsi(theme.fg("error", sanitizeErrorLine(turn.error)), width));
 		if (turn.status === "interrupted")
-			lines.push("", ...wrapTextWithAnsi(theme.fg("muted", "Not resumed in this view."), width));
+			lines.push("", ...wrapTextWithAnsi(theme.fg("muted", "未在此视图中恢复。"), width));
 		return {
 			turn: {
 				question: turn.question,
@@ -450,7 +450,7 @@ export class BtwHistoryPanel implements Component, Focusable {
 			const inner = Math.max(1, width - 1);
 			const lines: string[] = [];
 			if (record && this.#isCopied(record)) {
-				lines.push(...wrapTextWithAnsi(theme.fg("success", "✓ Copied to clipboard"), inner), "");
+				lines.push(...wrapTextWithAnsi(theme.fg("success", "✓ 已复制到剪贴板"), inner), "");
 			}
 			if (record) {
 				const turns = getBtwTurns(record);
@@ -463,7 +463,7 @@ export class BtwHistoryPanel implements Component, Focusable {
 				this.#turns.length = turns.length;
 			} else {
 				lines.push(
-					...wrapTextWithAnsi(theme.fg("muted", "No side questions yet. Use /btw QUESTION to start one."), inner),
+					...wrapTextWithAnsi(theme.fg("muted", "还没有侧边提问。用 /btw QUESTION 开始一个。"), inner),
 				);
 			}
 			this.#detailRecord = record;
@@ -476,7 +476,7 @@ export class BtwHistoryPanel implements Component, Focusable {
 	}
 
 	#focusLabel(focus: "list" | "answer"): string {
-		const label = focus === "list" ? `History (${this.#records.length})` : "Details";
+		const label = focus === "list" ? `历史（${this.#records.length}）` : "详情";
 		return this.#focus === focus
 			? theme.bold(theme.fg("accent", `${theme.nav.cursor} ${label}`))
 			: theme.fg("muted", `  ${label}`);
@@ -506,13 +506,13 @@ export class BtwHistoryPanel implements Component, Focusable {
 			? [rawKeyHint("Enter", this.#followUpPending ? "starting…" : "send"), rawKeyHint("Esc", "cancel")]
 			: [
 					rawKeyHint("Esc", latest?.status === "running" ? "cancel" : "close"),
-					rawKeyHint("Tab/Ctrl+/", "switch pane"),
+					rawKeyHint("Tab/Ctrl+/", "切换面板"),
 				];
 		if (!composer) {
-			if (record && this.#canFollowUp(record)) actions.push(rawKeyHint("f/Enter", "follow up"));
+			if (record && this.#canFollowUp(record)) actions.push(rawKeyHint("f/Enter", "追问"));
 			if (record && getBtwCopyText(record) !== undefined) {
-				if (this.#isCopied(record)) actions.push(theme.fg("success", "✓ copied · c to copy again"));
-				else actions.push(rawKeyHint("c", inner < 40 ? "copy" : "copy answer"));
+				if (this.#isCopied(record)) actions.push(theme.fg("success", "✓ 已复制 · c 再次复制"));
+				else actions.push(rawKeyHint("c", inner < 40 ? "copy" : "复制回答"));
 			}
 		}
 		const actionLines = wrapTextWithAnsi(actions.join(" · "), inner).slice(0, framed ? 2 : 1);
@@ -522,7 +522,7 @@ export class BtwHistoryPanel implements Component, Focusable {
 			const availableRows = Math.max(1, height - chrome - 1);
 			if (availableRows >= 2) {
 				composerLines.push(
-					theme.fg("dim", truncateToWidth(`Topic: ${sanitizeDisplayLine(record?.question ?? "")}`, inner)),
+					theme.fg("dim", truncateToWidth(`主题：${sanitizeDisplayLine(record?.question ?? "")}`, inner)),
 				);
 			}
 			if (composer.notice && availableRows >= 3) {
@@ -553,7 +553,7 @@ export class BtwHistoryPanel implements Component, Focusable {
 		}
 		const lines: string[] = [];
 		if (framed) {
-			lines.push(topBorder(width, "BTW history"));
+			lines.push(topBorder(width, "BTW 历史"));
 			lines.push(this.#labels.render(width)[0] ?? "");
 		} else if (height >= 3) {
 			lines.push(truncateToWidth(this.#focusLabel(this.#focus), width));
diff --git a/pi-tui/src/overlays/btw-panel.ts b/pi-tui-zh/src/overlays/btw-panel.ts
index 12547b3..78a6452 100644
--- a/pi-tui/src/overlays/btw-panel.ts
+++ b/pi-tui-zh/src/overlays/btw-panel.ts
@@ -75,7 +75,7 @@ export class BtwPanelComponent extends OverlayPanel {
 
 	#setCopied(copied: boolean): void {
 		this.#copied = copied;
-		this.title = copied ? `${this.#baseTitle} ✓ Copied` : this.#baseTitle;
+		this.title = copied ? `${this.#baseTitle} ✓ 已复制` : this.#baseTitle;
 	}
 
 	/** Shows that the completed answer is being promoted into the chat session. */
@@ -131,24 +131,24 @@ export class BtwPanelComponent extends OverlayPanel {
 	#footerLine(): string {
 		switch (this.#state) {
 			case "running":
-				return theme.fg("muted", "Esc to cancel");
+				return theme.fg("muted", "Esc 取消");
 			case "complete": {
 				const actions: string[] = [];
-				if (this.isCopyable()) actions.push(this.#copied ? "c to copy again" : "c to copy");
-				if (this.#canFollowUp?.()) actions.push("f to follow up");
-				if (this.#canBranch?.() ?? this.isBranchable()) actions.push("b to branch");
-				actions.push("Esc to close");
+				if (this.isCopyable()) actions.push(this.#copied ? "c 再次复制" : "c 复制");
+				if (this.#canFollowUp?.()) actions.push("f 追问");
+				if (this.#canBranch?.() ?? this.isBranchable()) actions.push("b 分叉");
+				actions.push("Esc 关闭");
 				if (this.#copied) {
-					return `${theme.fg("success", "✓ Copied to clipboard")}${theme.fg("muted", actions.length > 0 ? ` · ${actions.join(" · ")}` : "")}`;
+					return `${theme.fg("success", "✓ 已复制到剪贴板")}${theme.fg("muted", actions.length > 0 ? ` · ${actions.join(" · ")}` : "")}`;
 				}
 				return theme.fg("muted", actions.join(" · "));
 			}
 			case "branching":
-				return theme.fg("muted", `${theme.status.pending} Branching to chat…`);
+				return theme.fg("muted", `${theme.status.pending} 正在分叉到对话…`);
 			case "aborted":
-				return theme.fg("warning", `${theme.status.warning} Cancelled · Esc to close`);
+				return theme.fg("warning", `${theme.status.warning} 已取消 · Esc 关闭`);
 			case "error":
-				return theme.fg("error", `${theme.status.error} Error · Esc to close`);
+				return theme.fg("error", `${theme.status.error} 出错 · Esc 关闭`);
 		}
 	}
 
@@ -159,7 +159,7 @@ export class BtwPanelComponent extends OverlayPanel {
 		const text = this.#visibleAnswer;
 		if (!text) {
 			const waiting =
-				this.#state === "running" ? `${theme.status.pending} Waiting for response…` : "No text returned.";
+				this.#state === "running" ? `${theme.status.pending} 等待响应…` : "未返回文本。";
 			return new Text(theme.fg("dim", waiting), 0, 0);
 		}
 		return new Markdown(text, 0, 0, getMarkdownTheme());
diff --git a/pi-tui/src/overlays/composer-shape-registry.ts b/pi-tui-zh/src/overlays/composer-shape-registry.ts
index 7667465..e7fa02e 100644
--- a/pi-tui/src/overlays/composer-shape-registry.ts
+++ b/pi-tui-zh/src/overlays/composer-shape-registry.ts
@@ -8,43 +8,43 @@ export type ComposerShape = string;
 export const BUILTIN_COMPOSER_SHAPES = [
 	{
 		value: "band",
-		label: "Status Band (Default)",
-		description: "Flush soft-capped status band above a curved prompt, no frame",
+		label: "状态带（默认）",
+		description: "弯曲提示符上方平顶软上限状态带，无边框",
 	},
 	{
 		value: "box",
-		label: "Rounded Box",
-		description: "Status line embedded in top border, compact 2-line prompt",
+		label: "圆角框",
+		description: "状态行嵌入上边框，紧凑两行提示符",
 	},
 	{
 		value: "claude",
 		label: "Claude Code",
-		description: "Full-width horizontal rules above and below, status line at bottom",
+		description: "上下通栏横线，状态行在底部",
 	},
 	{
 		value: "pi",
 		label: "Pi",
-		description: "Framed horizontal rules with status line at bottom",
+		description: "框式横线，状态行在底部",
 	},
 	{
 		value: "borderless",
-		label: "Borderless",
-		description: "Clean prompt glyph with status line at bottom, no box borders",
+		label: "无边框",
+		description: "简洁提示符字形，状态行在底部，无框",
 	},
 	{
 		value: "rule",
-		label: "Top Rule Dock",
-		description: "Single top rule with status docked onto it and below",
+		label: "顶栏停靠",
+		description: "单条顶部横线，状态停靠其上与其下",
 	},
 	{
 		value: "field",
-		label: "Compact Field",
-		description: "Filled one-row field with accent end caps",
+		label: "紧凑输入场",
+		description: "单行填充输入场，两端强调收尾",
 	},
 	{
 		value: "rail",
-		label: "Accent Rail",
-		description: "Filled one-row field anchored by a single accent rail",
+		label: "强调轨道",
+		description: "由单条强调轨道锚定的单行填充输入场",
 	},
 ] as const;
 
diff --git a/pi-tui/src/overlays/copy-selector.ts b/pi-tui-zh/src/overlays/copy-selector.ts
index 93a2484..540fde6 100644
--- a/pi-tui/src/overlays/copy-selector.ts
+++ b/pi-tui-zh/src/overlays/copy-selector.ts
@@ -409,9 +409,9 @@ export class CopySelectorComponent implements Component {
 			const selected = index === this.#blockSelected;
 			const captionColor: ThemeColor = selected ? OUTLINE_COLOR : "dim";
 			const controls: Array<{ action: ControlRegion["action"]; text: string }> = [
-				{ action: "copy", text: `${theme.cmd.copy} copy` },
+				{ action: "copy", text: `${theme.cmd.copy} 复制` },
 			];
-			if (block.href && this.deps.onOpen) controls.push({ action: "open", text: `${theme.cmd.share} open` });
+			if (block.href && this.deps.onOpen) controls.push({ action: "open", text: `${theme.cmd.share} 打开` });
 			const controlsWidth = controls.reduce((sum, control) => sum + visibleWidth(control.text) + 2, 0);
 			const summary = truncateToWidth(
 				`${index + 1}/${blocks.length}${theme.sep.dot}${block.label}${theme.sep.dot}${raw.length} line${raw.length === 1 ? "" : "s"}`,
@@ -471,7 +471,7 @@ function recentEntries(entries: TranscriptEntry[], limit: number): TranscriptEnt
 }
 
 /** Raw multi-line text of a user message (string or text blocks). */
-function rawUserText(message: Extract<SessionMessageEntry["message"], { role: "user" }>): string {
+function rawUserText(message: Extract<SessionMessageEntry["消息"], { role: "user" }>): string {
 	if (typeof message.content === "string") return message.content;
 	return message.content
 		.filter((block): block is { type: "text"; text: string } => block.type === "text")
@@ -480,7 +480,7 @@ function rawUserText(message: Extract<SessionMessageEntry["message"], { role: "u
 }
 
 /** Concatenated visible text blocks of an assistant message. */
-function assistantVisibleText(message: Extract<SessionMessageEntry["message"], { role: "assistant" }>): string {
+function assistantVisibleText(message: Extract<SessionMessageEntry["消息"], { role: "assistant" }>): string {
 	let text = "";
 	for (const content of message.content) {
 		if (content.type === "text") text += content.text;
@@ -489,7 +489,7 @@ function assistantVisibleText(message: Extract<SessionMessageEntry["message"], {
 }
 
 /** Joined text content of a tool result. */
-function toolResultText(message: Extract<SessionMessageEntry["message"], { role: "toolResult" }>): string {
+function toolResultText(message: Extract<SessionMessageEntry["消息"], { role: "toolResult" }>): string {
 	return message.content
 		.filter((block): block is { type: "text"; text: string } => block.type === "text")
 		.map(block => block.text)
@@ -506,7 +506,7 @@ function pushMarkdownBlocks(blocks: CopyBlock[], text: string): void {
 				language: block.lang || undefined,
 			});
 		} else {
-			blocks.push({ label: "quote", content: block.text });
+			blocks.push({ label: "引用", content: block.text });
 		}
 	}
 	// Links follow the message's blocks. The preview shows the whole URL on one
@@ -537,7 +537,7 @@ function collectBlocks(entries: readonly TranscriptEntry[]): CopyBlock[] {
 					const command = commandFromToolCall(content);
 					if (command) {
 						blocks.push({
-							label: command.kind === "bash" ? "bash command" : "eval code",
+							label: command.kind === "bash" ? "bash command" : "eval 代码",
 							content: command.code,
 							language: command.language,
 						});
@@ -547,16 +547,16 @@ function collectBlocks(entries: readonly TranscriptEntry[]): CopyBlock[] {
 			}
 			case "toolResult": {
 				const text = toolResultText(message);
-				if (text) blocks.push({ label: `${message.toolName} result`, content: text });
+				if (text) blocks.push({ label: `${message.toolName} 结果`, content: text });
 				break;
 			}
 			case "bashExecution":
-				blocks.push({ label: "command", content: message.command, language: "bash" });
-				if (message.output.trim()) blocks.push({ label: "output", content: message.output });
+				blocks.push({ label: "命令", content: message.command, language: "bash" });
+				if (message.output.trim()) blocks.push({ label: "输出", content: message.output });
 				break;
 			case "pythonExecution":
-				blocks.push({ label: "eval code", content: message.code, language: "python" });
-				if (message.output.trim()) blocks.push({ label: "output", content: message.output });
+				blocks.push({ label: "eval 代码", content: message.code, language: "python" });
+				if (message.output.trim()) blocks.push({ label: "输出", content: message.output });
 				break;
 			default:
 				break;
@@ -571,35 +571,35 @@ function targetCopy(target: OutlineTarget, blocks: readonly CopyBlock[]): { cont
 	const message = transcriptEntryMessage(entry);
 	switch (message?.role) {
 		case "user":
-			return { content: rawUserText(message), label: "user message" };
+			return { content: rawUserText(message), label: "用户消息" };
 		case "assistant": {
 			const text = assistantVisibleText(message);
-			if (text) return { content: text, label: "assistant message" };
+			if (text) return { content: text, label: "助手消息" };
 			break;
 		}
 		case "toolResult": {
 			const text = toolResultText(message);
-			if (text) return { content: text, label: `${message.toolName} result` };
+			if (text) return { content: text, label: `${message.toolName} 结果` };
 			break;
 		}
 		case "bashExecution":
 			return {
 				content: [message.command, message.output].filter(part => part.trim()).join("\n"),
-				label: "bash execution",
+				label: "bash 执行",
 			};
 		case "pythonExecution":
 			return {
 				content: [message.code, message.output].filter(part => part.trim()).join("\n"),
-				label: "eval execution",
+				label: "eval 执行",
 			};
 		case "compactionSummary":
 		case "branchSummary":
-			return { content: message.summary, label: "summary" };
+			return { content: message.summary, label: "摘要" };
 		case "custom":
 		case "hookMessage": {
 			// A user-invoked skill/collab prompt copies as what the user typed, not the expanded body.
 			const draft = message.role === "custom" ? userTurnDraft(entry) : undefined;
-			if (draft?.trim()) return { content: draft, label: "user message" };
+			if (draft?.trim()) return { content: draft, label: "用户消息" };
 			const content =
 				typeof message.content === "string"
 					? message.content
@@ -607,12 +607,12 @@ function targetCopy(target: OutlineTarget, blocks: readonly CopyBlock[]): { cont
 							.filter((block): block is { type: "text"; text: string } => block.type === "text")
 							.map(block => block.text)
 							.join("\n");
-			if (content.trim()) return { content, label: "message" };
+			if (content.trim()) return { content, label: "消息" };
 			break;
 		}
 		default:
 			break;
 	}
 	// No direct prose (e.g. a pure tool turn): fall back to its blocks joined.
-	return { content: blocks.map(block => block.content).join("\n\n"), label: "turn content" };
+	return { content: blocks.map(block => block.content).join("\n\n"), label: "轮次内容" };
 }
diff --git a/pi-tui/src/overlays/login-dialog.ts b/pi-tui-zh/src/overlays/login-dialog.ts
index 24a8e88..fa34910 100644
--- a/pi-tui/src/overlays/login-dialog.ts
+++ b/pi-tui-zh/src/overlays/login-dialog.ts
@@ -172,7 +172,7 @@ export class LoginDialogComponent extends OverlayPanel {
 		this.#contentContainer.addChild(new Spacer(1));
 		this.#contentContainer.addChild(new Text(theme.fg("text", prompt.message), 0, 0));
 		if (prompt.placeholder) {
-			this.#contentContainer.addChild(new Text(theme.fg("dim", `e.g., ${prompt.placeholder}`), 0, 0));
+			this.#contentContainer.addChild(new Text(theme.fg("dim", `例如: ${prompt.placeholder}`), 0, 0));
 		}
 		this.#contentContainer.addChild(this.#input);
 		this.#contentContainer.addChild(new Text(theme.fg("dim", "(Escape to cancel, Enter to submit)"), 0, 0));
diff --git a/pi-tui/src/overlays/mcp-add-wizard.ts b/pi-tui-zh/src/overlays/mcp-add-wizard.ts
index c3d947e..1a47c10 100644
--- a/pi-tui/src/overlays/mcp-add-wizard.ts
+++ b/pi-tui-zh/src/overlays/mcp-add-wizard.ts
@@ -449,7 +449,7 @@ export class MCPAddWizard extends OverlayPanel {
 	#renderNameStep(): void {
 		this.#inputStep({
 			heading: "Step 1: Server Name",
-			prompt: "Enter a unique name for this server:",
+			prompt: "为此服务器输入唯一名称:",
 			initial: this.#state.name,
 			hint: "[Only letters, numbers, dash, underscore, dot, colon]\n[Enter to continue, Esc to cancel]",
 			optional: false,
@@ -460,7 +460,7 @@ export class MCPAddWizard extends OverlayPanel {
 	#renderTransportStep(): void {
 		this.#choiceStep({
 			heading: "Step 2: Transport Type",
-			intro: new Text("Select the transport type:", 0, 0),
+			intro: new Text("选择传输类型:", 0, 0),
 			choices: [
 				{ label: "stdio (Local process)" },
 				{ label: "http (HTTP server)" },
@@ -473,7 +473,7 @@ export class MCPAddWizard extends OverlayPanel {
 	#renderCommandStep(): void {
 		this.#inputStep({
 			heading: "Step 3: Command",
-			prompt: "Enter the command to run:",
+			prompt: "输入要运行的命令:",
 			initial: this.#state.command,
 			hint: "[Enter to continue, Esc to go back]",
 			optional: false,
@@ -483,7 +483,7 @@ export class MCPAddWizard extends OverlayPanel {
 	#renderArgsStep(): void {
 		this.#inputStep({
 			heading: "Step 4: Arguments (Optional)",
-			prompt: "Enter command arguments (space-separated):",
+			prompt: "输入命令参数(空格分隔):",
 			initial: this.#state.args,
 			hint: "[Press Enter to skip or continue]",
 			optional: true,
@@ -493,7 +493,7 @@ export class MCPAddWizard extends OverlayPanel {
 	#renderUrlStep(): void {
 		this.#inputStep({
 			heading: "Step 3: Server URL",
-			prompt: "Enter the server URL:",
+			prompt: "输入服务器 URL:",
 			initial: this.#state.url,
 			hint: "[Must start with http:// or https://]\n[Enter to continue, Esc to go back]",
 			optional: false,
@@ -505,14 +505,14 @@ export class MCPAddWizard extends OverlayPanel {
 		this.#choiceStep({
 			heading: "Step: How to provide the key?",
 			choices: [{ label: "Environment variable" }, { label: "HTTP header" }],
-			hint: "[↑↓ to navigate, Enter to select, Esc to go back]",
+			hint: "[↑↓ 导航,Enter 选择,Esc 返回]",
 		});
 	}
 
 	#renderEnvVarNameStep(): void {
 		this.#inputStep({
 			heading: "Step: Environment Variable Name",
-			prompt: "Enter the environment variable name:",
+			prompt: "输入环境变量名:",
 			initial: this.#state.envVarName,
 			hint: "[Enter to continue, Esc to go back]",
 			optional: false,
@@ -522,7 +522,7 @@ export class MCPAddWizard extends OverlayPanel {
 	#renderHeaderNameStep(): void {
 		this.#inputStep({
 			heading: "Step: HTTP Header Name",
-			prompt: "Enter the HTTP header name:",
+			prompt: "输入 HTTP 头名称:",
 			initial: this.#state.headerName,
 			hint: "[Enter to continue, Esc to go back]",
 			optional: false,
@@ -536,19 +536,19 @@ export class MCPAddWizard extends OverlayPanel {
 		this.#choiceStep({
 			heading: "Step: Configuration Scope",
 			choices: [{ label: `User level (${userPathLabel})` }, { label: `Project level (${projectPathLabel})` }],
-			hint: "[↑↓ to navigate, Enter to select, Esc to go back]",
+			hint: "[↑↓ 导航,Enter 选择,Esc 返回]",
 		});
 	}
 
 	#renderConfirmStep(): void {
 		const summary = new Container();
-		summary.addChild(new Text(`Name: ${theme.fg("accent", this.#state.name)}`, 0, 0));
-		summary.addChild(new Text(`Type: ${this.#state.transport}`, 0, 0));
+		summary.addChild(new Text(`名称: ${theme.fg("accent", this.#state.name)}`, 0, 0));
+		summary.addChild(new Text(`类型: ${this.#state.transport}`, 0, 0));
 
 		if (this.#state.transport === "stdio") {
-			summary.addChild(new Text(`Command: ${this.#state.command}`, 0, 0));
+			summary.addChild(new Text(`命令: ${this.#state.command}`, 0, 0));
 			if (this.#state.args) {
-				summary.addChild(new Text(`Args: ${this.#state.args}`, 0, 0));
+				summary.addChild(new Text(`参数: ${this.#state.args}`, 0, 0));
 			}
 		} else {
 			summary.addChild(new Text(`URL: ${sanitize(this.#state.url)}`, 0, 0));
@@ -556,27 +556,27 @@ export class MCPAddWizard extends OverlayPanel {
 
 		// Auth info
 		if (this.#state.authMethod === "none") {
-			summary.addChild(new Text("Auth: None", 0, 0));
+			summary.addChild(new Text("认证: 无", 0, 0));
 		} else if (this.#state.authMethod === "oauth") {
-			summary.addChild(new Text("Auth: OAuth (authenticated)", 0, 0));
+			summary.addChild(new Text("认证: OAuth(已认证)", 0, 0));
 		} else if (this.#state.authMethod === "manual") {
 			if (this.#state.authLocation === "env") {
-				summary.addChild(new Text(`Auth: API key via env (${this.#state.envVarName})`, 0, 0));
+				summary.addChild(new Text(`认证: 通过环境变量传 API 密钥 (${this.#state.envVarName})`, 0, 0));
 			} else {
-				summary.addChild(new Text(`Auth: API key via header (${this.#state.headerName})`, 0, 0));
+				summary.addChild(new Text(`认证: 通过请求头传 API 密钥 (${this.#state.headerName})`, 0, 0));
 			}
 		}
 
-		const scopeLabel = this.#state.scope === "user" ? "User level" : "Project level";
-		summary.addChild(new Text(`Scope: ${scopeLabel}`, 0, 0));
+		const scopeLabel = this.#state.scope === "user" ? "用户级" : "项目级";
+		summary.addChild(new Text(`范围: ${scopeLabel}`, 0, 0));
 		summary.addChild(new Spacer(1));
-		summary.addChild(new Text("Save this configuration?", 0, 0));
+		summary.addChild(new Text("保存此配置?", 0, 0));
 
 		this.#choiceStep({
-			heading: "Review Configuration",
+			heading: "检查配置",
 			intro: summary,
-			choices: [{ label: "Yes" }, { label: "No" }],
-			hint: "[↑↓ to navigate, Enter to select, Esc to go back]",
+			choices: [{ label: "是" }, { label: "否" }],
+			hint: "[↑↓ 导航,Enter 选择,Esc 返回]",
 			kind: "confirm",
 		});
 	}
@@ -931,14 +931,14 @@ export class MCPAddWizard extends OverlayPanel {
 				{ label: "OAuth flow (web-based)", description: "(opens browser)" },
 				{ label: "Manual API key/token", description: "(paste or use shell command)" },
 			],
-			hint: "[↑↓ to navigate, Enter to select, Esc to go back]",
+			hint: "[↑↓ 导航,Enter 选择,Esc 返回]",
 		});
 	}
 
 	#renderOAuthAuthUrlStep(): void {
 		this.#inputStep({
 			heading: "OAuth: Authorization URL",
-			prompt: "Enter the OAuth authorization endpoint:",
+			prompt: "输入 OAuth 授权端点:",
 			initial: this.#state.oauthAuthUrl,
 			details: ["e.g., https://auth.example.com/oauth/authorize"],
 			hint: "[Enter to continue, Esc to go back]",
@@ -949,7 +949,7 @@ export class MCPAddWizard extends OverlayPanel {
 	#renderOAuthTokenUrlStep(): void {
 		this.#inputStep({
 			heading: "OAuth: Token URL",
-			prompt: "Enter the OAuth token endpoint:",
+			prompt: "输入 OAuth 令牌端点:",
 			initial: this.#state.oauthTokenUrl,
 			details: ["e.g., https://auth.example.com/oauth/token"],
 			hint: "[Enter to continue, Esc to go back]",
@@ -960,7 +960,7 @@ export class MCPAddWizard extends OverlayPanel {
 	#renderOAuthClientIdStep(): void {
 		this.#inputStep({
 			heading: "OAuth: Client ID",
-			prompt: "Enter your OAuth client ID:",
+			prompt: "输入你的 OAuth 客户端 ID:",
 			initial: this.#state.oauthClientId,
 			hint: "[Enter to continue, Esc to go back]",
 			optional: false,
@@ -970,9 +970,9 @@ export class MCPAddWizard extends OverlayPanel {
 	#renderOAuthClientSecretStep(): void {
 		this.#inputStep({
 			heading: "OAuth: Client Secret (Optional)",
-			prompt: "Enter your OAuth client secret:",
+			prompt: "输入你的 OAuth 客户端密钥:",
 			initial: this.#state.oauthClientSecret,
-			details: ["(Leave empty for PKCE-only flows)"],
+			details: ["(PKCE 流程可留空)"],
 			hint: "[Enter to continue, Esc to go back]",
 			optional: true,
 		});
@@ -981,7 +981,7 @@ export class MCPAddWizard extends OverlayPanel {
 	#renderOAuthScopesStep(): void {
 		this.#inputStep({
 			heading: "OAuth: Scopes (Optional)",
-			prompt: "Enter OAuth scopes (space-separated):",
+			prompt: "输入 OAuth 作用域(空格分隔):",
 			initial: this.#state.oauthScopes,
 			details: ["e.g., read write"],
 			hint: "[Enter to continue, Esc to go back]",
@@ -996,22 +996,22 @@ export class MCPAddWizard extends OverlayPanel {
 			intro.addChild(new Text(errorLines[index] ?? "", 0, 0));
 			intro.addChild(new Spacer(1));
 		}
-		intro.addChild(new Text("Choose next action:", 0, 0));
+		intro.addChild(new Text("选择下一步操作:", 0, 0));
 		this.#choiceStep({
 			heading: this.#oauthErrorHeading?.text ?? "OAuth authentication failed",
 			headingTone: this.#oauthErrorHeading?.tone ?? "error",
 			intro,
-			choices: [{ label: "Retry OAuth authentication" }, { label: "Edit OAuth settings" }],
-			hint: "[↑↓ to navigate, Enter to select, Esc to go back]",
+			choices: [{ label: "重试 OAuth 认证" }, { label: "编辑 OAuth 设置" }],
+			hint: "[↑↓ 导航,Enter 选择,Esc 返回]",
 		});
 	}
 
 	#renderApiKeyStep(): void {
 		this.#inputStep({
 			heading: "API Key Required",
-			prompt: "Enter your API key or token:",
+			prompt: "输入你的 API 密钥或令牌:",
 			initial: this.#state.apiKey,
-			details: ["(Supports !command for password manager)"],
+			details: ["(支持 !command 调用密码管理器)"],
 			hint: "[Enter to continue, Esc to go back]",
 			optional: false,
 		});
@@ -1261,10 +1261,10 @@ export class MCPAddWizard extends OverlayPanel {
 
 			// Show success message
 			const healthBody = new Container();
-			healthBody.addChild(new Text(theme.fg("muted", "Running connection health check..."), 0, 0));
+			healthBody.addChild(new Text(theme.fg("muted", "正在运行连接健康检查..."), 0, 0));
 			const spinnerFrames = theme.spinnerFrames;
 			const initialFrame = spinnerFrames[0] ?? "|";
-			const healthText = new Text(theme.fg("muted", `${initialFrame} Checking server connection...`), 0, 0);
+			const healthText = new Text(theme.fg("muted", `${initialFrame} 正在检查服务器连接...`), 0, 0);
 			healthBody.addChild(healthText);
 			this.#asyncStep("✓ Authentication successful!", "success", healthBody);
 
diff --git a/pi-tui/src/overlays/model-browser.ts b/pi-tui-zh/src/overlays/model-browser.ts
index 0a892e1..0ad1b94 100644
--- a/pi-tui/src/overlays/model-browser.ts
+++ b/pi-tui-zh/src/overlays/model-browser.ts
@@ -557,7 +557,7 @@ function formatCostPair(model: Model): string {
  */
 export function modelSearchText({ provider, id, model }: ModelBrowserItem): string {
 	const base = `${provider}/${id}`;
-	return isFreeModel(model) ? `${base} free` : base;
+	return isFreeModel(model) ? `${base} 免费` : base;
 }
 
 /** Provider-supplied blurb, flattened to a single renderable detail-line cell. */
diff --git a/pi-tui/src/overlays/model-hub.ts b/pi-tui-zh/src/overlays/model-hub.ts
index 2b68b3a..f51491e 100644
--- a/pi-tui/src/overlays/model-hub.ts
+++ b/pi-tui-zh/src/overlays/model-hub.ts
@@ -2019,7 +2019,7 @@ export class ModelHubComponent implements Component {
 
 	#statusRow(width: number): string {
 		if (this.#assignmentPending) {
-			return truncateToWidth(theme.fg("accent", " Applying model…"), width);
+			return truncateToWidth(theme.fg("accent", " 正在应用模型…"), width);
 		}
 		if (this.#assigning !== null) {
 			if (this.#assigning.kind === "fallbackKey") {
@@ -2266,20 +2266,20 @@ export class ModelHubComponent implements Component {
 		const strip = this.#strip;
 		if (strip) {
 			if (strip.kind === "roleName") {
-				return "Enter create + pick model · Esc cancel";
+				return "Enter 创建并选择模型 · Esc 取消";
 			}
-			if (strip.kind === "role") return "←/→ choose · Enter assign/clear · Esc cancel";
-			if (strip.kind === "scope") return "←/→ save scope · Enter choose · Esc cancel";
+			if (strip.kind === "role") return "←/→ 选择 · Enter 分配/清除 · Esc 取消";
+			if (strip.kind === "scope") return "←/→ 保存范围 · Enter 选择 · Esc 取消";
 			return "←/→ thinking level · Enter apply · Esc keep";
 		}
 		if (this.#assigning !== null) {
 			switch (this.#assigning.kind) {
 				case "fallback":
-					return "Enter pick fallback · ↑/↓ providers · type to search · Alt+←/→ kind · Esc cancel";
+					return "Enter 选择回退 · ↑/↓ 提供商 · 输入搜索 · Alt+←/→ 类型 · Esc 取消";
 				case "fallbackKey":
-					return "Enter pick the protected model · ↑/↓ providers · type to search · Alt+←/→ kind · Esc cancel";
+					return "Enter 选择受保护模型 · ↑/↓ 提供商 · 输入搜索 · Alt+←/→ 类型 · Esc 取消";
 				default:
-					return "Enter assign · ↑/↓ providers · type to search · Alt+←/→ kind · Esc cancel";
+					return "Enter 分配 · ↑/↓ 提供商 · 输入搜索 · Alt+←/→ 类型 · Esc 取消";
 			}
 		}
 		const entry = this.#activeEntry();
@@ -2293,8 +2293,8 @@ export class ModelHubComponent implements Component {
 				// inherit and unknown models have no ladder to offer, so the
 				// action would be inert there.
 				const editable = this.#resolveFallbackEntry(row.role, row.chainIndex) !== undefined;
-				const thinking = editable ? " · t thinking" : "";
-				return `↑/↓ rows · Enter replace · f add another · x remove${thinking} · [/] reorder · ← providers`;
+				const thinking = editable ? " · t 思考" : "";
+				return `↑/↓ 行 · Enter 替换 · f 再添加 · x 移除${thinking} · [/] 排序 · ← 提供商`;
 			}
 			if (row?.kind === "chainKey") {
 				return "↑/↓ rows · Enter/f add fallback · x clear chain · ← providers";
@@ -2305,10 +2305,10 @@ export class ModelHubComponent implements Component {
 			return "↑/↓ rows · Enter pick · f fallback · x clear · t thinking · c cycle · [/] reorder · n new";
 		}
 		if (entry.kind === "provider" && entry.locked) {
-			return entry.oauth ? "Enter log in · ↑/↓ providers · Esc close" : "↑/↓ providers · Esc close";
+			return entry.oauth ? "Enter 登录 · ↑/↓ 提供商 · Esc 关闭" : "↑/↓ 提供商 · Esc 关闭";
 		}
-		const arrows = this.#focus === "scope" ? "↑/↓ providers · → models" : "↑/↓ models · ← providers";
-		const refresh = entry.kind === "provider" ? " · F5 refresh" : "";
+		const arrows = this.#focus === "scope" ? "↑/↓ 提供商 · → 模型" : "↑/↓ 模型 · ← 提供商";
+		const refresh = entry.kind === "provider" ? " · F5 刷新" : "";
 		return `Enter assign roles · ${arrows} · type to search · Alt+←/→ kind${refresh} · Esc close`;
 	}
 
diff --git a/pi-tui/src/overlays/move-overlay.ts b/pi-tui-zh/src/overlays/move-overlay.ts
index 53b492c..f5b623e 100644
--- a/pi-tui/src/overlays/move-overlay.ts
+++ b/pi-tui-zh/src/overlays/move-overlay.ts
@@ -161,7 +161,7 @@ export class MoveOverlay implements Component, Focusable {
 		}
 
 		lines.push(row("", w));
-		lines.push(row(theme.fg("dim", "Type to filter · ↑↓ navigate · Tab accept · Enter confirm · Esc cancel"), w));
+		lines.push(row(theme.fg("dim", "输入过滤 · ↑↓ 导航 · Tab 接受 · Enter 确认 · Esc 取消"), w));
 		lines.push(bottomBorder(w));
 		this.#renderMemo = { width: w, fieldLines, revision: this.#revision, lines };
 		return lines;
diff --git a/pi-tui/src/overlays/oauth-selector.ts b/pi-tui-zh/src/overlays/oauth-selector.ts
index 565d43f..bac009b 100644
--- a/pi-tui/src/overlays/oauth-selector.ts
+++ b/pi-tui-zh/src/overlays/oauth-selector.ts
@@ -247,7 +247,7 @@ export class OAuthSelectorComponent extends OverlayPanel {
 
 	#renderStatusLine(_total: number): string {
 		const query = this.#menu.query.trim();
-		const suffix = query ? `Search: ${this.#menu.query}` : "Type to search";
+		const suffix = query ? `搜索: ${this.#menu.query}` : "输入搜索";
 		return theme.fg("muted", suffix);
 	}
 
diff --git a/pi-tui/src/overlays/omfg-panel.ts b/pi-tui-zh/src/overlays/omfg-panel.ts
index f09b47b..197690e 100644
--- a/pi-tui/src/overlays/omfg-panel.ts
+++ b/pi-tui-zh/src/overlays/omfg-panel.ts
@@ -64,7 +64,7 @@ export class OmfgPanelComponent extends OverlayPanel {
 		if (this.#closed) return;
 		this.#state = "saved";
 		this.#savedPath = path;
-		this.#status = `Saved ${path}`;
+		this.#status = `已保存 ${path}`;
 		this.#errorMessage = undefined;
 		this.#rebuild();
 	}
@@ -72,7 +72,7 @@ export class OmfgPanelComponent extends OverlayPanel {
 	markRejected(): void {
 		if (this.#closed) return;
 		this.#state = "rejected";
-		this.#status = "Rule was not saved.";
+		this.#status = "规则未保存。";
 		this.#errorMessage = undefined;
 		this.#rebuild();
 	}
@@ -80,7 +80,7 @@ export class OmfgPanelComponent extends OverlayPanel {
 	markAborted(): void {
 		if (this.#closed) return;
 		this.#state = "aborted";
-		this.#status = "Cancelled.";
+		this.#status = "已取消。";
 		this.#errorMessage = undefined;
 		this.#rebuild();
 	}
@@ -88,7 +88,7 @@ export class OmfgPanelComponent extends OverlayPanel {
 	markError(message: string): void {
 		if (this.#closed) return;
 		this.#state = "error";
-		this.#status = "Could not create rule.";
+		this.#status = "无法创建规则。";
 		this.#errorMessage = message;
 		this.#rebuild();
 	}
@@ -108,18 +108,18 @@ export class OmfgPanelComponent extends OverlayPanel {
 			case "validating":
 			case "confirming":
 			case "saving":
-				return theme.fg("muted", "Esc cancel /omfg");
+				return theme.fg("muted", "Esc 取消 /omfg");
 			case "saved":
 				return theme.fg(
 					"success",
-					`${theme.status.success} Registered live · ${replaceTabs(this.#savedPath ?? "saved")} · Esc dismiss`,
+					`${theme.status.success} 已实时注册 · ${replaceTabs(this.#savedPath ?? "已保存")} · Esc 关闭`,
 				);
 			case "rejected":
-				return theme.fg("warning", `${theme.status.warning} Not saved · Esc dismiss`);
+				return theme.fg("warning", `${theme.status.warning} 未保存 · Esc 关闭`);
 			case "aborted":
-				return theme.fg("warning", `${theme.status.warning} Cancelled · Esc dismiss`);
+				return theme.fg("warning", `${theme.status.warning} 已取消 · Esc 关闭`);
 			case "error":
-				return theme.fg("error", `${theme.status.error} Error · Esc dismiss`);
+				return theme.fg("error", `${theme.status.error} 出错 · Esc 关闭`);
 		}
 	}
 
@@ -129,7 +129,7 @@ export class OmfgPanelComponent extends OverlayPanel {
 		}
 		const text = replaceTabs(this.#preview).trim();
 		if (!text) {
-			return new Text(theme.fg("dim", `${theme.status.pending} Waiting for candidate rule…`), 0, 0);
+			return new Text(theme.fg("dim", `${theme.status.pending} 等待候选规则…`), 0, 0);
 		}
 		return new Markdown(text, 0, 0, getMarkdownTheme());
 	}
diff --git a/pi-tui/src/overlays/plan-review-overlay.ts b/pi-tui-zh/src/overlays/plan-review-overlay.ts
index e0073af..87216ce 100644
--- a/pi-tui/src/overlays/plan-review-overlay.ts
+++ b/pi-tui-zh/src/overlays/plan-review-overlay.ts
@@ -54,7 +54,7 @@ import { padToWidth } from "../render/utils";
 import { renderSegmentTrack } from "../chrome/segment-track";
 
 /** Title shown in the overlay's top border. */
-const OVERLAY_TITLE = "Plan Review";
+const OVERLAY_TITLE = "计划审查";
 /** Minimum plan-body rows kept visible even on short terminals. */
 const MIN_BODY_ROWS = 3;
 /** Sidebar gates: enough headings, a wide terminal, and a usable body column. */
@@ -155,7 +155,7 @@ export interface PlanReviewOverlayOptions {
 }
 
 /** Default trailing footer hint when the caller supplies none. */
-const DEFAULT_HELP_SUFFIX = "esc cancel";
+const DEFAULT_HELP_SUFFIX = "esc 取消";
 
 export class PlanReviewOverlay implements Component {
 	#mdTheme: MarkdownTheme;
@@ -870,15 +870,15 @@ export class PlanReviewOverlay implements Component {
 			this.callbacks.onFeedbackChange?.("");
 			return;
 		}
-		let feedback = "Refinement feedback on the plan:\n";
+		let feedback = "对计划的完善反馈:\n";
 		if (this.#deleted.length > 0) {
-			feedback += "\nRemove these sections:\n";
+			feedback += "\n删除这些章节:\n";
 			for (const title of this.#deleted) feedback += `- ${title}\n`;
 		}
 		for (const section of annotated) {
-			feedback += `\n## ${section.title || "Plan preamble"}\n`;
+			feedback += `\n## ${section.title || "计划前言"}\n`;
 			for (const annotation of section.annotations) {
-				if (annotation.target.kind === "line") feedback += `> Line: ${annotation.target.context}\n`;
+				if (annotation.target.kind === "line") feedback += `> 行: ${annotation.target.context}\n`;
 				feedback += this.#formatAnnotationFeedback(annotation.note);
 			}
 		}
@@ -942,14 +942,14 @@ export class PlanReviewOverlay implements Component {
 				if (this.#slider) parts.push("◂▸ model");
 				break;
 			case "toc":
-				parts.push("↑↓ section", "⏎ open", "a annotate", "d delete", "u undo");
+				parts.push("↑↓ 章节", "⏎ 打开", "a 批注", "d 删除", "u 撤销");
 				break;
 			case "body":
-				parts.push("↑↓ scroll", "⇧ faster", "pgup/pgdn", "g/G ends", "a annotate");
+				parts.push("↑↓ 滚动", "⇧ 加速", "pgup/pgdn", "g/G 两端", "a 批注");
 				break;
 		}
-		if (this.callbacks.onCopyPlan) parts.push("c copy");
-		parts.push("tab regions");
+		if (this.callbacks.onCopyPlan) parts.push("c 复制");
+		parts.push("tab 区域");
 		if (this.#externalEditorLabel && this.#focus !== "toc") parts.push(`${this.#externalEditorLabel} editor`);
 		parts.push(this.#helpSuffix);
 		return parts.join(sep);
@@ -1147,11 +1147,11 @@ export class PlanReviewOverlay implements Component {
 					? `‹${title}›`
 					: `‹${title}› · ${truncateToWidth(target?.context ?? "", Math.max(1, innerWidth - 16), Ellipsis.Unicode)}`;
 			const caption = truncateToWidth(
-				`${theme.fg("dim", "Annotate")} ${theme.fg("accent", location)}`,
+				`${theme.fg("dim", "批注")} ${theme.fg("accent", location)}`,
 				innerWidth,
 				Ellipsis.Unicode,
 			);
-			const hintParts = ["enter save", "esc cancel"];
+			const hintParts = ["enter 保存", "esc 取消"];
 			if (this.#externalEditorLabel) hintParts.push(`${this.#externalEditorLabel} editor`);
 			return [caption, this.#input.render(innerWidth)[0] ?? "", theme.fg("dim", hintParts.join(" · "))];
 		}
@@ -1168,7 +1168,7 @@ export class PlanReviewOverlay implements Component {
 
 		const committed = this.#committed;
 		const sliderLines = committed ? [] : this.#renderSliderLines();
-		const submittingLabel = this.#committedLabel ? `${this.#committedLabel} — submitting…` : "Submitting…";
+		const submittingLabel = this.#committedLabel ? `${this.#committedLabel} — 正在提交…` : "正在提交…";
 		const optionLines = committed ? [theme.bold(theme.fg("accent", submittingLabel))] : this.#renderOptionLines();
 		const promptLines = this.#promptTitle ? [theme.bold(theme.fg("accent", this.#promptTitle))] : [];
 		const footerLines = committed
diff --git a/pi-tui/src/overlays/settings-defs.ts b/pi-tui-zh/src/overlays/settings-defs.ts
index df11774..6771877 100644
--- a/pi-tui/src/overlays/settings-defs.ts
+++ b/pi-tui-zh/src/overlays/settings-defs.ts
@@ -32,16 +32,16 @@ export const SETTING_TABS: SettingTab[] = [
 
 /** Tab display metadata - icon is a symbol key from theme.ts (tab.*) */
 export const TAB_METADATA: Record<SettingTab, TabMetadata> = {
-	appearance: { label: "Appearance", icon: "tab.appearance" },
-	model: { label: "Model", icon: "tab.model" },
-	interaction: { label: "Interaction", icon: "tab.interaction" },
-	context: { label: "Context", icon: "tab.context" },
-	memory: { label: "Memory", icon: "tab.memory" },
-	files: { label: "Files", icon: "tab.files" },
+	appearance: { label: "外观", icon: "tab.appearance" },
+	model: { label: "模型", icon: "tab.model" },
+	interaction: { label: "交互", icon: "tab.interaction" },
+	context: { label: "上下文", icon: "tab.context" },
+	memory: { label: "记忆", icon: "tab.memory" },
+	files: { label: "文件", icon: "tab.files" },
 	shell: { label: "Shell", icon: "tab.shell" },
-	tools: { label: "Tools", icon: "tab.tools" },
-	tasks: { label: "Tasks", icon: "tab.tasks" },
-	providers: { label: "Providers", icon: "tab.providers" },
+	tools: { label: "工具", icon: "tab.tools" },
+	tasks: { label: "任务", icon: "tab.tasks" },
+	providers: { label: "提供商", icon: "tab.providers" },
 };
 
 /**
diff --git a/pi-tui/src/overlays/settings-selector.ts b/pi-tui-zh/src/overlays/settings-selector.ts
index 62b035c..d036147 100644
--- a/pi-tui/src/overlays/settings-selector.ts
+++ b/pi-tui-zh/src/overlays/settings-selector.ts
@@ -366,7 +366,7 @@ class ProviderLimitsSubmenu extends Container {
 				theme: formTheme,
 				label: `Max In-Flight Requests: ${provider}`,
 				description:
-					"Enter a positive number. Decimals round down. Clear the field to make this provider unlimited.",
+					"输入正数。小数向下取整。清空字段使该提供商无限制。",
 				initialValue: limits[provider]?.toString() ?? undefined,
 				empty: "submit",
 				hint: "  Enter to save · Esc to cancel · Clear field to unset",
@@ -432,7 +432,7 @@ function getSettingsTabs(): Tab[] {
 			const icon = theme.symbol(meta.icon);
 			return { id, label: `${icon} ${meta.label}`, short: icon };
 		}),
-		{ id: "plugins", label: `${theme.icon.package} Plugins`, short: theme.icon.package },
+		{ id: "plugins", label: `${theme.icon.package} 插件`, short: theme.icon.package },
 	];
 }
 
@@ -568,10 +568,10 @@ export class SettingsSelectorComponent implements Component {
 
 	#footerHintText(): string {
 		if (this.#searchList) {
-			return "Enter to change · Tab to jump tabs · Esc to exit search";
+			return "Enter 修改 · Tab 切换标签 · Esc 退出搜索";
 		}
 		if (this.#currentTabId === "plugins") {
-			return "Tab to switch tabs · Esc to close";
+			return "Tab 切换标签 · Esc 关闭";
 		}
 		if (this.#currentList?.sectionFocused) {
 			return "↑/↓ to jump sections · Tab/Enter to settings · ←/→ to switch tabs · Esc to close";
@@ -847,7 +847,7 @@ export class SettingsSelectorComponent implements Component {
 		// Plugins hosts its own UI; it is not part of the schema-backed search.
 		empty.push({
 			id: "plugins",
-			label: `${theme.icon.package} Plugins`,
+			label: `${theme.icon.package} 插件`,
 			short: theme.icon.package,
 			muted: true,
 		});
diff --git a/pi-tui/src/overlays/show-images-selector.ts b/pi-tui-zh/src/overlays/show-images-selector.ts
index a7b2629..adfcdc7 100644
--- a/pi-tui/src/overlays/show-images-selector.ts
+++ b/pi-tui-zh/src/overlays/show-images-selector.ts
@@ -13,8 +13,8 @@ export class ShowImagesSelectorComponent extends OverlayPanel {
 		super("Show Images");
 
 		const items: SelectItem[] = [
-			{ value: "yes", label: "Yes", description: "Show images inline in terminal" },
-			{ value: "no", label: "No", description: "Show text placeholder instead" },
+			{ value: "yes", label: "是", description: "在终端内联显示图片" },
+			{ value: "no", label: "否", description: "改为显示文本占位符" },
 		];
 
 		// Create selector
diff --git a/pi-tui/src/overlays/snapcompact-shape-preview.ts b/pi-tui-zh/src/overlays/snapcompact-shape-preview.ts
index d07bf6f..c49aae2 100644
--- a/pi-tui/src/overlays/snapcompact-shape-preview.ts
+++ b/pi-tui-zh/src/overlays/snapcompact-shape-preview.ts
@@ -117,7 +117,7 @@ export class SnapcompactShapePreview implements Component {
 		// Only the unicode-placeholder path returns text-cell `lines`; cursor-moving
 		// placements would corrupt the bordered settings frame, so skip them.
 		if (!result?.lines) {
-			lines.push(theme.fg("dim", "  (graphic sample needs Kitty unicode-placeholder graphics)"));
+			lines.push(theme.fg("dim", "  (图形示例需要 Kitty unicode-placeholder 图形)"));
 			return lines;
 		}
 		if (result.transmit) {
diff --git a/pi-tui/src/overlays/tiny-title-download-progress.ts b/pi-tui-zh/src/overlays/tiny-title-download-progress.ts
index 4638422..698f782 100644
--- a/pi-tui/src/overlays/tiny-title-download-progress.ts
+++ b/pi-tui-zh/src/overlays/tiny-title-download-progress.ts
@@ -37,13 +37,13 @@ function currentFile(event: TinyTitleDownloadProgress | undefined): string | und
 }
 
 function statusLabel(event: TinyTitleDownloadProgress | undefined): string {
-	if (!event) return "Preparing";
-	if (event.status === "error") return "Failed";
-	if (event.status === "ready") return "Ready";
-	if (event.status === "done") return "Downloaded";
-	if (event.status === "download") return "Downloading";
-	if (event.status === "progress" || event.status === "progress_total") return "Downloading";
-	return "Preparing";
+	if (!event) return "准备中";
+	if (event.status === "error") return "失败";
+	if (event.status === "ready") return "就绪";
+	if (event.status === "done") return "已下载";
+	if (event.status === "download") return "下载中";
+	if (event.status === "progress" || event.status === "progress_total") return "下载中";
+	return "准备中";
 }
 
 function byteLabel(event: TinyTitleDownloadProgress | undefined): string | undefined {
@@ -93,7 +93,7 @@ export class TinyTitleDownloadProgressComponent implements Component {
 		const pct =
 			this.#event?.progress === undefined ? "" : `${Math.floor(this.#event.progress).toString().padStart(3, " ")}%`;
 		const bytes = byteLabel(this.#event);
-		const title = `${theme.fg("accent", "Tiny model")} ${theme.fg("muted", status)} ${this.#modelLabel}`;
+		const title = `${theme.fg("accent", "小模型")} ${theme.fg("muted", status)} ${this.#modelLabel}`;
 		const bar = this.#bar.render(Math.max(8, width - 36))[0] ?? "";
 		const details = [bar, pct, bytes, file].filter((part): part is string => Boolean(part)).join(" ");
 
diff --git a/pi-tui/src/prompt/tips.txt b/pi-tui-zh/src/prompt/tips.txt
index 93c7089..e33a954 100644
--- a/pi-tui/src/prompt/tips.txt
+++ b/pi-tui-zh/src/prompt/tips.txt
@@ -1,28 +1,26 @@
-Tired of typing "keep going"? Just send a '.'
-You can /btw to ask a side question
-Use /tan to fork the current conversation into a background agent
-With default keybindings, Ctrl+D deletes the character under the cursor instead of quitting your draft — see /hotkeys for your exit key
-Find out which model you emotionally abuse the most with `omp stats`
-Try task isolation to create CoW worktrees
-Need a cheap nested model call? Use `completion(x...)`. Have a big batch of tasks? Ask clanker to use it!
-Spaghetti code? Try complaining with /omfg
-Did you know? Each kitty/tmux/cmux/zellij/wezterm split keeps its own session — `omp -c` resumes the right one
-Drop the word `ultrathink` in your message for harder multi-step reasoning — watch it glow rainbow as you type
-Say `orchestrate` in your message to drive a multi-phase task with parallel subagents — watch it glow as you type
-Say `workflowz` in your message to drive the task with parallel subagents in eval — watch it glow as you type
-Bulk data to classify or triage? Say `jevify` in your message — the eval kernel's `judge()` processes the batch and you read only what it flags — watch it glow as you type
-Log in to several accounts of the same provider — `/login` again — and omp load-balances across them automatically
-Run `omp auth-broker serve` once and every machine pulls live tokens over the wire — refresh keys never leave the host; `omp auth-gateway` fronts it as a drop-in proxy any OpenAI-compatible client can hit
-Press alt+p (or /switch) to switch provider, and ctrl+p to cycle role models smol -> slow -> etc
-Press ctrl+r to search your prompt history and reuse a past message
-`/force read` pins the next turn to one specific tool when the model keeps reaching for the wrong one
-`/copy code` grabs the last code block to your clipboard — `/copy cmd` grabs the last shell/python command
-`/shake` rips heavy tool results out of context to reclaim tokens without a full /compact — `/shake images` drops just images
-Pair up live: `/collab` shares your session through an end-to-end encrypted relay link — a teammate runs `/join <link>` to watch tool calls stream and prompt the agent from their own omp
-Press ← ← to drill into a running or finished agent and inspect its tool calls and transcript
-Hit a Codex rate limit? `/usage reset` spends a saved reset credit to immediately restore your quota
-No native tool_calling? Inference provider botches parsing them? `PI_DIALECT=glm|kimi|anthropic…` rolls it locally for them!
-Turn on `/advisor` to attach a second model that reviews every turn and quietly injects advice
-Try starting your prompt with a ->, and writing a list (1. Do X, 2. Do Y)
-Press shift+tab to cycle through reasoning effort levels
-Lint/type errors piling up? `omp cleanse` (or /cleanse right here) hunts project diagnostics and fixes them with parallel subagents — esc cancels
\ No newline at end of file
+懒得打 "keep going"?直接发一个 '.'
+可以用 /btw 问个题外话
+用 /tan 把当前对话分叉成后台代理
+Ctrl+D 可以退出,而且草稿会保留!
+用 `omp stats` 看看你平时最虐待哪个模型
+试试任务隔离,创建 CoW 工作树
+需要廉价的嵌套模型调用?用 `completion(x...)`。有一大批任务?让 clanker 用上它!
+代码一团乱麻?用 /omfg 抱怨试试
+你知道吗?kitty/tmux/cmux/zellij/wezterm 的每个分屏都有自己的会话——`omp -c` 会恢复正确的那一个
+在消息里写 `ultrathink` 可进行更困难的多步推理——输入时它会闪烁彩虹
+在消息里说 `orchestrate`,用并行子代理驱动多阶段任务——输入时会发光
+在消息里说 `workflowz`,在 eval 里用并行子代理驱动任务——输入时会发光
+同一提供商登录多个账号——再执行一次 `/login`——omp 会在它们之间自动负载均衡
+运行一次 `omp auth-broker serve`,每台机器都能在线拉取令牌——刷新密钥永不离机;`omp auth-gateway` 作为即插即用代理,任何 OpenAI 兼容客户端都能使用
+按 alt+p(或 /switch)切换提供商,按 ctrl+p 循环切换角色模型 smol -> slow -> ...
+按 ctrl+r 搜索提示词历史并复用旧消息
+模型总拿错工具时,`/force read` 可把下一轮锁定到指定工具
+`/copy code` 把最后一个代码块复制到剪贴板——`/copy cmd` 复制最后一条 shell/python 命令
+`/shake` 把沉重的工具结果从上下文里剔除以回收 token,无需完整 /compact——`/shake images` 只丢弃图片
+实时结对:`/collab` 通过端到端加密中继链接分享你的会话——队友运行 `/join <链接>` 即可观看工具调用流并从自己的 omp 向代理提问
+按 ← ← 深入查看运行中或已完成的代理,检查其工具调用与记录
+遇到 Codex 限流?`/usage reset` 消耗一次保存的重置额度,立即恢复配额
+模型不支持原生 tool_calling?提供商解析不好?`PI_DIALECT=glm|kimi|anthropic…` 可在本地代为处理!
+开启 `/advisor`,挂一个第二模型,每轮审查并悄悄注入建议
+试试用 -> 开头写提示,并写成列表(1. 做 X, 2. 做 Y)
+按 shift+tab 循环切换推理强度
diff --git a/pi-tui/src/prompt/welcome.ts b/pi-tui-zh/src/prompt/welcome.ts
index 11e63e8..dfc7e36 100644
--- a/pi-tui/src/prompt/welcome.ts
+++ b/pi-tui-zh/src/prompt/welcome.ts
@@ -29,7 +29,7 @@ export const WELCOME_LSP_SLOTS = 4;
 const NEW_TIP_MARKER = /\s*\[NEW\]\s*$/;
 
 /** Visible text rendered in place of {@link NEW_TIP_MARKER}. */
-const NEW_TAG_TEXT = "NEW!";
+const NEW_TAG_TEXT = "新!";
 
 /** Milliseconds for one full hue rotation of the rainbow "NEW!" tag. */
 const NEW_GLOW_PERIOD_MS = 1500;
@@ -77,7 +77,7 @@ function renderNewTag(phase: number, encoding: ColorEncoding): string {
 	return out + reset;
 }
 export function renderWelcomeTip(tip: string, boxWidth: number, phase = 0): string[] {
-	const label = "Tip: ";
+	const label = "小贴士: ";
 	const labelWidth = visibleWidth(label);
 	const bodyBudget = boxWidth - 1 - labelWidth; // 1 = leading indent
 	if (bodyBudget < 8) return [];
@@ -159,7 +159,7 @@ export class WelcomeComponent implements Component {
 		this.#nagRoll ??= Math.random();
 		this.#tipRoll ??= Math.random();
 		if (theme.getSymbolPreset() === "unicode" && this.#nagRoll < 0.1) {
-			return "Please use nerdfont 😭.";
+			return "请使用 nerdfont 字体 😭。";
 		}
 		return pickWeightedTip(TIPS, this.#tipRoll) || undefined;
 	}
@@ -271,7 +271,7 @@ export class WelcomeComponent implements Component {
 		// Dynamic model/provider labels are truncated inside the fixed column.
 		// Letting them influence the responsive breakpoint changes the box height
 		// when authoritative session data replaces the empty prepaint labels.
-		const leftMinContentWidth = Math.max(minLeftCol, visibleWidth("Welcome back!"));
+		const leftMinContentWidth = Math.max(minLeftCol, visibleWidth("欢迎回来!"));
 		const desiredLeftCol = Math.max(
 			Math.min(preferredLeftCol, Math.max(minLeftCol, Math.floor(dualContentWidth * 0.35))),
 			leftMinContentWidth,
@@ -291,7 +291,7 @@ export class WelcomeComponent implements Component {
 		// Left column - centered content
 		const leftLines = [
 			"",
-			this.#centerText(theme.bold("Welcome back!"), leftCol),
+			this.#centerText(theme.bold("欢迎回来!"), leftCol),
 			"",
 			...logoColored.map(l => this.#centerText(l, leftCol)),
 			"",
@@ -306,7 +306,7 @@ export class WelcomeComponent implements Component {
 		// Recent sessions content
 		const sessionLines: string[] = [];
 		if (this.recentSessions.length === 0) {
-			sessionLines.push(` ${theme.fg("dim", "No recent sessions")}`);
+			sessionLines.push(` ${theme.fg("dim", "暂无最近会话")}`);
 		} else {
 			// Reserve width for the bullet prefix (" • ") and the trailing " (timeAgo)"
 			// so the relative time is never the part that gets truncated. The name
@@ -332,7 +332,7 @@ export class WelcomeComponent implements Component {
 		// LSP servers content
 		const lspLines: string[] = [];
 		if (this.lspServers.length === 0) {
-			lspLines.push(` ${theme.fg("dim", "No LSP servers")}`);
+			lspLines.push(` ${theme.fg("dim", "暂无 LSP 服务器")}`);
 		} else {
 			for (const server of this.lspServers.slice(0, WELCOME_LSP_SLOTS)) {
 				const icon =
@@ -354,16 +354,16 @@ export class WelcomeComponent implements Component {
 
 		// Right column
 		const rightLines = [
-			` ${theme.bold(theme.fg("accent", "Tips"))}`,
-			` ${theme.fg("dim", "#")}${theme.fg("muted", " for prompt actions")}`,
-			` ${theme.fg("dim", "/")}${theme.fg("muted", " for commands")}`,
-			` ${theme.fg("dim", "!")}${theme.fg("muted", " to run bash")}`,
-			` ${theme.fg("dim", "$")}${theme.fg("muted", " to run python")}`,
+			` ${theme.bold(theme.fg("accent", "小贴士"))}`,
+			` ${theme.fg("dim", "#")}${theme.fg("muted", " 提示词操作")}`,
+			` ${theme.fg("dim", "/")}${theme.fg("muted", " 命令")}`,
+			` ${theme.fg("dim", "!")}${theme.fg("muted", " 运行 bash")}`,
+			` ${theme.fg("dim", "$")}${theme.fg("muted", " 运行 python")}`,
 			separator,
-			` ${theme.bold(theme.fg("accent", "LSP Servers"))}`,
+			` ${theme.bold(theme.fg("accent", "LSP 服务器"))}`,
 			...lspLines,
 			separator,
-			` ${theme.bold(theme.fg("accent", "Recent sessions"))}`,
+			` ${theme.bold(theme.fg("accent", "最近会话"))}`,
 			...sessionLines,
 			"",
 		];
diff --git a/pi-tui/src/render/code-cell.ts b/pi-tui-zh/src/render/code-cell.ts
index ff8ced6..8e47adb 100644
--- a/pi-tui/src/render/code-cell.ts
+++ b/pi-tui-zh/src/render/code-cell.ts
@@ -71,7 +71,7 @@ function formatHeader(options: CodeCellOptions, theme: Theme): { title: string;
 			spinnerFrame,
 		);
 		if (status === "pending" || status === "running") {
-			parts.push(`${icon} ${theme.fg("muted", status)}`);
+			parts.push(`${icon} ${theme.fg("muted", status === "pending" ? "待处理" : "运行中")}`);
 		} else {
 			parts.push(icon);
 		}
@@ -82,7 +82,7 @@ function formatHeader(options: CodeCellOptions, theme: Theme): { title: string;
 	if (title) {
 		parts.push(theme.fg("toolTitle", title));
 	}
-	const headerTitle = parts.length > 0 ? parts.join(" ") : theme.fg("toolTitle", "Code");
+	const headerTitle = parts.length > 0 ? parts.join(" ") : theme.fg("toolTitle", "代码");
 
 	const metaParts: string[] = [];
 	if (duration !== undefined) {
@@ -170,7 +170,7 @@ export function renderCodeCell(options: CodeCellOptions, theme: Theme): string[]
 		if (tail) {
 			// Earlier rows scrolled above the live tail window — mark them on top so
 			// the newest streamed line stays pinned to the bottom of the box.
-			const earlier = `… ${hiddenCodeLines} earlier line${hiddenCodeLines === 1 ? "" : "s"}${hint ? ` ${hint}` : ""}`;
+			const earlier = `… 之前还有 ${hiddenCodeLines} 行${hint ? ` ${hint}` : ""}`;
 			codeLines.unshift(theme.fg("dim", gutterPad + earlier));
 		} else {
 			const moreLine = `${formatMoreItems(hiddenCodeLines, "line")}${hint ? ` ${hint}` : ""}`;
@@ -182,7 +182,7 @@ export function renderCodeCell(options: CodeCellOptions, theme: Theme): string[]
 
 	const sections: Array<{ label?: string; lines: readonly string[] }> = [{ lines: codeLines }];
 	if (outputLines.length > 0) {
-		sections.push({ label: theme.fg("toolTitle", "Output"), lines: outputLines });
+		sections.push({ label: theme.fg("toolTitle", "输出"), lines: outputLines });
 	}
 
 	return renderOutputBlock({ header: title, headerMeta: meta, state, sections, width }, theme);
@@ -237,7 +237,7 @@ export function renderMarkdownCell(options: MarkdownCellOptions, theme: Theme):
 
 	const sections: Array<{ label?: string; lines: readonly string[] }> = [{ lines: contentLines }];
 	if (outputLines.length > 0) {
-		sections.push({ label: theme.fg("toolTitle", "Output"), lines: outputLines });
+		sections.push({ label: theme.fg("toolTitle", "输出"), lines: outputLines });
 	}
 
 	return renderOutputBlock({ header: title, headerMeta: meta, state, sections, width }, theme);
diff --git a/pi-tui/src/render/render-utils.ts b/pi-tui-zh/src/render/render-utils.ts
index 932df63..a230649 100644
--- a/pi-tui/src/render/render-utils.ts
+++ b/pi-tui-zh/src/render/render-utils.ts
@@ -305,7 +305,7 @@ export function formatStatusIcon(status: ToolUIStatus, theme: Theme, spinnerFram
 export function formatExpandHint(theme: Theme, expanded?: boolean, hasMore?: boolean): string {
 	if (expanded) return "";
 	if (hasMore === false) return "";
-	return theme.fg("dim", wrapBrackets(`${expandKeyHint()}: Expand`, theme));
+	return theme.fg("dim", wrapBrackets(`${expandKeyHint()}: 展开`, theme));
 }
 
 /**
@@ -323,7 +323,7 @@ export function formatBadge(label: string, color: ToolUIColor, theme: Theme): st
  */
 export function formatMoreItems(remaining: number, itemType: string): string {
 	const safeRemaining = Number.isFinite(remaining) ? remaining : 0;
-	return `… ${safeRemaining} more ${pluralize(itemType, safeRemaining)}`;
+	return `… 还有 ${safeRemaining} ${COUNT_UNIT_ZH[itemType] ?? itemType}`;
 }
 
 /**
@@ -368,7 +368,7 @@ export function capPreviewLines(
 	const visible = max <= 1 ? [] : lines.slice(lines.length - (max - 1));
 	const hidden = lines.length - visible.length;
 	const hint = options.expandHint === false ? "" : formatExpandHint(theme, false, true);
-	const marker = `… ${hidden} earlier ${pluralize("line", hidden)}${hint ? ` ${hint}` : ""}`;
+	const marker = `… 之前还有 ${hidden} 行${hint ? ` ${hint}` : ""}`;
 	return [`${options.prefix ?? ""}${theme.fg("dim", marker)}`, ...visible];
 }
 
@@ -379,7 +379,7 @@ export function formatMeta(meta: string[], theme: Theme): string {
 
 function sanitizeErrorText(message: string | undefined): string {
 	const clean = (message ?? "").replace(/^Error:\s*/, "").trim();
-	return clean ? replaceTabs(truncateToWidth(clean, TRUNCATE_LENGTHS.LINE)) : "Unknown error";
+	return clean ? replaceTabs(truncateToWidth(clean, TRUNCATE_LENGTHS.LINE)) : "未知错误";
 }
 
 /**
@@ -399,7 +399,7 @@ export function sanitizeDisplayLines(text: string): string[] {
 
 /** Render a sanitized error message with its status icon. */
 export function formatErrorMessage(message: string | undefined, theme: Theme): string {
-	return `${theme.styledSymbol("status.error", "error")} ${theme.fg("error", `Error: ${sanitizeErrorText(message)}`)}`;
+	return `${theme.styledSymbol("status.error", "error")} ${theme.fg("error", `错误: ${sanitizeErrorText(message)}`)}`;
 }
 
 /**
@@ -545,7 +545,7 @@ export function formatDiagnostics(
 		: theme.styledSymbol("status.warning", "warning");
 	const summary = sanitizeDiagnosticDisplayText(diag.summary);
 	const summaryTag = summary ? ` ${theme.fg("dim", `(${summary})`)}` : "";
-	let output = `\n\n${headerIcon} ${theme.fg("toolTitle", options?.title ?? "Diagnostics")}${summaryTag}`;
+	let output = `\n\n${headerIcon} ${theme.fg("toolTitle", options?.title ?? "诊断")}${summaryTag}`;
 
 	const maxDiags = expanded ? diag.messages.length : 5;
 	let diagsShown = 0;
@@ -629,7 +629,7 @@ export function formatDiagnostics(
 		const remaining = totalDiags - diagsShown;
 		output += `\n ${theme.fg("dim", theme.tree.last)} ${theme.fg(
 			"muted",
-			`… ${remaining} more`,
+			`… 还有 ${remaining} 个`,
 		)} ${formatExpandHint(theme)}`;
 	}
 
@@ -680,7 +680,7 @@ export function formatDiffStats(added: number, removed: number, hunks: number, t
 	const parts: string[] = [];
 	if (added > 0) parts.push(theme.fg("toolDiffAdded", `+${added}`));
 	if (removed > 0) parts.push(theme.fg("toolDiffRemoved", `-${removed}`));
-	if (hunks > 0) parts.push(theme.fg("dim", `${hunks} hunk${hunks !== 1 ? "s" : ""}`));
+	if (hunks > 0) parts.push(theme.fg("dim", `${hunks} 个 hunk`));
 	return parts.join(theme.fg("dim", " / "));
 }
 
@@ -960,7 +960,7 @@ export function sanitizeDisplayWarnings(warnings: readonly string[]): string[] {
 		.slice(0, PREVIEW_LIMITS.COLLAPSED_ITEMS)
 		.map(warning => truncateToWidth(sanitizeDisplayWarning(warning), TRUNCATE_LENGTHS.LONG));
 	const hidden = warnings.length - visible.length;
-	if (hidden > 0) visible.push(`… ${hidden} more ${pluralize("warning", hidden)}`);
+	if (hidden > 0) visible.push(`… 还有 ${hidden} 条警告`);
 	return visible;
 }
 
@@ -1006,7 +1006,7 @@ export function formatParseErrors(errors: string[], total?: number): string[] {
 	if (deduped.length === 0) return [];
 	const fullCount = total ?? deduped.length;
 	const capped = deduped.slice(0, PARSE_ERRORS_LIMIT);
-	const header = fullCount > capped.length ? `Parse issues (${capped.length} / ${fullCount}):` : "Parse issues:";
+	const header = fullCount > capped.length ? `解析问题 (${capped.length} / ${fullCount}):` : "解析问题:";
 	return [header, ...capped.map(err => `- ${err}`)];
 }
 
@@ -1134,7 +1134,7 @@ export function appendParseErrorsBulletList(
 		lines.push(theme.fg("warning", `  - ${err}`));
 	}
 	if (fullCount > capped.length) {
-		lines.push(theme.fg("dim", `  … ${fullCount - capped.length} more`));
+		lines.push(theme.fg("dim", `  … 还有 ${fullCount - capped.length} 个`));
 	}
 }
 
@@ -1145,8 +1145,8 @@ export function appendParseErrorsBulletList(
 export function formatParseErrorsCountLabel(parseErrors: readonly string[], total?: number): string {
 	const fullCount = total ?? parseErrors.length;
 	return fullCount > PARSE_ERRORS_LIMIT
-		? `${PARSE_ERRORS_LIMIT} / ${fullCount} parse issues`
-		: `${fullCount} parse issue${fullCount !== 1 ? "s" : ""}`;
+		? `${PARSE_ERRORS_LIMIT} / ${fullCount} 个解析问题`
+		: `${fullCount} 个解析问题`;
 }
 
 /**
diff --git a/pi-tui/src/setup/lazy.ts b/pi-tui-zh/src/setup/lazy.ts
index fe9baed..61f0d6e 100644
--- a/pi-tui/src/setup/lazy.ts
+++ b/pi-tui-zh/src/setup/lazy.ts
@@ -7,7 +7,7 @@ export async function runProviderSetupWizard(ctx: SetupHost): Promise<void> {
 	const { ALL_SCENES, runSetupWizard } = await import("./wizard");
 	const providersScene = ALL_SCENES.find(scene => scene.id === "providers");
 	if (!providersScene) {
-		ctx.showError("Provider setup is unavailable.");
+		ctx.showError("提供商设置不可用。");
 		return;
 	}
 	await runSetupWizard(ctx, [providersScene], {
diff --git a/pi-tui/src/setup/scenes/glyph.ts b/pi-tui-zh/src/setup/scenes/glyph.ts
index e83176a..4c6c7ac 100644
--- a/pi-tui/src/setup/scenes/glyph.ts
+++ b/pi-tui-zh/src/setup/scenes/glyph.ts
@@ -28,8 +28,8 @@ const GLYPH_ITEMS: readonly SelectItem[] = GLYPH_PRESETS.map((preset, index) =>
 }));
 
 class GlyphSceneController implements SetupSceneController {
-	title = "Choose glyph mode";
-	subtitle = "Pick the row that renders cleanly in your terminal.";
+	title = "选择字形模式";
+	subtitle = "挑选在终端里显示最清晰的一行。";
 	#selectList: SelectList;
 	#previewRequest = 0;
 	#committing = false;
@@ -80,7 +80,7 @@ class GlyphSceneController implements SetupSceneController {
 		if (!this.#step) {
 			this.#step = new WizardStep({
 				kind: "choice",
-				intro: new Text(theme.fg("muted", "If a row shows boxes, tofu, or misaligned icons, pick another."), 0, 0),
+				intro: new Text(theme.fg("muted", "如果某一行显示为方框、豆腐块或图标错位,请换一行。"), 0, 0),
 				content: this.#selectList,
 				minContentLines: GLYPH_ITEMS.length,
 				fitContent: () => {
@@ -120,7 +120,7 @@ class GlyphSceneController implements SetupSceneController {
  */
 export const glyphSetupScene: SetupScene = {
 	id: "glyph-mode",
-	title: "Choose glyph mode",
+	title: "选择字形模式",
 	minVersion: 1,
 	shouldRun: () => !TERMINAL.glyphProtocol,
 	mount: host => new GlyphSceneController(host),
diff --git a/pi-tui/src/setup/scenes/model.ts b/pi-tui-zh/src/setup/scenes/model.ts
index d36fcb3..7e141c4 100644
--- a/pi-tui/src/setup/scenes/model.ts
+++ b/pi-tui-zh/src/setup/scenes/model.ts
@@ -10,8 +10,8 @@ import type { SetupScene, SetupSceneController, SetupSceneHost } from "./types";
 const MAX_VISIBLE_MODELS = 10;
 
 class ModelSceneController implements SetupSceneController {
-	title = "Choose your default model";
-	subtitle = "Search configured models and save the model used for new sessions.";
+	title = "选择默认模型";
+	subtitle = "搜索已配置的模型,并保存新会话使用的模型。";
 	#browser: ModelBrowser;
 	#status: string | undefined;
 	#selecting = false;
@@ -31,7 +31,7 @@ class ModelSceneController implements SetupSceneController {
 	}
 
 	async onMount(): Promise<void> {
-		this.#status = theme.fg("muted", "Discovering available models…");
+		this.#status = theme.fg("muted", "正在发现可用模型…");
 		this.#host.requestRender();
 		await this.#refreshModels();
 	}
@@ -58,7 +58,7 @@ class ModelSceneController implements SetupSceneController {
 
 	render(width: number, maxLines?: number): readonly string[] {
 		const intro = new Text(
-			this.#status ?? theme.fg("muted", "Type to search. Enter saves the highlighted model as your default."),
+			this.#status ?? theme.fg("muted", "输入搜索。Enter 将高亮模型保存为默认。"),
 			0,
 			0,
 		);
@@ -132,7 +132,7 @@ class ModelSceneController implements SetupSceneController {
 /** Setup step that assigns one available model to the persisted default role. */
 export const modelSetupScene: SetupScene = {
 	id: "model",
-	title: "Choose your default model",
+	title: "选择默认模型",
 	minVersion: 1,
 	mount: host => new ModelSceneController(host),
 };
diff --git a/pi-tui/src/setup/scenes/outro.ts b/pi-tui-zh/src/setup/scenes/outro.ts
index 796ffd6..8a99118 100644
--- a/pi-tui/src/setup/scenes/outro.ts
+++ b/pi-tui-zh/src/setup/scenes/outro.ts
@@ -11,8 +11,8 @@ export function renderSetupOutro(width: number, height: number, elapsedMs: numbe
 	const lines = renderStarfield(width, height, frame + 1000);
 	const progress = Math.max(0, Math.min(1, elapsedMs / SETUP_OUTRO_MS));
 	const logo = gradientLogo(PI_LOGO, progress * 1.2, { pos: (progress * 2) % 1, strength: 1 - progress });
-	const title = theme.bold(theme.fg("success", `${theme.status.success} Setup saved`));
-	const subtitle = theme.fg("muted", "Handing off to the normal CLI…");
+	const title = theme.bold(theme.fg("success", `${theme.status.success} 设置已保存`));
+	const subtitle = theme.fg("muted", "正在移交到常规 CLI…");
 	const sweepWidth = Math.max(1, Math.min(width - 8, Math.floor((width - 8) * progress)));
 	const sweep = `${theme.fg("accent", "━".repeat(sweepWidth))}${theme.fg("dim", "─".repeat(Math.max(0, width - 8 - sweepWidth)))}`;
 	const content = [...logo, "", title, subtitle, "", sweep];
diff --git a/pi-tui/src/setup/scenes/providers.ts b/pi-tui-zh/src/setup/scenes/providers.ts
index 01206ba..d370df9 100644
--- a/pi-tui/src/setup/scenes/providers.ts
+++ b/pi-tui-zh/src/setup/scenes/providers.ts
@@ -12,8 +12,8 @@ import { WebSearchTab } from "./web-search";
  * temporarily suppress tab switching.
  */
 class ProvidersSceneController implements SetupSceneController {
-	title = "Set up your providers";
-	subtitle = "Sign in and pick a web search provider. Press Esc when you're done.";
+	title = "配置你的提供商";
+	subtitle = "登录并选择网页搜索提供商。完成后按 Esc。";
 
 	#tabs: SetupTab[];
 	#tabBar: TabBar;
@@ -23,7 +23,7 @@ class ProvidersSceneController implements SetupSceneController {
 	constructor(host: SetupSceneHost) {
 		this.#tabs = [new SignInTab(host), new WebSearchTab(host)];
 		this.#tabBar = new TabBar(
-			"Providers",
+			"提供商",
 			this.#tabs.map(tab => ({ id: tab.id, label: tab.label })),
 			getTabBarTheme(),
 		);
@@ -101,7 +101,7 @@ class ProvidersSceneController implements SetupSceneController {
 /** Configure provider sign-in and the preferred web search backend. */
 export const providersSetupScene: SetupScene = {
 	id: "providers",
-	title: "Set up your providers",
+	title: "配置你的提供商",
 	minVersion: 1,
 	mount: host => new ProvidersSceneController(host),
 };
diff --git a/pi-tui/src/setup/scenes/sign-in.ts b/pi-tui-zh/src/setup/scenes/sign-in.ts
index ec520a7..51a4410 100644
--- a/pi-tui/src/setup/scenes/sign-in.ts
+++ b/pi-tui-zh/src/setup/scenes/sign-in.ts
@@ -73,7 +73,7 @@ interface PromptState {
  */
 export class SignInTab implements SetupTab {
 	readonly id = "sign-in";
-	readonly label = "Sign in";
+	readonly label = "登录";
 
 	#authStorage: AuthStorage;
 	#selector: OAuthSelectorComponent;
@@ -143,7 +143,7 @@ export class SignInTab implements SetupTab {
 		if (this.#loggingInProvider === undefined && (maxLines === undefined || maxLines >= 17 + 2)) {
 			intro = new Container();
 			intro.addChild(
-				new Text(theme.fg("muted", "Pick a provider to sign in — you can connect more than one."), 0, 0),
+				new Text(theme.fg("muted", "选择一个提供商登录——可以连接多个。"), 0, 0),
 			);
 			intro.addChild(new Spacer(1));
 		}
@@ -223,7 +223,7 @@ export class SignInTab implements SetupTab {
 		const useManualInput = PASTE_CODE_LOGIN_PROVIDERS.has(providerId);
 		this.#selector.stopValidation();
 		this.#loggingInProvider = providerId;
-		this.#statusLines = [theme.fg("dim", "Starting OAuth flow…")];
+		this.#statusLines = [theme.fg("dim", "正在启动 OAuth 流程…")];
 		this.#authUrl = undefined;
 		this.#authLaunchUrl = undefined;
 		this.#loginAbort = new AbortController();
@@ -250,7 +250,7 @@ export class SignInTab implements SetupTab {
 						this.#statusLines.push(theme.fg("warning", info.instructions));
 					}
 					if (useManualInput) {
-						this.#statusLines.push(theme.fg("dim", "Paste the returned code or redirect URL when prompted."));
+						this.#statusLines.push(theme.fg("dim", "提示时粘贴返回的代码或重定向 URL。"));
 					}
 					void this.#copyAuthUrl();
 					this.#host.ctx.openInBrowser(info.url);
@@ -262,7 +262,7 @@ export class SignInTab implements SetupTab {
 					this.#host.requestRender();
 				},
 				onManualCodeInput: signal =>
-					this.#showPrompt({ message: "Paste the authorization code (or full redirect URL):" }, signal),
+					this.#showPrompt({ message: "粘贴授权码(或完整重定向 URL):" }, signal),
 			});
 			// Provider-scoped online refresh so the just-persisted credential re-runs
 			// discovery instead of reusing a fresh authoritative cache row (#5780).
@@ -283,14 +283,14 @@ export class SignInTab implements SetupTab {
 		} catch (error) {
 			if (this.#disposed) return;
 			if (this.#loginAbort?.signal.aborted) {
-				this.#statusLines = [theme.fg("dim", "Login cancelled.")];
+				this.#statusLines = [theme.fg("dim", "登录已取消。")];
 				this.#authUrl = undefined;
 				this.#authLaunchUrl = undefined;
 			} else {
 				const message = error instanceof Error ? error.message : String(error);
 				this.#statusLines = [
-					theme.fg("error", `Login failed: ${message}`),
-					theme.fg("dim", "Choose another provider or press Esc to continue."),
+					theme.fg("error", `登录失败: ${message}`),
+					theme.fg("dim", "选择其他提供商或按 Esc 继续。"),
 				];
 				this.#authUrl = undefined;
 				this.#authLaunchUrl = undefined;
diff --git a/pi-tui/src/setup/scenes/theme.ts b/pi-tui-zh/src/setup/scenes/theme.ts
index f9e4b2e..4ec87fc 100644
--- a/pi-tui/src/setup/scenes/theme.ts
+++ b/pi-tui-zh/src/setup/scenes/theme.ts
@@ -22,12 +22,12 @@ import type { SetupScene, SetupSceneController, SetupSceneHost } from "./types";
 type ThemeMode = "curated" | "all";
 
 const CURATED_ITEMS: readonly SelectItem[] = [
-	{ value: "auto", label: "Match terminal", description: "Titanium in dark terminals, Light in light terminals" },
-	{ value: "theme:titanium", label: "Titanium", description: "Default dark theme" },
-	{ value: "theme:light", label: "Light", description: "Default light theme" },
-	{ value: "colorblind", label: "Colorblind colors", description: "Adjust red/green contrast" },
-	{ value: "ansi", label: "ANSI-safe", description: "ASCII glyphs with the dark terminal theme" },
-	{ value: "browse", label: "Browse all…", description: "Show every built-in and custom theme" },
+	{ value: "auto", label: "跟随终端", description: "深色终端用 Titanium,浅色终端用 Light" },
+	{ value: "theme:titanium", label: "Titanium", description: "默认深色主题" },
+	{ value: "theme:light", label: "Light", description: "默认浅色主题" },
+	{ value: "colorblind", label: "色盲配色", description: "调整红/绿对比度" },
+	{ value: "ansi", label: "ANSI 安全", description: "ASCII 字形 + 深色终端主题" },
+	{ value: "browse", label: "浏览全部…", description: "显示所有内置与自定义主题" },
 ];
 
 function fillStyledLine(content: string, width: number): string {
@@ -58,8 +58,8 @@ function renderMockEditor(width: number): string[] {
 	const horizontal = box.horizontal.repeat(innerWidth);
 	const top = theme.fg("borderAccent", `${box.topLeft}${horizontal}${box.topRight}`);
 	const bottom = theme.fg("borderMuted", `${box.bottomLeft}${horizontal}${box.bottomRight}`);
-	const prompt = `${theme.fg("accent", ">")} ${theme.fg("text", "Ask anything, edit files, run tools")}${theme.inverse(" ")}`;
-	const hint = theme.fg("dim", "enter send · shift+enter newline · / commands");
+	const prompt = `${theme.fg("accent", ">")} ${theme.fg("text", "问任何问题、编辑文件、运行工具")}${theme.inverse(" ")}`;
+	const hint = theme.fg("dim", "enter 发送 · shift+enter 换行 · / 命令");
 	return [
 		top,
 		`${theme.fg("borderAccent", box.vertical)}${innerWidth > 0 ? padToWidth(prompt, innerWidth) : ""}${theme.fg("borderAccent", box.vertical)}`,
@@ -71,19 +71,19 @@ function renderMockEditor(width: number): string[] {
 function renderThemePreview(width: number): string[] {
 	const previewWidth = Math.max(24, Math.min(width, 88));
 	return [
-		theme.bold("Preview"),
+		theme.bold("预览"),
 		`${theme.fg("success", `${theme.status.success} success`)}  ${theme.fg("warning", `${theme.status.warning} warning`)}  ${theme.fg("error", `${theme.status.error} error`)}  ${theme.fg("accent", "accent")}`,
 		"",
-		theme.fg("muted", "Status line"),
+		theme.fg("muted", "状态栏"),
 		renderMockStatusLine(previewWidth),
-		theme.fg("muted", "Editor"),
+		theme.fg("muted", "编辑器"),
 		...renderMockEditor(previewWidth),
 	];
 }
 
 class ThemeSceneController implements SetupSceneController {
-	title = "Pick a theme";
-	subtitle = "Move through the list to preview; Enter saves the highlighted choice.";
+	title = "选择主题";
+	subtitle = "在列表中移动可预览;Enter 保存高亮选项。";
 	#mode: ThemeMode = "curated";
 	#selectList: SelectList;
 	#loadingAllThemes = false;
@@ -135,13 +135,13 @@ class ThemeSceneController implements SetupSceneController {
 	render(width: number, maxLines?: number): readonly string[] {
 		const intro = new Container();
 		intro.addChild(
-			new Text(theme.fg("muted", "Theme changes preview live. Nothing is saved until you press Enter."), 0, 0),
+			new Text(theme.fg("muted", "主题更改实时预览。按 Enter 前不会保存任何内容。"), 0, 0),
 		);
 		intro.addChild(
 			new Text(
 				this.#mode === "all"
-					? theme.fg("dim", "Browsing all themes · Esc returns to curated choices")
-					: theme.fg("dim", "Esc skips this step"),
+					? theme.fg("dim", "正在浏览全部主题 · Esc 返回精选列表")
+					: theme.fg("dim", "Esc 跳过此步骤"),
 				0,
 				0,
 			),
@@ -154,7 +154,7 @@ class ThemeSceneController implements SetupSceneController {
 		for (const line of renderThemePreview(width)) {
 			preview.addChild(new Text(line, 0, 0));
 		}
-		const loading = this.#loadingAllThemes ? new Text(theme.fg("dim", "Loading themes…"), 0, 0) : undefined;
+		const loading = this.#loadingAllThemes ? new Text(theme.fg("dim", "正在加载主题…"), 0, 0) : undefined;
 		const status = this.#message ? new Text(this.#message, 0, 0) : undefined;
 		if (!this.#step) {
 			this.#step = new WizardStep({
@@ -307,7 +307,7 @@ class ThemeSceneController implements SetupSceneController {
 		}
 		if (request !== this.#previewRequest || this.#disposed) return;
 		if (!result.success) {
-			this.#message = theme.fg("error", result.error ?? "Theme preview failed");
+			this.#message = theme.fg("error", result.error ?? "主题预览失败");
 		}
 		this.#host.ctx.ui.invalidate();
 		this.#host.requestRender();
@@ -337,7 +337,7 @@ class ThemeSceneController implements SetupSceneController {
 /** Preview and persist the terminal color theme. */
 export const themeSetupScene: SetupScene = {
 	id: "theme",
-	title: "Pick a theme",
+	title: "选择主题",
 	minVersion: 1,
 	mount: host => new ThemeSceneController(host),
 };
diff --git a/pi-tui/src/setup/scenes/web-search.ts b/pi-tui-zh/src/setup/scenes/web-search.ts
index 397a9f1..4079000 100644
--- a/pi-tui/src/setup/scenes/web-search.ts
+++ b/pi-tui-zh/src/setup/scenes/web-search.ts
@@ -28,7 +28,7 @@ type Availability = "checking" | boolean;
  */
 export class WebSearchTab implements SetupTab {
 	readonly id = "web-search";
-	readonly label = "Web search";
+	readonly label = "网页搜索";
 	readonly modal = false;
 
 	#list: SelectList;
@@ -80,7 +80,7 @@ export class WebSearchTab implements SetupTab {
 	}
 
 	render(width: number, maxLines?: number): readonly string[] {
-		const intro = new Text(theme.fg("muted", "Choose the provider the web_search tool should prefer."), 0, 0);
+		const intro = new Text(theme.fg("muted", "选择 web_search 工具应优先使用的提供商。"), 0, 0);
 		const status = new Container();
 		const selected = this.#list.getSelectedItem();
 		if (selected) {
@@ -145,21 +145,21 @@ export class WebSearchTab implements SetupTab {
 		const label = WEB_SEARCH_ITEMS.find(item => item.value === value)?.label ?? value;
 		this.#status = [theme.fg("success", `${theme.status.success} Web search set to ${label}`)];
 		if (value !== "auto" && this.#availability.get(value as SearchProviderId) === false) {
-			this.#status.push(theme.fg("dim", "Not configured yet — add its API key or sign in to enable it."));
+			this.#status.push(theme.fg("dim", "尚未配置——添加其 API 密钥或登录以启用。"));
 		}
 		this.#host.requestRender();
 	}
 
 	#readinessLines(value: string): string[] {
 		if (value === "auto") {
-			return [theme.fg("dim", "Automatically uses the first configured provider.")];
+			return [theme.fg("dim", "自动使用第一个已配置的提供商。")];
 		}
 		const state = this.#availability.get(value as SearchProviderId);
 		if (state === undefined || state === "checking") {
-			return [theme.fg("dim", "Checking availability…")];
+			return [theme.fg("dim", "正在检查可用性…")];
 		}
 		return state
-			? [theme.fg("success", `${theme.status.success} Ready to use`)]
+			? [theme.fg("success", `${theme.status.success} 可用`)]
 			: [theme.fg("warning", `${theme.status.pending} Needs credentials`)];
 	}
 }
diff --git a/pi-tui/src/status-line/footer.ts b/pi-tui-zh/src/status-line/footer.ts
index 4344599..d30a7d5 100644
--- a/pi-tui/src/status-line/footer.ts
+++ b/pi-tui-zh/src/status-line/footer.ts
@@ -249,7 +249,7 @@ export class FooterComponent implements Component {
 		let statsLeft = statsParts.join(" ");
 
 		// Add model name on the right side, plus thinking level if model supports it
-		const modelName = state.model?.id || "no-model";
+		const modelName = state.model?.id || "无模型";
 
 		// Add thinking level hint when the current model advertises supported efforts
 		let rightSide = modelName;
@@ -258,7 +258,7 @@ export class FooterComponent implements Component {
 				// Pending (no turn classified yet / classifying) shows a symbol-theme
 				// question-box marker; once resolved it shows `<level>`.
 				const resolved = this.session.autoResolvedThinkingLevel();
-				rightSide = `${modelName} • ${resolved ? resolved : `${theme.thinking.autoPending} auto`}`;
+				rightSide = `${modelName} • ${resolved ? resolved : `${theme.thinking.autoPending} 自动`}`;
 			} else {
 				const thinkingLevel = state.thinkingLevel ?? ThinkingLevel.Off;
 				rightSide = `${modelName} • ${thinkingLevel}`;
diff --git a/pi-tui/src/tools/ask.ts b/pi-tui-zh/src/tools/ask.ts
index b4daa02..ecaccfe 100644
--- a/pi-tui/src/tools/ask.ts
+++ b/pi-tui-zh/src/tools/ask.ts
@@ -263,7 +263,7 @@ function renderAnswerOptionLines(
 
 	// Nothing was chosen (and no custom answer) → a lone cancelled marker.
 	if (selected.size === 0 && customInput === undefined && note === undefined) {
-		return [` ${uiTheme.styledSymbol("status.warning", "warning")} ${uiTheme.fg("warning", "Cancelled")}`];
+		return [` ${uiTheme.styledSymbol("status.warning", "warning")} ${uiTheme.fg("warning", "已取消")}`];
 	}
 
 	const out: string[] = [];
@@ -296,12 +296,12 @@ export const askToolRenderer = {
 		// throw here takes down the whole TUI render loop — normalize first.
 		const questions = normalizeRenderQuestions(args.questions);
 		if (questions && questions.length > 0) {
-			const header = `${label} ${uiTheme.fg("muted", `${questions.length} questions`)}`;
+			const header = `${label} ${uiTheme.fg("muted", `${questions.length} 个问题`)}`;
 			return framedToolCard(uiTheme, ({ width }) => {
 				const sections = questions.map(q => {
 					const meta: string[] = [];
-					if (q.multi) meta.push("multi");
-					if (q.options?.length) meta.push(`options:${q.options.length}`);
+					if (q.multi) meta.push("多选");
+					if (q.options?.length) meta.push(`选项:${q.options.length}`);
 					const metaStr = meta.length > 0 ? uiTheme.fg("dim", ` · ${meta.join(" · ")}`) : "";
 					// md() returns a shared cached array (module-level Markdown LRU) — copy before appending.
 					const mdLines = md(q.question, width);
@@ -316,7 +316,7 @@ export const askToolRenderer = {
 
 		// Single question
 		if (typeof args.question !== "string" || !args.question) {
-			const errorLine = formatErrorMessage("No question provided", uiTheme);
+			const errorLine = formatErrorMessage("未提供问题", uiTheme);
 			return framedToolCard(uiTheme, () => ({
 				header: errorLine,
 				sections: [],
@@ -327,9 +327,9 @@ export const askToolRenderer = {
 
 		const question = sanitizeCarriageReturns(args.question);
 		const meta: string[] = [];
-		if (args.multi) meta.push("multi");
+		if (args.multi) meta.push("多选");
 		const questionOptions = normalizeRenderOptions(args.options);
-		if (questionOptions?.length) meta.push(`options:${questionOptions.length}`);
+		if (questionOptions?.length) meta.push(`选项:${questionOptions.length}`);
 		const header = `${label}${formatMeta(meta, uiTheme)}`;
 		const multi = args.multi;
 		return framedToolCard(uiTheme, ({ width }) => {
@@ -369,7 +369,7 @@ export const askToolRenderer = {
 
 		// Chat redirect: user chose "Chat about this" instead of answering.
 		if (details.chatRedirect) {
-			const header = renderStatusLine({ icon: "info", title: "Ask", meta: ["chat redirect"] }, uiTheme);
+			const header = renderStatusLine({ icon: "info", title: "Ask", meta: ["聊天重定向"] }, uiTheme);
 			const questions = details.questions ?? [];
 			return framedToolCard(uiTheme, ({ width }) => ({
 				header,
@@ -392,7 +392,7 @@ export const askToolRenderer = {
 				{
 					icon: hasAnySelection ? "success" : "warning",
 					title: "Ask",
-					meta: [`${results.length} questions`],
+					meta: [`${results.length} 个问题`],
 				},
 				uiTheme,
 			);
@@ -469,7 +469,7 @@ export const askToolRenderer = {
 			];
 			if (dTimedOut) {
 				// Distinguish auto-selection from a real user choice in the transcript.
-				bodyLines.push(uiTheme.fg("dim", "auto-selected after timeout — not a user choice"));
+				bodyLines.push(uiTheme.fg("dim", "超时后自动选择 — 非用户选择"));
 			}
 			return {
 				header,
diff --git a/pi-tui/src/tools/ast-edit.ts b/pi-tui-zh/src/tools/ast-edit.ts
index 6ade927..7226de4 100644
--- a/pi-tui/src/tools/ast-edit.ts
+++ b/pi-tui-zh/src/tools/ast-edit.ts
@@ -87,13 +87,13 @@ export const astEditToolRenderer = {
 	inline: true,
 	renderCall(args: AstEditRenderArgs, _options: RenderResultOptions, uiTheme: Theme): Component {
 		const meta: string[] = [];
-		if (args.paths?.length) meta.push(`in ${args.paths.join(", ")}`);
+		if (args.paths?.length) meta.push(`于 ${args.paths.join(", ")}`);
 		const rewriteCount = args.ops?.length ?? 0;
-		if (rewriteCount > 1) meta.push(`${rewriteCount} rewrites`);
+		if (rewriteCount > 1) meta.push(`${rewriteCount} 处重写`);
 
 		const description =
-			rewriteCount === 1 ? patternPreview(args.ops?.[0]?.pat) : rewriteCount ? `${rewriteCount} rewrites` : "?";
-		const header = renderStatusLine({ icon: "pending", title: "AST Edit", description, meta }, uiTheme);
+			rewriteCount === 1 ? patternPreview(args.ops?.[0]?.pat) : rewriteCount ? `${rewriteCount} 处重写` : "?";
+		const header = renderStatusLine({ icon: "pending", title: "AST 编辑", description, meta }, uiTheme);
 		// Pending call has no body yet — a lone status line is sleeker than an empty frame.
 		return new Text(header, 0, 0);
 	},
@@ -107,8 +107,8 @@ export const astEditToolRenderer = {
 		const details = result.details;
 
 		if (result.isError) {
-			const errorText = result.content?.find(c => c.type === "text")?.text || "Unknown error";
-			const header = renderStatusLine({ icon: "error", title: "AST Edit" }, uiTheme);
+			const errorText = result.content?.find(c => c.type === "text")?.text || "未知错误";
+			const header = renderStatusLine({ icon: "error", title: "AST 编辑" }, uiTheme);
 			return framedToolCard(uiTheme, () => ({
 				header,
 				sections: [{ content: formatErrorDetail(errorText, uiTheme).split("\n") }],
@@ -125,10 +125,10 @@ export const astEditToolRenderer = {
 		if (totalReplacements === 0) {
 			const rewriteCount = args?.ops?.length ?? 0;
 			const description = rewriteCount === 1 ? patternPreview(args?.ops?.[0]?.pat) : undefined;
-			const meta = ["0 replacements"];
-			if (details?.scopePath) meta.push(`in ${details.scopePath}`);
-			if (filesSearched > 0) meta.push(`searched ${filesSearched}`);
-			const header = renderStatusLine({ icon: "warning", title: "AST Edit", description, meta }, uiTheme);
+			const meta = ["0 处替换"];
+			if (details?.scopePath) meta.push(`于 ${details.scopePath}`);
+			if (filesSearched > 0) meta.push(`已搜索 ${filesSearched} 个文件`);
+			const header = renderStatusLine({ icon: "warning", title: "AST 编辑", description, meta }, uiTheme);
 			// The "0 replacements" count already rides on the status line; only parse
 			// errors are worth a body, so frame solely when there are some.
 			const bodyLines: string[] = [];
@@ -144,9 +144,9 @@ export const astEditToolRenderer = {
 
 		const summaryParts = [formatCount("replacement", totalReplacements), formatCount("file", filesTouched)];
 		const meta = [...summaryParts];
-		if (details?.scopePath) meta.push(`in ${details.scopePath}`);
-		meta.push(`searched ${filesSearched}`);
-		if (limitReached) meta.push(uiTheme.fg("warning", "limit reached"));
+		if (details?.scopePath) meta.push(`于 ${details.scopePath}`);
+		meta.push(`已搜索 ${filesSearched} 个文件`);
+		if (limitReached) meta.push(uiTheme.fg("warning", "已达上限"));
 		const rewriteCount = args?.ops?.length ?? 0;
 		const description = rewriteCount === 1 ? patternPreview(args?.ops?.[0]?.pat) : undefined;
 
@@ -179,15 +179,15 @@ export const astEditToolRenderer = {
 			})
 			.map(indices => indices.map(index => styledLines[index]!));
 
-		const badge = { label: "proposed", color: "warning" as const };
+		const badge = { label: "已提议", color: "warning" as const };
 		const header = renderStatusLine(
-			{ icon: limitReached ? "warning" : "success", title: "AST Edit", description, badge, meta },
+			{ icon: limitReached ? "warning" : "success", title: "AST 编辑", description, badge, meta },
 			uiTheme,
 		);
 
 		const extraLines: string[] = [];
 		if (limitReached) {
-			extraLines.push(uiTheme.fg("warning", "limit reached; narrow path"));
+			extraLines.push(uiTheme.fg("warning", "已达上限;请缩小 path"));
 		}
 		if (details?.parseErrors?.length) {
 			extraLines.push(
diff --git a/pi-tui/src/tools/ast-grep.ts b/pi-tui-zh/src/tools/ast-grep.ts
index be84652..7d04c8c 100644
--- a/pi-tui/src/tools/ast-grep.ts
+++ b/pi-tui-zh/src/tools/ast-grep.ts
@@ -56,11 +56,11 @@ export const astGrepToolRenderer = {
 	renderCall(args: AstGrepRenderArgs, _options: RenderResultOptions, uiTheme: Theme): Component {
 		const meta: string[] = [];
 		const scopePaths = toPathList(args.path ?? args.paths);
-		if (scopePaths.length) meta.push(`in ${scopePaths.join(", ")}`);
+		if (scopePaths.length) meta.push(`于 ${scopePaths.join(", ")}`);
 		if (args.skip !== undefined && args.skip > 0) meta.push(`skip:${args.skip}`);
 
 		const description = args.pat ?? "?";
-		const text = renderStatusLine({ icon: "pending", title: "AST Grep", description, meta }, uiTheme);
+		const text = renderStatusLine({ icon: "pending", title: "AST 搜索", description, meta }, uiTheme);
 		return new Text(text, 0, 0);
 	},
 
@@ -73,7 +73,7 @@ export const astGrepToolRenderer = {
 		const details = result.details;
 
 		if (result.isError) {
-			const errorText = result.content?.find(c => c.type === "text")?.text || "Unknown error";
+			const errorText = result.content?.find(c => c.type === "text")?.text || "未知错误";
 			return new Text(formatErrorMessage(errorText, uiTheme), 0, 0);
 		}
 
@@ -84,13 +84,13 @@ export const astGrepToolRenderer = {
 
 		if (matchCount === 0) {
 			const description = args?.pat;
-			const meta = ["0 matches"];
-			if (details?.scopePath) meta.push(`in ${details.scopePath}`);
-			if (filesSearched > 0) meta.push(`searched ${filesSearched}`);
-			const header = renderStatusLine({ icon: "warning", title: "AST Grep", description, meta }, uiTheme);
-			const lines = [header, formatEmptyMessage("No matches found", uiTheme)];
+			const meta = ["0 处匹配"];
+			if (details?.scopePath) meta.push(`于 ${details.scopePath}`);
+			if (filesSearched > 0) meta.push(`已搜索 ${filesSearched} 个文件`);
+			const header = renderStatusLine({ icon: "warning", title: "AST 搜索", description, meta }, uiTheme);
+			const lines = [header, formatEmptyMessage("未找到匹配", uiTheme)];
 			if (details?.parseErrors?.length) {
-				lines.push(uiTheme.fg("warning", "Query may be mis-scoped; narrow `path` before concluding absence"));
+				lines.push(uiTheme.fg("warning", "查询可能范围过宽;请先缩小 `path` 再判定无结果"));
 				appendParseErrorsBulletList(lines, details.parseErrors, uiTheme, details.parseErrorsTotal);
 			}
 			return new Text(lines.join("\n"), 0, 0);
@@ -98,16 +98,16 @@ export const astGrepToolRenderer = {
 
 		const summaryParts = [formatCount("match", matchCount), formatCount("file", fileCount)];
 		const meta = [...summaryParts];
-		if (details?.scopePath) meta.push(`in ${details.scopePath}`);
-		meta.push(`searched ${filesSearched}`);
-		if (limitReached) meta.push(uiTheme.fg("warning", "limit reached"));
+		if (details?.scopePath) meta.push(`于 ${details.scopePath}`);
+		meta.push(`已搜索 ${filesSearched} 个文件`);
+		if (limitReached) meta.push(uiTheme.fg("warning", "已达上限"));
 		const description = args?.pat;
 		const header = renderStatusLine(
 			{
 				...(limitReached
 					? { icon: "warning" as const }
 					: { iconOverride: uiTheme.fg("accent", uiTheme.symbol("icon.search")) }),
-				title: "AST Grep",
+				title: "AST 搜索",
 				description,
 				meta,
 			},
@@ -141,7 +141,7 @@ export const astGrepToolRenderer = {
 
 		const extraLines: string[] = [];
 		if (limitReached) {
-			extraLines.push(uiTheme.fg("warning", "limit reached; narrow path or increase limit"));
+			extraLines.push(uiTheme.fg("warning", "已达上限;请缩小 path 或增大 limit"));
 		}
 		if (details?.parseErrors?.length) {
 			extraLines.push(
diff --git a/pi-tui/src/tools/bash-interactive.ts b/pi-tui-zh/src/tools/bash-interactive.ts
index f60badd..932a66c 100644
--- a/pi-tui/src/tools/bash-interactive.ts
+++ b/pi-tui-zh/src/tools/bash-interactive.ts
@@ -78,7 +78,7 @@ function normalizeInputForPty(data: string, applicationCursorKeysMode: boolean):
 /** Interactive terminal overlay driven by an external PTY controller. */
 export class BashInteractiveOverlayComponent implements Component {
 	#terminal: XtermTerminalType;
-	#state: "running" | "complete" | "timed_out" | "killed" = "running";
+	#state: "运行中" | "complete" | "timed_out" | "已终止" = "运行中";
 	#exitCode: number | undefined;
 	#onInput: (data: string) => void = () => {};
 	#onDismiss: () => void = () => {};
@@ -197,18 +197,18 @@ export class BashInteractiveOverlayComponent implements Component {
 			return;
 		}
 		if (result.cancelled) {
-			this.#state = "killed";
+			this.#state = "已终止";
 			return;
 		}
 		this.#state = "complete";
 	}
 
 	handleInput(data: string): void {
-		if (this.#state === "running" && (matchesKey(data, "escape") || matchesKey(data, "esc"))) {
+		if (this.#state === "运行中" && (matchesKey(data, "escape") || matchesKey(data, "esc"))) {
 			this.#onDismiss();
 			return;
 		}
-		if (this.#state !== "running") {
+		if (this.#state !== "运行中") {
 			return;
 		}
 		const normalizedInput = normalizeInputForPty(data, this.#terminal.modes.applicationCursorKeysMode);
@@ -218,12 +218,12 @@ export class BashInteractiveOverlayComponent implements Component {
 		this.#onInput(normalizedInput);
 	}
 	#stateText(): string {
-		if (this.#state === "running") return this.#uiTheme.fg("warning", "running");
-		if (this.#state === "timed_out") return this.#uiTheme.fg("warning", "timed out");
-		if (this.#state === "killed") return this.#uiTheme.fg("warning", "killed");
-		if (this.#exitCode === 0) return this.#uiTheme.fg("success", "exit 0");
-		if (this.#exitCode === undefined) return this.#uiTheme.fg("warning", "exited");
-		return this.#uiTheme.fg("error", `exit ${this.#exitCode}`);
+		if (this.#state === "运行中") return this.#uiTheme.fg("warning", "运行中");
+		if (this.#state === "timed_out") return this.#uiTheme.fg("warning", "超时");
+		if (this.#state === "已终止") return this.#uiTheme.fg("warning", "已终止");
+		if (this.#exitCode === 0) return this.#uiTheme.fg("success", "退出码 0");
+		if (this.#exitCode === undefined) return this.#uiTheme.fg("warning", "已退出");
+		return this.#uiTheme.fg("error", `退出码 ${this.#exitCode}`);
 	}
 
 	#readViewport(innerWidth: number, maxContentRows: number): string[] {
@@ -252,12 +252,12 @@ export class BashInteractiveOverlayComponent implements Component {
 			}
 		}
 		const statusIcon =
-			this.#state === "running"
-				? formatStatusIcon("running", this.#uiTheme)
+			this.#state === "运行中"
+				? formatStatusIcon("运行中", this.#uiTheme)
 				: this.#state === "complete" && this.#exitCode === 0
 					? this.#uiTheme.styledSymbol("tool.bash", "accent")
 					: formatStatusIcon("warning", this.#uiTheme);
-		const title = this.#uiTheme.fg("accent", "Console");
+		const title = this.#uiTheme.fg("accent", "控制台");
 		const statusBadge = `${this.#uiTheme.fg("dim", this.#uiTheme.format.bracketLeft)}${this.#stateText()}${this.#uiTheme.fg("dim", this.#uiTheme.format.bracketRight)}`;
 		const prefix = `${statusIcon} ${title} `;
 		const suffix = ` ${statusBadge}`;
@@ -265,12 +265,12 @@ export class BashInteractiveOverlayComponent implements Component {
 		const cmd = truncateToWidth(this.#uiTheme.fg("muted", replaceTabs(this.#command)), available);
 		const header = truncateToWidth(`${prefix}${cmd}${suffix}`, innerWidth);
 		const footer =
-			this.#state === "running"
+			this.#state === "运行中"
 				? truncateToWidth(
-						`${this.#uiTheme.fg("warning", "esc")} ${this.#uiTheme.fg("dim", "force-kill")} ${this.#uiTheme.fg("dim", "· input forwarded to PTY")}`,
+						`${this.#uiTheme.fg("warning", "esc")} ${this.#uiTheme.fg("dim", "强制终止")} ${this.#uiTheme.fg("dim", "· 输入已转发到 PTY")}`,
 						innerWidth,
 					)
-				: truncateToWidth(this.#uiTheme.fg("dim", "session finished"), innerWidth);
+				: truncateToWidth(this.#uiTheme.fg("dim", "会话已结束"), innerWidth);
 		const visibleLines = this.#readViewport(innerWidth, maxContentRows);
 		const content = visibleLines.length > 0 ? visibleLines : [padding(innerWidth)];
 		const borderHorizontal = this.#uiTheme.fg("border", this.#uiTheme.boxRound.horizontal.repeat(innerWidth));
diff --git a/pi-tui/src/tools/bash.ts b/pi-tui-zh/src/tools/bash.ts
index dfafe10..36dc7eb 100644
--- a/pi-tui/src/tools/bash.ts
+++ b/pi-tui-zh/src/tools/bash.ts
@@ -360,26 +360,26 @@ export function createShellRenderer<TArgs>(config: ShellRendererConfig<TArgs>) {
 					const wallTimeMs = details?.wallTimeMs;
 					const statsParts: string[] = [];
 					if (details?.async?.state === "running") {
-						statsParts.push(`Backgrounded: ${details.async.jobId}`);
+						statsParts.push(`后台运行: ${details.async.jobId}`);
 					}
 					if (wallTimeMs !== undefined) {
-						statsParts.push(`Wall: ${formatWallTimeSeconds(wallTimeMs)}s`);
+						statsParts.push(`耗时: ${formatWallTimeSeconds(wallTimeMs)}s`);
 					}
 					if (timeoutDisabled) {
-						statsParts.push("Timeout: disabled");
+						statsParts.push("超时: 已禁用");
 					}
 					if (typeof timeoutSeconds === "number") {
 						statsParts.push(
 							requestedTimeoutSeconds !== undefined && requestedTimeoutSeconds !== timeoutSeconds
-								? `Timeout: ${timeoutSeconds}s (requested ${requestedTimeoutSeconds}s clamped)`
-								: `Timeout: ${timeoutSeconds}s`,
+								? `超时: ${timeoutSeconds}s (请求 ${requestedTimeoutSeconds}s 已截断)`
+								: `超时: ${timeoutSeconds}s`,
 						);
 					}
 					if (rawOutputArtifact.artifactId) {
-						statsParts.push(`Artifact: ${rawOutputArtifact.artifactId}`);
+						statsParts.push(`产物: ${rawOutputArtifact.artifactId}`);
 					}
 					if (isError && typeof details?.exitCode === "number") {
-						statsParts.push(`Exit: ${details.exitCode}`);
+						statsParts.push(`退出码: ${details.exitCode}`);
 					}
 					const timeoutLine =
 						statsParts.length > 0
@@ -427,7 +427,7 @@ export function createShellRenderer<TArgs>(config: ShellRendererConfig<TArgs>) {
 								// render identically; only ctrl+o uncaps.
 								content: capPreviewLines(cmdLines ?? [], uiTheme, { expanded }),
 							},
-							{ label: uiTheme.fg("toolTitle", "Output"), content: outputLines },
+							{ label: uiTheme.fg("toolTitle", "输出"), content: outputLines },
 						],
 					};
 
diff --git a/pi-tui/src/tools/debug.ts b/pi-tui-zh/src/tools/debug.ts
index 5c51bfd..5c19728 100644
--- a/pi-tui/src/tools/debug.ts
+++ b/pi-tui-zh/src/tools/debug.ts
@@ -84,7 +84,7 @@ export function formatSessionSnapshot(snapshot: DebugSessionSnapshot): string[]
 }
 
 function summarizeDebugCall(args: DebugRenderArgs): string {
-	const action = args.action ? args.action.replaceAll("_", " ") : "request";
+	const action = args.action ? args.action.replaceAll("_", " ") : "请求";
 	if (args.program) {
 		return `${action} ${truncateToWidth(args.program, TRUNCATE_LENGTHS.TITLE)}`;
 	}
@@ -119,7 +119,7 @@ function summarizeDebugCall(args: DebugRenderArgs): string {
 export const debugToolRenderer = {
 	animatedPartialResult: true,
 	renderCall(args: DebugRenderArgs, _options: RenderResultOptions, theme: Theme): Component {
-		const text = renderStatusLine({ icon: "pending", title: "Debug", description: summarizeDebugCall(args) }, theme);
+		const text = renderStatusLine({ icon: "pending", title: "调试", description: summarizeDebugCall(args) }, theme);
 		return new Text(text, 0, 0);
 	},
 
@@ -135,11 +135,11 @@ export const debugToolRenderer = {
 			const statusIcon = success
 				? theme.styledSymbol("tool.debug", "accent")
 				: formatStatusIcon(options.isPartial ? "running" : "error", theme, options.spinnerFrame);
-			const header = `${statusIcon} Debug ${action}`;
+			const header = `${statusIcon} 调试 ${action}`;
 			const summaryLines = result.details?.snapshot
 				? formatSessionSnapshot(result.details.snapshot).map(line => replaceTabs(line))
 				: [];
-			const text = result.content.find(block => block.type === "text")?.text ?? "No output";
+			const text = result.content.find(block => block.type === "text")?.text ?? "无输出";
 			const rawLines = replaceTabs(text).split("\n");
 			const previewLimit = options.expanded ? PREVIEW_LIMITS.EXPANDED_LINES : PREVIEW_LIMITS.COLLAPSED_LINES;
 			const displayedLines = rawLines
@@ -148,15 +148,15 @@ export const debugToolRenderer = {
 			const remaining = rawLines.length - displayedLines.length;
 			if (remaining > 0) {
 				displayedLines.push(
-					theme.fg("muted", `… ${remaining} more lines ${formatExpandHint(theme, options.expanded, true)}`),
+					theme.fg("muted", `… 还有 ${remaining} 行 ${formatExpandHint(theme, options.expanded, true)}`),
 				);
 			}
 			return {
 				header,
 				phase: options.isPartial ? "partial" : result.isError ? "error" : "success",
 				sections: [
-					...(summaryLines.length > 0 ? [{ label: theme.fg("toolTitle", "Session"), content: summaryLines }] : []),
-					{ label: theme.fg("toolTitle", "Output"), content: displayedLines },
+					...(summaryLines.length > 0 ? [{ label: theme.fg("toolTitle", "会话"), content: summaryLines }] : []),
+					{ label: theme.fg("toolTitle", "输出"), content: displayedLines },
 				],
 				applyBg: false,
 			};
diff --git a/pi-tui/src/tools/default-renderer.ts b/pi-tui-zh/src/tools/default-renderer.ts
index 5fd1590..27fc16c 100644
--- a/pi-tui/src/tools/default-renderer.ts
+++ b/pi-tui-zh/src/tools/default-renderer.ts
@@ -86,7 +86,7 @@ function buildDefaultToolSnapshot(
 
 	if (options.expanded && input.args !== undefined) {
 		body.push("");
-		body.push(uiTheme.fg("dim", "Args"));
+		body.push(uiTheme.fg("dim", "参数"));
 		const tree = renderJsonTreeLines(
 			input.args,
 			uiTheme,
@@ -104,7 +104,7 @@ function buildDefaultToolSnapshot(
 	if (result) {
 		const textContent = result.output.trimEnd();
 		if (!textContent) {
-			body.push(uiTheme.fg("dim", "(no output)"));
+			body.push(uiTheme.fg("dim", "(无输出)"));
 		} else {
 			let renderedAsJson = false;
 			if (textContent.startsWith("{") || textContent.startsWith("[")) {
diff --git a/pi-tui/src/tools/eval.ts b/pi-tui-zh/src/tools/eval.ts
index ce7a614..5b9bdfd 100644
--- a/pi-tui/src/tools/eval.ts
+++ b/pi-tui-zh/src/tools/eval.ts
@@ -146,7 +146,7 @@ type AgentEventStatus = "pending" | "running" | "completed" | "failed" | "aborte
  * hundreds of throttled progress ticks.
  */
 export function upsertStatusEvent(events: EvalStatusEvent[], event: EvalStatusEvent): void {
-	if ((event.op === "agent" || event.op === "judge_batch") && typeof event.id === "string") {
+	if ((event.op === "代理" || event.op === "judge_batch") && typeof event.id === "string") {
 		const { op, id } = event;
 		const idx = events.findIndex(e => e.op === op && e.id === id);
 		if (idx >= 0) {
@@ -207,7 +207,7 @@ function renderAgentProgressEvents(
 					presentation: "eval",
 					status,
 					prefix,
-					id: sanitizeText(eventString(event.id) ?? "agent").replace(/\s+/g, " "),
+					id: sanitizeText(eventString(event.id) ?? "代理").replace(/\s+/g, " "),
 					width,
 					model: eventString(event.resolvedModelIdentity ?? event.model ?? event.resolvedModel),
 					thinkingLevel: event.resolvedThinkingLevel,
@@ -293,55 +293,55 @@ function formatStatusEvent(event: EvalStatusEvent, theme: Theme): string {
 
 	switch (op) {
 		case "read":
-			parts.push(`${data.chars ?? data.bytes ?? 0} chars`);
-			if (data.path) parts.push(`from ${shortenPath(String(data.path))}`);
+			parts.push(`${data.chars ?? data.bytes ?? 0} 字符`);
+			if (data.path) parts.push(`来自 ${shortenPath(String(data.path))}`);
 			break;
 		case "write":
-			parts.push(`${data.chars ?? data.bytes ?? 0} chars`);
-			if (data.path) parts.push(`to ${shortenPath(String(data.path))}`);
+			parts.push(`${data.chars ?? data.bytes ?? 0} 字符`);
+			if (data.path) parts.push(`至 ${shortenPath(String(data.path))}`);
 			break;
 		case "cat":
-			parts.push(`${data.files} file${(data.files as number) !== 1 ? "s" : ""}`);
-			parts.push(`${data.chars} chars`);
+			parts.push(`${data.files} 个文件`);
+			parts.push(`${data.chars} 字符`);
 			break;
 		case "ls":
-			parts.push(`${data.count} entr${(data.count as number) !== 1 ? "ies" : "y"}`);
+			parts.push(`${data.count} 个条目`);
 			break;
 		case "env":
 			if (data.action === "set") {
-				parts.push(`set ${data.key}=${truncateToWidth(String(data.value ?? ""), 30)}`);
+				parts.push(`设置 ${data.key}=${truncateToWidth(String(data.value ?? ""), 30)}`);
 			} else if (data.action === "get") {
 				parts.push(`${data.key}=${truncateToWidth(String(data.value ?? ""), 30)}`);
 			} else {
-				parts.push(`${data.count} variable${(data.count as number) !== 1 ? "s" : ""}`);
+				parts.push(`${data.count} 个变量`);
 			}
 			break;
 		case "git_status":
 			if (data.clean) {
-				parts.push("clean");
+				parts.push("无更改");
 			} else {
 				const statusParts: string[] = [];
-				if (data.staged) statusParts.push(`${data.staged} staged`);
-				if (data.modified) statusParts.push(`${data.modified} modified`);
-				if (data.untracked) statusParts.push(`${data.untracked} untracked`);
-				parts.push(statusParts.join(", ") || "unknown");
+				if (data.staged) statusParts.push(`${data.staged} 个已暂存`);
+				if (data.modified) statusParts.push(`${data.modified} 个已修改`);
+				if (data.untracked) statusParts.push(`${data.untracked} 个未跟踪`);
+				parts.push(statusParts.join(", ") || "未知");
 			}
-			if (data.branch) parts.push(`on ${data.branch}`);
+			if (data.branch) parts.push(`分支 ${data.branch}`);
 			break;
 		case "git_log":
-			parts.push(`${data.commits} commit${(data.commits as number) !== 1 ? "s" : ""}`);
+			parts.push(`${data.commits} 个提交`);
 			break;
 		case "git_diff":
-			parts.push(`${data.lines} line${(data.lines as number) !== 1 ? "s" : ""}`);
-			if (data.staged) parts.push("(staged)");
+			parts.push(`${data.lines} 行`);
+			if (data.staged) parts.push("(已暂存)");
 			break;
 		case "batch":
-			parts.push(`${data.files} file${(data.files as number) !== 1 ? "s" : ""} processed`);
+			parts.push(`${data.files} 个文件已处理`);
 			break;
 		case "completion":
 			if (data.model) parts.push(String(data.model));
 			if (data.tier && data.tier !== data.model) parts.push(`(${data.tier})`);
-			parts.push(`${data.chars ?? 0} chars`);
+			parts.push(`${data.chars ?? 0} 字符`);
 			break;
 		case "tool_define":
 			parts.push(`${data.name}(${(Array.isArray(data.params) ? data.params : []).join(", ")})`);
@@ -412,7 +412,7 @@ function formatStatusEventExpanded(event: EvalStatusEvent, theme: Theme): string
 			lines.push(`   ${theme.fg("toolOutput", truncateToWidth(replaceTabs(line), 80))}`);
 		}
 		if (previewLines.hidden > 0) {
-			lines.push(`   ${theme.fg("dim", `… ${previewLines.hidden} more lines`)}`);
+			lines.push(`   ${theme.fg("dim", `… 还有 ${previewLines.hidden} 行`)}`);
 		}
 	};
 
@@ -465,7 +465,7 @@ function renderStatusEvents(events: EvalStatusEvent[], theme: Theme, expanded: b
 
 	const lines: string[] = [];
 	if (hidden > 0) {
-		lines.push(`${theme.fg("dim", theme.tree.branch)} ${theme.fg("dim", `… ${hidden} earlier`)}`);
+		lines.push(`${theme.fg("dim", theme.tree.branch)} ${theme.fg("dim", `… 之前还有 ${hidden} 条`)}`);
 	}
 	for (let i = 0; i < visible.length; i++) {
 		const isLast = i === visible.length - 1;
@@ -626,7 +626,7 @@ export const evalToolRenderer = {
 		const timeoutSeconds = options.renderContext?.timeout;
 		const timeoutLine =
 			typeof timeoutSeconds === "number"
-				? uiTheme.fg("dim", wrapBrackets(`Timeout: ${timeoutSeconds}s`, uiTheme))
+				? uiTheme.fg("dim", wrapBrackets(`超时: ${timeoutSeconds}s`, uiTheme))
 				: undefined;
 		let warningLine: string | undefined;
 		if (details?.meta?.truncation || details?.meta?.artifactError) {
@@ -662,8 +662,8 @@ export const evalToolRenderer = {
 					for (let i = 0; i < displayCells.length; i++) {
 						const { cell, code, language } = displayCells[i];
 						const allEvents = cell.statusEvents ?? [];
-						const agentEvents = allEvents.filter(e => e.op === "agent");
-						const otherEvents = agentEvents.length > 0 ? allEvents.filter(e => e.op !== "agent") : allEvents;
+						const agentEvents = allEvents.filter(e => e.op === "代理");
+						const otherEvents = agentEvents.length > 0 ? allEvents.filter(e => e.op !== "代理") : allEvents;
 						const statusLines = renderStatusEvents(otherEvents, uiTheme, expanded);
 						const outputContent = formatCellOutputLines(cell, expanded, previewLines, uiTheme, width);
 						const outputLines = [...outputContent.lines];
@@ -674,7 +674,7 @@ export const evalToolRenderer = {
 						}
 						if (statusLines.length > 0) {
 							if (outputLines.length > 0) {
-								outputLines.push(uiTheme.fg("dim", "Status"));
+								outputLines.push(uiTheme.fg("dim", "状态"));
 							}
 							outputLines.push(...statusLines);
 						}
@@ -748,7 +748,7 @@ export const evalToolRenderer = {
 
 		if (!combinedOutput && statusLines.length > 0) {
 			const lines = [
-				uiTheme.fg("dim", "Status"),
+				uiTheme.fg("dim", "状态"),
 				...statusLines,
 				timeoutLine,
 				noticeLine,
@@ -765,7 +765,7 @@ export const evalToolRenderer = {
 				.join("\n");
 			const lines = [
 				styledOutput,
-				...(statusLines.length > 0 ? [uiTheme.fg("dim", "Status"), ...statusLines] : []),
+				...(statusLines.length > 0 ? [uiTheme.fg("dim", "状态"), ...statusLines] : []),
 				timeoutLine,
 				noticeLine,
 				asyncLine,
@@ -809,7 +809,7 @@ export const evalToolRenderer = {
 				}
 				outputLines.push(...cachedLines);
 				if (statusLines.length > 0) {
-					outputLines.push(truncateToWidth(uiTheme.fg("dim", "Status"), width));
+					outputLines.push(truncateToWidth(uiTheme.fg("dim", "状态"), width));
 					for (const statusLine of statusLines) {
 						outputLines.push(truncateToWidth(statusLine, width));
 					}
diff --git a/pi-tui/src/tools/fetch.ts b/pi-tui-zh/src/tools/fetch.ts
index c0efe41..594fa9e 100644
--- a/pi-tui/src/tools/fetch.ts
+++ b/pi-tui-zh/src/tools/fetch.ts
@@ -35,7 +35,7 @@ export function repairCollapsedScheme(value: string): string {
 
 /** Recognize a valid raw, tail, or line-range URL selector token. */
 function isUrlSelectorToken(token: string): boolean {
-	if (token.toLowerCase() === "raw") return true;
+	if (token.toLowerCase() === "原始") return true;
 	if (/^-\d+$/.test(token)) return Number.parseInt(token.slice(1), 10) > 0;
 	return readSelectorRangeStart(token) !== undefined;
 }
@@ -82,7 +82,7 @@ function readUrlLinkTarget(input: string): string {
 	try {
 		const repaired = repairCollapsedScheme(input);
 		const embedded = tryExtractEmbeddedUrlSelector(repaired);
-		if (embedded && embedded.sels.filter(token => token.toLowerCase() !== "raw").length > 1) return input;
+		if (embedded && embedded.sels.filter(token => token.toLowerCase() !== "原始").length > 1) return input;
 		return embedded?.path ?? repaired;
 	} catch {
 		return input;
@@ -111,8 +111,8 @@ export function renderReadUrlCall(
 	const url = args.path ?? args.url ?? "";
 	const description = formatReadUrlDescription(url);
 	const meta: string[] = [];
-	if (args.raw) meta.push("raw");
-	const text = renderStatusLine({ icon: "pending", title: "Read", description, meta }, uiTheme);
+	if (args.raw) meta.push("原始");
+	const text = renderStatusLine({ icon: "pending", title: "读取", description, meta }, uiTheme);
 	return new Text(text, 0, 0);
 }
 
@@ -126,10 +126,10 @@ export function renderReadUrlResult(
 
 	if (result.isError || !details) {
 		const rawErrorText = result.content?.find(c => c.type === "text")?.text ?? "";
-		const errorText = (rawErrorText || "No response data").replace(/^Error:\s*/, "");
+		const errorText = (rawErrorText || "无响应数据").replace(/^Error:\s*/, "");
 		const urlText = details?.finalUrl ?? details?.url ?? "";
 		const description = urlText ? formatReadUrlDescription(urlText) : undefined;
-		const header = renderStatusLine({ icon: "error", title: "Read", description }, uiTheme);
+		const header = renderStatusLine({ icon: "error", title: "读取", description }, uiTheme);
 		const errorLines = sanitizeDisplayLines(errorText).map(line => uiTheme.fg("error", line));
 		return framedToolCard(uiTheme, () => ({
 			header,
@@ -147,7 +147,7 @@ export function renderReadUrlResult(
 	const header = renderStatusLine(
 		{
 			icon: truncated ? "warning" : "success",
-			title: "Read",
+			title: "读取",
 			description,
 		},
 		uiTheme,
@@ -162,23 +162,23 @@ export function renderReadUrlResult(
 	const contentLines = contentBody.split("\n").filter(l => l.trim());
 
 	const metadataLines: string[] = [
-		`${uiTheme.fg("muted", "Content-Type:")} ${details.contentType || "unknown"}`,
-		`${uiTheme.fg("muted", "Method:")} ${details.method}`,
+		`${uiTheme.fg("muted", "内容类型:")} ${details.contentType || "未知"}`,
+		`${uiTheme.fg("muted", "方法:")} ${details.method}`,
 	];
 	if (hasRedirect) {
 		metadataLines.push(
-			`${uiTheme.fg("muted", "Final URL:")} ${formatReadUrlMetadataValue(details.finalUrl, uiTheme)}`,
+			`${uiTheme.fg("muted", "最终 URL:")} ${formatReadUrlMetadataValue(details.finalUrl, uiTheme)}`,
 		);
 	}
-	const lineLabel = `${lineCount} line${lineCount === 1 ? "" : "s"}`;
-	metadataLines.push(`${uiTheme.fg("muted", "Lines:")} ${lineLabel}`);
-	metadataLines.push(`${uiTheme.fg("muted", "Chars:")} ${charCount}`);
+	const lineLabel = `${lineCount} 行`;
+	metadataLines.push(`${uiTheme.fg("muted", "行数:")} ${lineLabel}`);
+	metadataLines.push(`${uiTheme.fg("muted", "字符数:")} ${charCount}`);
 	if (truncated) {
-		metadataLines.push(uiTheme.fg("warning", `${uiTheme.status.warning} Output truncated`));
+		metadataLines.push(uiTheme.fg("warning", `${uiTheme.status.warning} 输出已截断`));
 		if (truncation?.artifactId) metadataLines.push(formatStyledArtifactReference(truncation.artifactId, uiTheme));
 	}
 	if (hasNotes) {
-		metadataLines.push(`${uiTheme.fg("muted", "Notes:")} ${details.notes.join("; ")}`);
+		metadataLines.push(`${uiTheme.fg("muted", "备注:")} ${details.notes.join("; ")}`);
 	}
 
 	let lastExpanded: boolean | undefined;
@@ -198,10 +198,10 @@ export function renderReadUrlResult(
 				contentPreviewLines =
 					previewLines.length > 0
 						? previewLines.map(line => uiTheme.fg("dim", line))
-						: [uiTheme.fg("dim", "(no content)")];
+						: [uiTheme.fg("dim", "(无内容)")];
 				if (remaining > 0) {
 					const hint = formatExpandHint(uiTheme, expanded, true);
-					contentPreviewLines.push(uiTheme.fg("muted", `… ${remaining} more lines${hint ? ` ${hint}` : ""}`));
+					contentPreviewLines.push(uiTheme.fg("muted", `… 还有 ${remaining} 行${hint ? ` ${hint}` : ""}`));
 				}
 				lastExpanded = expanded;
 			}
@@ -210,8 +210,8 @@ export function renderReadUrlResult(
 				header,
 				phase: truncated ? "warning" : "success",
 				sections: [
-					{ label: uiTheme.fg("toolTitle", "Metadata"), content: metadataLines },
-					{ label: uiTheme.fg("toolTitle", "Content Preview"), content: contentPreviewLines },
+					{ label: uiTheme.fg("toolTitle", "元数据"), content: metadataLines },
+					{ label: uiTheme.fg("toolTitle", "内容预览"), content: contentPreviewLines },
 				],
 				applyBg: false,
 			};
diff --git a/pi-tui/src/tools/find.ts b/pi-tui-zh/src/tools/find.ts
index 6eb4f1d..f7ea14e 100644
--- a/pi-tui/src/tools/find.ts
+++ b/pi-tui-zh/src/tools/find.ts
@@ -119,7 +119,7 @@ function renderHit(hit: FindHit, rangeLimit: number, cwd: string | undefined, th
 	const absPath = cwd === undefined ? undefined : path.join(cwd, hit.rel);
 	const link = (text: string, line?: number) =>
 		absPath === undefined ? text : fileHyperlink(absPath, text, { line });
-	const coverage = hit.truncated ? `${hit.linesSeen} lines judged, partial` : `${hit.linesSeen} lines judged`;
+	const coverage = hit.truncated ? `${hit.linesSeen} 行已判定,不完整` : `${hit.linesSeen} 行已判定`;
 	const lines = [
 		`${gauge(hit.contentScore, theme)} ${theme.fg(scoreColor(hit.contentScore), hit.contentScore.toFixed(2))} ${link(theme.fg("accent", hit.rel))} ${theme.fg("dim", coverage)}`,
 	];
@@ -143,12 +143,12 @@ export const findToolRenderer = {
 	renderCall(args: FindRenderArgs, options: RenderResultOptions, uiTheme: Theme): Component {
 		const keywords = args.grep_keywords ?? [];
 		const meta = keywords.length > 0 ? [keywords.join(" ")] : [];
-		if (args.path) meta.push(`in ${args.path}`);
+		if (args.path) meta.push(`于 ${args.path}`);
 		const text = renderStatusLine(
 			{
 				icon: "pending",
 				spinnerFrame: options.spinnerFrame,
-				title: "Find",
+				title: "语义查找",
 				titleColor: "toolTitle",
 				description: quoteQuery(args.query),
 				meta,
@@ -168,7 +168,7 @@ export const findToolRenderer = {
 		const text = result.content?.find(c => c.type === "text")?.text ?? "";
 
 		if (result.isError) {
-			return new Text(formatErrorMessage(text || "Unknown error", uiTheme), 1, 0);
+			return new Text(formatErrorMessage(text || "未知错误", uiTheme), 1, 0);
 		}
 
 		// Streaming progress: the tool reports the phase it is in.
@@ -177,7 +177,7 @@ export const findToolRenderer = {
 				{
 					icon: "pending",
 					spinnerFrame: options.spinnerFrame,
-					title: "Find",
+					title: "语义查找",
 					titleColor: "toolTitle",
 					description: quoteQuery(args?.query ?? details?.query),
 					meta: text ? [text] : [],
@@ -189,26 +189,26 @@ export const findToolRenderer = {
 
 		const { hits, stats, threshold } = details;
 		const description = quoteQuery(details.query);
-		const scope = details.scopePath === undefined ? [] : [`in ${details.scopePath}`];
+		const scope = details.scopePath === undefined ? [] : [`于 ${details.scopePath}`];
 		const meta = [
 			formatCount("hit", hits.length),
 			...scope,
-			`${stats.filesRead} files read`,
+			`${stats.filesRead} 个文件已读取`,
 			`τ ${threshold.toFixed(2)}`,
 			`${formatNumber(stats.inputTokens)} tokens`,
 			`$${stats.cost.toFixed(4)}`,
 			formatDuration(details.elapsedMs),
 		];
-		if (stats.errors > 0) meta.push(uiTheme.fg("warning", `${stats.errors} failed`));
+		if (stats.errors > 0) meta.push(uiTheme.fg("warning", `${stats.errors} 失败`));
 
 		if (hits.length === 0) {
-			const emptyMeta = ["0 hits", ...scope, `$${stats.cost.toFixed(4)}`, formatDuration(details.elapsedMs)];
-			if (stats.errors > 0) emptyMeta.push(uiTheme.fg("warning", `${stats.errors} failed`));
+			const emptyMeta = ["0 处命中", ...scope, `$${stats.cost.toFixed(4)}`, formatDuration(details.elapsedMs)];
+			if (stats.errors > 0) emptyMeta.push(uiTheme.fg("warning", `${stats.errors} 失败`));
 			const header = renderStatusLine(
-				{ icon: "warning", title: "Find", titleColor: "toolTitle", description, meta: emptyMeta },
+				{ icon: "warning", title: "语义查找", titleColor: "toolTitle", description, meta: emptyMeta },
 				uiTheme,
 			);
-			const lines = [header, formatEmptyMessage("No relevant passages found", uiTheme)];
+			const lines = [header, formatEmptyMessage("未找到相关片段", uiTheme)];
 			for (const failure of stats.failures) lines.push(uiTheme.fg("warning", failure));
 			return new Text(lines.join("\n"), 1, 0);
 		}
@@ -216,7 +216,7 @@ export const findToolRenderer = {
 		const header = renderStatusLine(
 			{
 				iconOverride: uiTheme.fg("toolTitle", uiTheme.symbol("icon.search")),
-				title: "Find",
+				title: "语义查找",
 				titleColor: "toolTitle",
 				description,
 				meta,
@@ -241,7 +241,7 @@ export const findToolRenderer = {
 				);
 				const extra: string[] = [];
 				if (expanded) {
-					extra.push(uiTheme.fg("dim", `keywords: ${details.keywords.join(", ")}`));
+					extra.push(uiTheme.fg("dim", `关键词: ${details.keywords.join(", ")}`));
 					for (const failure of stats.failures) extra.push(uiTheme.fg("warning", failure));
 				}
 				return [header, ...hitLines, ...extra].map(line => truncateToWidth(line, width, Ellipsis.Omit));
diff --git a/pi-tui/src/tools/github.ts b/pi-tui-zh/src/tools/github.ts
index 22491e9..bcf5320 100644
--- a/pi-tui/src/tools/github.ts
+++ b/pi-tui-zh/src/tools/github.ts
@@ -34,15 +34,15 @@ const PENDING_STATUSES = new Set(["queued", "requested", "waiting", "pending"]);
 const FALLBACK_WIDTH = 80;
 
 const OP_TITLES: Record<string, string> = {
-	repo_view: "GitHub Repo",
-	pr_checkout: "GitHub PR Checkout",
-	pr_push: "GitHub PR Push",
-	search_issues: "GitHub Search Issues",
-	search_prs: "GitHub Search PRs",
-	search_code: "GitHub Search Code",
-	search_commits: "GitHub Search Commits",
-	search_repos: "GitHub Search Repos",
-	run_watch: "GitHub Run Watch",
+	repo_view: "GitHub 仓库",
+	pr_checkout: "GitHub PR 检出",
+	pr_push: "GitHub PR 推送",
+	search_issues: "GitHub 搜索 Issue",
+	search_prs: "GitHub 搜索 PR",
+	search_code: "GitHub 搜索代码",
+	search_commits: "GitHub 搜索提交",
+	search_repos: "GitHub 搜索仓库",
+	run_watch: "GitHub 运行监视",
 };
 
 function formatOpTitle(op: string | undefined): string {
@@ -66,7 +66,7 @@ function formatPrIdentifier(pr: string | string[] | undefined): string | undefin
 		const parts = pr.map(p => extractIssueId(p)).filter((p): p is string => p !== undefined);
 		if (parts.length === 0) return undefined;
 		if (parts.length > 3) {
-			return `${parts.slice(0, 3).join(", ")}, +${parts.length - 3} more`;
+			return `${parts.slice(0, 3).join(", ")}, +${parts.length - 3} 更多`;
 		}
 		return parts.join(", ");
 	}
@@ -115,18 +115,18 @@ function buildOpMeta(args: GithubToolRenderArgs): string[] {
 function getWatchHeader(watch: GhRunWatchViewDetails): string {
 	if (watch.mode === "run" && watch.run) {
 		if (watch.state === "watching") {
-			return `watching run #${watch.run.id} on ${watch.repo}`;
+			return `正在监视运行 #${watch.run.id} 于 ${watch.repo}`;
 		}
 
-		return `run #${watch.run.id} on ${watch.repo}`;
+		return `运行 #${watch.run.id} 于 ${watch.repo}`;
 	}
 
-	const shortSha = formatShortSha(watch.headSha) ?? "this commit";
+	const shortSha = formatShortSha(watch.headSha) ?? "此提交";
 	if (watch.state === "watching") {
-		return `watching ${shortSha} on ${watch.repo}`;
+		return `正在监视提交 ${shortSha} 于 ${watch.repo}`;
 	}
 
-	return `workflow runs for ${shortSha} on ${watch.repo}`;
+	return `提交 ${shortSha} 的工作流运行 于 ${watch.repo}`;
 }
 
 function getRunLabel(run: GhRunWatchRunDetails): string {
@@ -215,7 +215,7 @@ function renderJobLine(job: GhRunWatchJobDetails, width: number, theme: Theme):
 function renderRunBlock(run: GhRunWatchRunDetails, width: number, theme: Theme): string[] {
 	const lines = [formatRunLine(run, theme)];
 	if (run.jobs.length === 0) {
-		lines.push(theme.fg("dim", "waiting for workflow jobs..."));
+		lines.push(theme.fg("dim", "等待工作流任务..."));
 		return lines;
 	}
 
@@ -237,13 +237,13 @@ function renderFailedLogs(
 
 	const lines: string[] = [];
 	for (const entry of failedLogs) {
-		const context = entry.workflowName ? `${entry.workflowName}  #${entry.runId}` : `run #${entry.runId}`;
+		const context = entry.workflowName ? `${entry.workflowName}  #${entry.runId}` : `运行 #${entry.runId}`;
 		lines.push(
 			theme.fg("error", `${theme.status.error} ${replaceTabs(entry.jobName)}  ${theme.fg("muted", context)}`),
 		);
 
 		if (!entry.available || !entry.tail) {
-			lines.push(theme.fg("dim", "  log tail unavailable"));
+			lines.push(theme.fg("dim", "  日志尾部不可用"));
 			continue;
 		}
 
@@ -257,7 +257,7 @@ function renderFailedLogs(
 
 		if (!expanded && tailLines.length > previewLimit) {
 			const remaining = tailLines.length - previewLimit;
-			lines.push(theme.fg("dim", `  … ${remaining} more log lines ${formatExpandHint(theme, false, true)}`));
+			lines.push(theme.fg("dim", `  … 还有 ${remaining} 行日志 ${formatExpandHint(theme, false, true)}`));
 		}
 	}
 
@@ -281,7 +281,7 @@ function buildWatchSections(
 	} else if (watch.mode === "commit") {
 		const runs = watch.runs ?? [];
 		if (runs.length === 0) {
-			main.push(theme.fg("dim", "waiting for workflow runs..."));
+			main.push(theme.fg("dim", "等待工作流运行..."));
 		} else {
 			runs.forEach((run, index) => {
 				if (index > 0) {
@@ -299,7 +299,7 @@ function buildWatchSections(
 
 	const failed = renderFailedLogs(watch.failedLogs ?? [], width, theme, options.expanded);
 	if (failed.length > 0) {
-		sections.push({ label: "failed logs", content: failed });
+		sections.push({ label: "失败日志", content: failed });
 	}
 
 	return sections;
@@ -342,7 +342,7 @@ function renderFallbackComponent(
 	);
 
 	if (!text) {
-		const empty = isError ? "request failed" : "no output";
+		const empty = isError ? "请求失败" : "无输出";
 		return new Text(`${header}\n${theme.fg("dim", empty)}`, 0, 0);
 	}
 
@@ -395,18 +395,18 @@ function renderWatchCall(args: GithubToolRenderArgs, options: RenderResultOption
 	const runId = typeof args.run === "string" && args.run.trim().length > 0 ? args.run.trim() : undefined;
 	const branch = typeof args.branch === "string" && args.branch.trim().length > 0 ? args.branch.trim() : undefined;
 
-	const titleText = theme.fg("accent", "GitHub Run Watch");
+	const titleText = theme.fg("accent", "GitHub 运行监视");
 	let metaText: string;
 	if (runId) {
 		metaText = theme.fg("muted", `#${runId}`);
 	} else if (branch) {
 		metaText = theme.fg("text", branch);
 	} else {
-		metaText = theme.fg("muted", "current HEAD");
+		metaText = theme.fg("muted", "当前 HEAD");
 	}
 
 	const header = `${icon} ${titleText}  ${metaText}`;
-	const wait = theme.fg("dim", "waiting for workflow data...");
+	const wait = theme.fg("dim", "等待工作流数据...");
 	return new Text(`${header}\n${wait}`, 0, 0);
 }
 
@@ -449,19 +449,19 @@ export const githubToolRenderer = {
 					? {
 							icon: options.spinnerFrame !== undefined ? "running" : "pending",
 							spinnerFrame: options.spinnerFrame,
-							title: "GitHub Run Watch",
+							title: "GitHub 运行监视",
 							meta: [getWatchHeader(watch)],
 						}
 					: isError
 						? {
 								icon: "error",
-								title: "GitHub Run Watch",
+								title: "GitHub 运行监视",
 								titleColor: "error",
 								meta: [getWatchHeader(watch)],
 							}
 						: {
 								iconOverride: uiTheme.styledSymbol("tool.gh", "accent"),
-								title: "GitHub Run Watch",
+								title: "GitHub 运行监视",
 								titleColor: "accent",
 								meta: [getWatchHeader(watch)],
 							},
diff --git a/pi-tui/src/tools/glob.ts b/pi-tui-zh/src/tools/glob.ts
index 3e0ff72..03a0636 100644
--- a/pi-tui/src/tools/glob.ts
+++ b/pi-tui-zh/src/tools/glob.ts
@@ -66,7 +66,7 @@ export const globToolRenderer = {
 		const text = renderStatusLine(
 			{
 				icon: "pending",
-				title: "Glob",
+				title: "查找",
 				titleColor: "toolTitle",
 				description: formatGlobRenderPaths(args) || "*",
 				meta,
@@ -85,7 +85,7 @@ export const globToolRenderer = {
 		const details = result.details;
 
 		if (result.isError || details?.error) {
-			const errorText = details?.error || result.content?.find(c => c.type === "text")?.text || "Unknown error";
+			const errorText = details?.error || result.content?.find(c => c.type === "text")?.text || "未知错误";
 			return new Text(formatErrorMessage(errorText, uiTheme), 1, 0);
 		}
 
@@ -96,17 +96,17 @@ export const globToolRenderer = {
 			if (
 				!textContent ||
 				textContent.includes("No files matching") ||
-				textContent.includes("No files found") ||
+				textContent.includes("未找到文件") ||
 				textContent.trim() === ""
 			) {
-				return new Text(formatEmptyMessage("No files found", uiTheme), 1, 0);
+				return new Text(formatEmptyMessage("未找到文件", uiTheme), 1, 0);
 			}
 
 			const lines = textContent.split("\n").filter(l => l.trim());
 			const header = renderStatusLine(
 				{
 					iconOverride: globStatusIcon(uiTheme),
-					title: "Glob",
+					title: "查找",
 					titleColor: "toolTitle",
 					description: formatGlobRenderPaths(args),
 					meta: [formatCount("file", lines.length)],
@@ -140,19 +140,19 @@ export const globToolRenderer = {
 
 		const missingPaths = details?.missingPaths ?? [];
 		const missingNote =
-			missingPaths.length > 0 ? uiTheme.fg("warning", `skipped missing: ${missingPaths.join(", ")}`) : undefined;
+			missingPaths.length > 0 ? uiTheme.fg("warning", `跳过缺失: ${missingPaths.join(", ")}`) : undefined;
 
 		if (fileCount === 0) {
 			// `truncated` on an empty result means the scan timed out mid-walk —
 			// render "incomplete", not a definitive "No files found".
-			const emptyLabel = truncated ? "No matches before timeout (scan incomplete)" : "No files found";
+			const emptyLabel = truncated ? "超时前未找到匹配(扫描未完成)" : "未找到文件";
 			const header = renderStatusLine(
 				{
 					icon: "warning",
-					title: "Glob",
+					title: "查找",
 					titleColor: "toolTitle",
 					description: formatGlobRenderPaths(args),
-					meta: truncated ? ["0 files", uiTheme.fg("warning", "timed out")] : ["0 files"],
+					meta: truncated ? ["0 个文件", uiTheme.fg("warning", "超时")] : ["0 个文件"],
 				},
 				uiTheme,
 			);
@@ -161,12 +161,12 @@ export const globToolRenderer = {
 			return new Text(lines.join("\n"), 1, 0);
 		}
 		const meta: string[] = [formatCount("file", fileCount)];
-		if (details?.scopePath) meta.push(`in ${details.scopePath}`);
-		if (truncated) meta.push(uiTheme.fg("warning", "truncated"));
+		if (details?.scopePath) meta.push(`于 ${details.scopePath}`);
+		if (truncated) meta.push(uiTheme.fg("warning", "已截断"));
 		const header = renderStatusLine(
 			{
 				...(truncated ? { icon: "warning" as const } : { iconOverride: globStatusIcon(uiTheme) }),
-				title: "Glob",
+				title: "查找",
 				titleColor: "toolTitle",
 				description: formatGlobRenderPaths(args),
 				meta,
@@ -175,15 +175,15 @@ export const globToolRenderer = {
 		);
 
 		const truncationReasons: string[] = [];
-		if (details?.resultLimitReached) truncationReasons.push(`limit ${details.resultLimitReached} results`);
-		if (limits?.resultLimit) truncationReasons.push(`limit ${limits.resultLimit.reached} results`);
-		if (truncation) truncationReasons.push(truncation.truncatedBy === "lines" ? "line limit" : "size limit");
+		if (details?.resultLimitReached) truncationReasons.push(`上限 ${details.resultLimitReached} 条结果`);
+		if (limits?.resultLimit) truncationReasons.push(`上限 ${limits.resultLimit.reached} 条结果`);
+		if (truncation) truncationReasons.push(truncation.truncatedBy === "lines" ? "行数上限" : "大小上限");
 		const artifactId = truncation && "artifactId" in truncation ? truncation.artifactId : undefined;
 		if (artifactId) truncationReasons.push(formatFullOutputReference(artifactId));
 
 		const extraLines: string[] = [];
 		if (truncationReasons.length > 0) {
-			extraLines.push(uiTheme.fg("warning", `truncated: ${truncationReasons.join(", ")}`));
+			extraLines.push(uiTheme.fg("warning", `已截断: ${truncationReasons.join(", ")}`));
 		}
 		if (missingNote) extraLines.push(missingNote);
 
diff --git a/pi-tui/src/tools/grep.ts b/pi-tui-zh/src/tools/grep.ts
index 8e5320b..07f880a 100644
--- a/pi-tui/src/tools/grep.ts
+++ b/pi-tui-zh/src/tools/grep.ts
@@ -84,7 +84,7 @@ const SEARCH_CODE_FRAME_LINE_RE = /^\s*\*?(\d+)│/;
 function searchScopeMeta(details: GrepToolDetails | undefined): string | undefined {
 	if (!details?.scopePath) return undefined;
 	const label = details.searchPath ? fileHyperlink(details.searchPath, details.scopePath) : details.scopePath;
-	return `in ${label}`;
+	return `于 ${label}`;
 }
 
 function linkUrlLikeSearchHeader(
@@ -243,13 +243,13 @@ export const grepToolRenderer = {
 	renderCall(args: GrepRenderArgs, _options: RenderResultOptions, uiTheme: Theme): Component {
 		const paths = toPathList(args.path ?? args.paths);
 		const meta: string[] = [];
-		if (paths.length) meta.push(`in ${paths.join(", ")}`);
+		if (paths.length) meta.push(`于 ${paths.join(", ")}`);
 		if (args.case === false) meta.push("case:insensitive");
 		if (args.gitignore === false) meta.push("gitignore:false");
 		if (args.skip !== undefined && args.skip > 0) meta.push(`skip:${args.skip}`);
 
 		const text = renderStatusLine(
-			{ icon: "pending", title: "Grep", titleColor: "toolTitle", description: args.pattern || "?", meta },
+			{ icon: "pending", title: "搜索", titleColor: "toolTitle", description: args.pattern || "?", meta },
 			uiTheme,
 		);
 		return new Text(text, 1, 0);
@@ -264,7 +264,7 @@ export const grepToolRenderer = {
 		const details = result.details;
 
 		if (result.isError || details?.error) {
-			const errorText = details?.error || result.content?.find(c => c.type === "text")?.text || "Unknown error";
+			const errorText = details?.error || result.content?.find(c => c.type === "text")?.text || "未知错误";
 			return new Text(formatErrorMessage(errorText, uiTheme), 1, 0);
 		}
 
@@ -272,15 +272,15 @@ export const grepToolRenderer = {
 
 		if (!hasDetailedData) {
 			const textContent = result.details?.displayContent ?? result.content?.find(c => c.type === "text")?.text;
-			if (!textContent || textContent === "No matches found") {
-				return new Text(formatEmptyMessage("No matches found", uiTheme), 1, 0);
+			if (!textContent || textContent === "未找到匹配") {
+				return new Text(formatEmptyMessage("未找到匹配", uiTheme), 1, 0);
 			}
 			const lines = textContent.split("\n").filter(line => line.trim() !== "");
 			const description = args?.pattern ?? undefined;
 			const header = renderStatusLine(
 				{
 					iconOverride: grepStatusIcon(uiTheme),
-					title: "Grep",
+					title: "搜索",
 					titleColor: "toolTitle",
 					description,
 					meta: [formatCount("item", lines.length)],
@@ -316,18 +316,18 @@ export const grepToolRenderer = {
 		const missingPathsList = details?.missingPaths ?? [];
 		const missingNote =
 			missingPathsList.length > 0
-				? uiTheme.fg("warning", `skipped missing: ${missingPathsList.join(", ")}`)
+				? uiTheme.fg("warning", `跳过缺失: ${missingPathsList.join(", ")}`)
 				: undefined;
 
 		if (matchCount === 0) {
-			const meta = ["0 matches"];
+			const meta = ["0 处匹配"];
 			const scopeMeta = searchScopeMeta(details);
 			if (scopeMeta) meta.push(scopeMeta);
 			const header = renderStatusLine(
-				{ icon: "warning", title: "Grep", titleColor: "toolTitle", description: args?.pattern, meta },
+				{ icon: "warning", title: "搜索", titleColor: "toolTitle", description: args?.pattern, meta },
 				uiTheme,
 			);
-			const lines = [header, formatEmptyMessage("No matches found", uiTheme)];
+			const lines = [header, formatEmptyMessage("未找到匹配", uiTheme)];
 			if (missingNote) lines.push(missingNote);
 			return new Text(lines.join("\n"), 1, 0);
 		}
@@ -336,12 +336,12 @@ export const grepToolRenderer = {
 		const meta = [...summaryParts];
 		const scopeMeta = searchScopeMeta(details);
 		if (scopeMeta) meta.push(scopeMeta);
-		if (truncated) meta.push(uiTheme.fg("warning", "truncated"));
+		if (truncated) meta.push(uiTheme.fg("warning", "已截断"));
 		const description = args?.pattern ?? undefined;
 		const header = renderStatusLine(
 			{
 				...(truncated ? { icon: "warning" as const } : { iconOverride: grepStatusIcon(uiTheme) }),
-				title: "Grep",
+				title: "搜索",
 				titleColor: "toolTitle",
 				description,
 				meta,
diff --git a/pi-tui/src/tools/hub.ts b/pi-tui-zh/src/tools/hub.ts
index 7df6764..cb5548d 100644
--- a/pi-tui/src/tools/hub.ts
+++ b/pi-tui-zh/src/tools/hub.ts
@@ -341,10 +341,10 @@ export function readyPendingSummary(daemon: DaemonSnapshot, ready?: LaunchParams
  * wait — the process may well be ready and simply still running.
  */
 export function waitPendingSummary(daemon: DaemonSnapshot, params: Pick<LaunchParams, "for" | "pattern">): string[] {
-	if (params.pattern) return [`output pattern /${params.pattern}/ never matched`];
-	if ((params.for ?? "exit") === "exit") return [`process exit (still ${daemon.state})`];
+	if (params.pattern) return [`输出模式 /${params.pattern}/ 从未匹配`];
+	if ((params.for ?? "exit") === "exit") return [`进程退出（仍为 ${daemon.state}）`];
 	const pending = readyPendingSummary(daemon);
-	return pending.length > 0 ? pending : [`readiness (still ${daemon.state})`];
+	return pending.length > 0 ? pending : [`就绪检查（仍为 ${daemon.state}）`];
 }
 /** Hub roster ordering (running before idle before parked) shared with the child prompt's live-row cap. */
 export const LIST_STATUS_ORDER: Record<string, number> = { running: 0, idle: 1, parked: 2 };
@@ -432,23 +432,23 @@ function flattenStructuredPreview(text: string): string {
 }
 
 function describeTarget(args: JobRenderArgs | undefined): string {
-	if (args?.list) return "background jobs";
+	if (args?.list) return "后台任务";
 	const poll = args?.poll ?? [];
 	const cancel = args?.cancel ?? [];
 	const parts: string[] = [];
 	if (cancel.length > 0) {
-		parts.push(cancel.length === 1 ? `cancel ${cancel[0]}` : `cancel ${cancel.length} jobs`);
+		parts.push(cancel.length === 1 ? `取消 ${cancel[0]}` : `取消 ${cancel.length} 个任务`);
 	}
 	if (poll.length > 0) {
-		parts.push(poll.length === 1 ? `poll ${poll[0]}` : `poll ${poll.length} jobs`);
+		parts.push(poll.length === 1 ? `轮询 ${poll[0]}` : `轮询 ${poll.length} 个任务`);
 	}
-	if (parts.length === 0) return "all running jobs";
+	if (parts.length === 0) return "所有运行中的任务";
 	return parts.join(", ");
 }
 
 /** Pending-call frame for job ops (wait/cancel/jobs). */
 export function jobsRenderCall(args: HubRenderArgs, _options: RenderResultOptions, uiTheme: Theme): Component {
-	const text = renderStatusLine({ icon: "pending", title: describeTarget(toJobRenderArgs(args)) || "Job" }, uiTheme);
+	const text = renderStatusLine({ icon: "pending", title: describeTarget(toJobRenderArgs(args)) || "任务" }, uiTheme);
 	return new Text(text, 0, 0);
 }
 
@@ -464,8 +464,8 @@ export function jobsRenderResult(
 	const agents = result.details?.agents ?? [];
 
 	if (jobs.length === 0 && agents.length === 0) {
-		const fallback = result.content?.find(c => c.type === "text")?.text || "No jobs to process";
-		const header = renderStatusLine({ icon: "warning", title: describeTarget(args) || "Job" }, uiTheme);
+		const fallback = result.content?.find(c => c.type === "text")?.text || "没有可处理的任务";
+		const header = renderStatusLine({ icon: "warning", title: describeTarget(args) || "任务" }, uiTheme);
 		return new Text([header, formatEmptyMessage(fallback, uiTheme)].join("\n"), 0, 0);
 	}
 
@@ -487,9 +487,9 @@ export function jobsRenderResult(
 	// The title already carries the running count, so meta lists only the
 	// settled categories — "waiting on 19 of 19 · 19 running" read awkward.
 	const meta: string[] = [];
-	if (counts.completed > 0) meta.push(uiTheme.fg("success", `${counts.completed} done`));
-	if (counts.failed > 0) meta.push(uiTheme.fg("error", `${counts.failed} failed`));
-	if (counts.cancelled > 0) meta.push(uiTheme.fg("warning", `${counts.cancelled} cancelled`));
+	if (counts.completed > 0) meta.push(uiTheme.fg("success", `${counts.completed} 已完成`));
+	if (counts.failed > 0) meta.push(uiTheme.fg("error", `${counts.failed} 失败`));
+	if (counts.cancelled > 0) meta.push(uiTheme.fg("warning", `${counts.cancelled} 已取消`));
 	if (agents.length > 0 && jobs.length > 0) {
 		meta.push(uiTheme.fg("accent", `${agents.length} agent${agents.length === 1 ? "" : "s"}`));
 	}
@@ -582,7 +582,7 @@ export function jobsRenderResult(
 							Math.max(0, rowWidth - visibleWidth(`${icon} ${typeBadge} ${durationSuffix}`)),
 							Ellipsis.Unicode,
 						);
-						const rawLabelLines = (job.label || "(no label)").split(/\r?\n/);
+						const rawLabelLines = (job.label || "(无标签)").split(/\r?\n/);
 						const maxLabelLines = expanded ? LABEL_LINES_EXPANDED : LABEL_LINES_COLLAPSED;
 						const visibleLabelLines = rawLabelLines
 							.slice(0, maxLabelLines)
@@ -748,7 +748,7 @@ function stateColor(state: DaemonState): ThemeColor {
 /** Compact `state · pid · uptime` fragments for the status-line meta slot. */
 function daemonMeta(daemon: DaemonSnapshot, theme: Theme): string[] {
 	const meta = [theme.fg(stateColor(daemon.state), daemon.state)];
-	if (daemon.readyPending?.length) meta.push(theme.fg("warning", `waiting on ${daemon.readyPending.join("+")}`));
+	if (daemon.readyPending?.length) meta.push(theme.fg("warning", `等待 ${daemon.readyPending.join("+")}`));
 	if (daemon.exitCode !== undefined) {
 		meta.push(theme.fg(daemon.exitCode === 0 ? "muted" : "error", `exit ${daemon.exitCode}`));
 	} else if (daemon.pid !== undefined) {
@@ -770,11 +770,11 @@ function launchCallMeta(args: LaunchRenderArgs): string[] {
 			if (args.application) meta.push([args.application, ...(args.args ?? [])].join(" "));
 			break;
 		case "logs":
-			if (args.follow) meta.push("follow");
+			if (args.follow) meta.push("跟随");
 			if (args.grep) meta.push(`grep /${args.grep}/`);
 			break;
 		case "wait":
-			meta.push(args.pattern ? `for /${args.pattern}/` : `for ${args.for ?? "exit"}`);
+			meta.push(args.pattern ? `等待 /${args.pattern}/` : `等待 ${args.for ?? "exit"}`);
 			break;
 		case "send":
 			if (args.signal) meta.push(args.signal);
@@ -792,7 +792,7 @@ export function launchRenderCall(args: LaunchRenderArgs, options: RenderResultOp
 		{
 			icon: options.spinnerFrame !== undefined ? "running" : "pending",
 			spinnerFrame: options.spinnerFrame,
-			title: `Launch ${args.op ?? "…"}`,
+			title: `启动 ${args.op ?? "…"}`,
 			description: target ? replaceTabs(target) : undefined,
 			meta: launchCallMeta(args),
 		},
@@ -831,7 +831,7 @@ export function launchRenderResult(
 			case "start": {
 				meta.push(...launchCallMeta(params));
 				if (daemon) meta.push(...daemonMeta(daemon, theme));
-				if (daemon?.readyMatch) body.push(theme.fg("dim", `log matched: ${replaceTabs(daemon.readyMatch)}`));
+				if (daemon?.readyMatch) body.push(theme.fg("dim", `日志匹配: ${replaceTabs(daemon.readyMatch)}`));
 				if (daemon?.state === "failed" && daemon.exitReason)
 					body.push(theme.fg("error", replaceTabs(daemon.exitReason)));
 				if (details?.timedOut) {
@@ -840,8 +840,8 @@ export function launchRenderResult(
 						theme.fg(
 							"warning",
 							pending.length > 0
-								? `Not ready — ${pending.join("; ")}. Still running.`
-								: "Readiness timed out; the process is still running.",
+								? `未就绪 — ${pending.join("; ")}。仍在运行。`
+								: "就绪检测超时;进程仍在运行。",
 						),
 					);
 				} else if (params.ready && daemon && daemon.readyAt === undefined && TERMINAL_STATES[daemon.state]) {
@@ -860,14 +860,14 @@ export function launchRenderResult(
 			case "wait": {
 				meta.push(...launchCallMeta(params));
 				if (daemon) meta.push(...daemonMeta(daemon, theme));
-				if (details?.matched) body.push(theme.fg("dim", `matched: ${replaceTabs(details.matched)}`));
+				if (details?.matched) body.push(theme.fg("dim", `匹配: ${replaceTabs(details.matched)}`));
 				if (details?.timedOut) {
 					body.push(
 						theme.fg(
 							"warning",
 							daemon
-								? `Wait timed out — still waiting on ${waitPendingSummary(daemon, params).join("; ")}.`
-								: "Wait timed out.",
+								? `等待超时 —— 仍在等待 ${waitPendingSummary(daemon, params).join("; ")}。`
+								: "等待超时。",
 						),
 					);
 				} else if (details && params.pattern && details.matched === undefined) {
@@ -888,7 +888,7 @@ export function launchRenderResult(
 			case "logs": {
 				if (details?.state) meta.push(theme.fg(stateColor(details.state), details.state));
 				if (details?.cursor !== undefined) meta.push(`cursor ${details.cursor}`);
-				if (details?.timedOut) meta.push(theme.fg("warning", "follow timed out"));
+				if (details?.timedOut) meta.push(theme.fg("warning", "跟随超时"));
 				// Strip the trailing `[name: state; cursor=N]` status suffix `toolContent` appends.
 				const logText = text.replace(/\n?\[[^\n]*\]$/, "").trimEnd();
 				const terminalRows = details?.terminalRows;
@@ -926,7 +926,7 @@ export function launchRenderResult(
 				: options.isPartial
 					? { icon: "pending" as const }
 					: { iconOverride: theme.styledSymbol("tool.launch", "accent") }),
-			title: `Launch ${op ?? ""}`.trimEnd(),
+			title: `启动 ${op ?? ""}`.trimEnd(),
 			description: description ? replaceTabs(description) : undefined,
 			meta,
 		},
@@ -997,11 +997,11 @@ function outcomeColor(outcome: IrcDeliveryReceipt["outcome"]): ToolUIColor {
 function peerStatusBadge(status: string, theme: Theme): string {
 	switch (status) {
 		case "running":
-			return theme.fg("accent", `${theme.status.running} running`);
+			return theme.fg("accent", `${theme.status.running} 运行中`);
 		case "idle":
-			return theme.fg("success", `${theme.status.enabled} idle`);
+			return theme.fg("success", `${theme.status.enabled} 空闲`);
 		case "parked":
-			return theme.fg("muted", `${theme.status.shadowed} parked`);
+			return theme.fg("muted", `${theme.status.shadowed} 驻留`);
 		default:
 			return theme.fg("error", `${theme.status.aborted} ${status}`);
 	}
@@ -1053,9 +1053,9 @@ function callTitle(args: HubRenderArgs | undefined, theme: Theme): string {
 		case "wait":
 			return `IRC ${theme.nav.back} ${args.from?.trim() || "anyone"}`;
 		case "inbox":
-			return "IRC inbox";
+			return "IRC 收件箱";
 		case "list":
-			return "IRC peers";
+			return "IRC 代理";
 		default:
 			return "Hub";
 	}
@@ -1064,11 +1064,11 @@ function callTitle(args: HubRenderArgs | undefined, theme: Theme): string {
 function messagingCallMeta(args: HubRenderArgs | undefined): string[] {
 	const meta: string[] = [];
 	if (args?.op === "send") {
-		if (args.to === "all") meta.push("broadcast");
-		if (args.await) meta.push("await reply");
-		if (args.replyTo) meta.push("reply");
+		if (args.to === "all") meta.push("广播");
+		if (args.await) meta.push("等待回复");
+		if (args.replyTo) meta.push("回复");
 	}
-	if (args?.op === "inbox" && args.peek) meta.push("peek");
+	if (args?.op === "inbox" && args.peek) meta.push("预览");
 	return meta;
 }
 
@@ -1077,7 +1077,7 @@ function renderErrorResult(
 	args: HubRenderArgs | undefined,
 	theme: Theme,
 ): string[] {
-	const text = firstTextBlock(result) || "IRC call failed.";
+	const text = firstTextBlock(result) || "IRC 调用失败。";
 	return [
 		renderStatusLine({ icon: "error", title: callTitle(args, theme), meta: messagingCallMeta(args) }, theme),
 		formatErrorDetail(text, theme),
@@ -1118,7 +1118,7 @@ export function createIrcMessageCard(
 	const meta: string[] = [];
 	if (card.kind === "autoreply") meta.push("auto");
 	if (card.kind === "workpool" && card.mode) meta.push(card.mode);
-	if (card.replyTo) meta.push("reply");
+	if (card.replyTo) meta.push("回复");
 	const age = messageAge(card.timestamp);
 	if (age) meta.push(age);
 	return createCachedComponent(
@@ -1147,7 +1147,7 @@ function renderSendResult(
 
 	// Pre-delivery failures (validation) and empty broadcasts carry no receipts.
 	if (receipts.length === 0) {
-		const text = firstTextBlock(result) || (result.isError ? "Send failed." : "Nothing to deliver.");
+		const text = firstTextBlock(result) || (result.isError ? "发送失败。" : "没有可发送的内容。");
 		return [
 			renderStatusLine({ icon: result.isError ? "error" : "warning", title }, theme),
 			result.isError ? formatErrorDetail(text, theme) : `  ${theme.fg("muted", replaceTabs(text))}`,
@@ -1160,15 +1160,15 @@ function renderSendResult(
 	const timedOut = waited === null;
 
 	const meta: string[] = [];
-	if (to === "all") meta.push("broadcast");
+	if (to === "all") meta.push("广播");
 	if (receipts.length === 1) {
 		const receipt = receipts[0]!;
 		meta.push(theme.fg(outcomeColor(receipt.outcome), receipt.outcome));
 	} else {
-		if (delivered.length > 0) meta.push(theme.fg("success", `${delivered.length} delivered`));
-		if (failedCount > 0) meta.push(theme.fg("error", `${failedCount} failed`));
+		if (delivered.length > 0) meta.push(theme.fg("success", `${delivered.length} 已送达`));
+		if (failedCount > 0) meta.push(theme.fg("error", `${failedCount} 失败`));
 	}
-	if (timedOut) meta.push(theme.fg("warning", "no reply"));
+	if (timedOut) meta.push(theme.fg("warning", "暂无回复"));
 
 	const icon = result.isError
 		? { icon: "error" as const }
@@ -1187,7 +1187,7 @@ function renderSendResult(
 					items: receipts,
 					expanded,
 					maxCollapsed: PREVIEW_LIMITS.COLLAPSED_ITEMS,
-					itemType: "recipient",
+					itemType: "个收件人",
 					renderItem: receipt => {
 						const badge = formatBadge(receipt.outcome, outcomeColor(receipt.outcome), theme);
 						const error =
@@ -1223,7 +1223,7 @@ function renderWaitResult(
 ): string[] {
 	const waited = details.waited;
 	if (!waited) {
-		const text = firstTextBlock(result) || "No message arrived.";
+		const text = firstTextBlock(result) || "未收到消息。";
 		return [
 			renderStatusLine(
 				{ icon: "warning", title: `IRC ${theme.nav.back} ${args?.from?.trim() || "anyone"}`, meta: ["timed out"] },
@@ -1233,7 +1233,7 @@ function renderWaitResult(
 		];
 	}
 	const meta = [messageAge(waited.ts)];
-	if (waited.replyTo) meta.push("reply");
+	if (waited.replyTo) meta.push("回复");
 	return [
 		renderStatusLine({ iconOverride: ircGlyph(theme), title: `IRC ${theme.nav.back} ${waited.from}`, meta }, theme),
 		...bodyLines(waited.body, expanded, theme, { indent: "  " }),
@@ -1248,11 +1248,11 @@ function renderInboxResult(
 ): string[] {
 	const messages = details.inbox ?? [];
 	if (messages.length === 0) {
-		return [renderStatusLine({ iconOverride: ircGlyph(theme), title: "IRC inbox", meta: ["empty"] }, theme)];
+		return [renderStatusLine({ iconOverride: ircGlyph(theme), title: "IRC 收件箱", meta: ["空"] }, theme)];
 	}
 	const meta = [`${messages.length} ${messages.length === 1 ? "message" : "messages"}`];
-	if (args?.peek) meta.push("peek");
-	const header = renderStatusLine({ iconOverride: ircGlyph(theme), title: "IRC inbox", meta }, theme);
+	if (args?.peek) meta.push("预览");
+	const header = renderStatusLine({ iconOverride: ircGlyph(theme), title: "IRC 收件箱", meta }, theme);
 	const items = renderTreeList<IrcMessage>(
 		{
 			items: messages,
@@ -1261,7 +1261,7 @@ function renderInboxResult(
 			itemType: "message",
 			renderItem: msg => {
 				const age = messageAge(msg.ts);
-				const replyBadge = msg.replyTo ? ` ${formatBadge("reply", "muted", theme)}` : "";
+				const replyBadge = msg.replyTo ? ` ${formatBadge("回复", "muted", theme)}` : "";
 				const head = `${theme.fg("accent", msg.from)}${age ? ` ${theme.fg("dim", age)}` : ""}${replyBadge}`;
 				return [head, ...bodyLines(msg.body, expanded, theme, { collapsedLines: 1 })];
 			},
@@ -1286,8 +1286,8 @@ function renderListResult(details: Partial<CoordinationDetails>, expanded: boole
 						`${rosterCounts.parked} parked`,
 						...(rosterCounts.truncated > 0 ? [`${rosterCounts.truncated} truncated`] : []),
 					]
-				: ["no other agents"];
-		return [renderStatusLine({ icon: "info", title: "IRC peers", meta }, theme)];
+				: ["没有其他代理"];
+		return [renderStatusLine({ icon: "info", title: "IRC 代理", meta }, theme)];
 	}
 	const counts = new Map<string, number>();
 	for (const peer of peers) counts.set(peer.status, (counts.get(peer.status) ?? 0) + 1);
@@ -1300,16 +1300,16 @@ function renderListResult(details: Partial<CoordinationDetails>, expanded: boole
 			]
 		: [...counts].map(([status, count]) => `${count} ${status}`);
 	const unreadTotal = peers.reduce((sum, peer) => sum + peer.unread, 0);
-	if (unreadTotal > 0) meta.push(theme.fg("warning", `${unreadTotal} unread`));
-	const header = renderStatusLine({ iconOverride: ircGlyph(theme), title: "IRC peers", meta }, theme);
+	if (unreadTotal > 0) meta.push(theme.fg("warning", `${unreadTotal} 条未读`));
+	const header = renderStatusLine({ iconOverride: ircGlyph(theme), title: "IRC 代理", meta }, theme);
 	const items = renderTreeList(
 		{
 			items: peers,
 			expanded,
 			maxCollapsed: PREVIEW_LIMITS.COLLAPSED_ITEMS,
-			itemType: "peer",
+			itemType: "个代理",
 			renderItem: peer => {
-				const kindText = peer.parentId ? `${peer.kind}${theme.sep.dot}of ${peer.parentId}` : peer.kind;
+				const kindText = peer.parentId ? `${peer.kind}${theme.sep.dot}所属 ${peer.parentId}` : peer.kind;
 				const unread = peer.unread > 0 ? ` ${formatBadge(`${peer.unread} unread`, "warning", theme)}` : "";
 				const age = messageAge(peer.lastActivity);
 				const activity = peer.activity ? ` ${theme.fg("dim", replaceTabs(peer.activity))}` : "";
@@ -1340,7 +1340,7 @@ function buildResultLines(
 		case "list":
 			return result.isError ? renderErrorResult(result, args, theme) : renderListResult(details, expanded, theme);
 		default: {
-			const text = firstTextBlock(result) || (result.isError ? "Hub call failed." : "Done.");
+			const text = firstTextBlock(result) || (result.isError ? "Hub 调用失败。" : "完成。");
 			return [
 				renderStatusLine({ icon: result.isError ? "error" : "success", title: callTitle(args, theme) }, theme),
 				result.isError ? formatErrorDetail(text, theme) : `  ${theme.fg("muted", replaceTabs(text))}`,
diff --git a/pi-tui/src/tools/json-tree.ts b/pi-tui-zh/src/tools/json-tree.ts
index 8924432..71e9df3 100644
--- a/pi-tui/src/tools/json-tree.ts
+++ b/pi-tui-zh/src/tools/json-tree.ts
@@ -31,7 +31,7 @@ const ARGS_INLINE_MORE_WIDTH = Bun.stringWidth(ARGS_INLINE_MORE);
 const ARGS_INLINE_TAIL_VALUE_RESERVE = 4;
 
 function isRecord(value: unknown): value is Record<string, unknown> {
-	return !!value && typeof value === "object" && !Array.isArray(value);
+	return !!value && typeof value === "对象" && !Array.isArray(value);
 }
 
 /** Sanitization, string summary, and budgeting policy for inline JSON. */
@@ -61,10 +61,10 @@ export function formatScalar(value: unknown, maxLen: number, options: InlineForm
 		const truncated = truncateToWidth(escaped, maxLen);
 		return `"${truncated}"`;
 	}
-	if (Array.isArray(value)) return `[${value.length} items]`;
-	if (typeof value === "object") {
+	if (Array.isArray(value)) return `[${value.length} 项]`;
+	if (typeof value === "对象") {
 		const keys = Object.keys(value);
-		return `{${keys.length} keys}`;
+		return `{${keys.length} 个键}`;
 	}
 	const text = String(value);
 	return options.sanitizeText?.(text) ?? text;
@@ -144,7 +144,7 @@ const OUTPUT_INLINE_OPTIONS: InlineFormatOptions = {
 export function formatOutputInline(data: unknown, maxWidth = 80): string {
 	const options = OUTPUT_INLINE_OPTIONS;
 	if (data === null || data === undefined) return "Output: none";
-	if (typeof data !== "object") return `Output: ${formatScalar(data, 60, options)}`;
+	if (typeof data !== "对象") return `Output: ${formatScalar(data, 60, options)}`;
 	if (Array.isArray(data)) {
 		if (data.length === 0) return "Output: []";
 		return `Output: [${data.length} items] ${formatScalar(data[0], 40, options)}${data.length > 1 ? "…" : ""}`;
@@ -173,7 +173,7 @@ export interface JsonTreeRenderOptions {
 	rootConnectors?: "hooked" | "siblings";
 }
 
-type JsonTreeNodeKind = "array" | "object" | "scalar" | "placeholder";
+type JsonTreeNodeKind = "数组" | "对象" | "scalar" | "placeholder";
 
 interface JsonTreeNode {
 	id: number;
@@ -186,8 +186,8 @@ interface JsonTreeNode {
 }
 
 function jsonNodeKind(value: unknown): JsonTreeNodeKind {
-	if (Array.isArray(value)) return "array";
-	if (isRecord(value)) return "object";
+	if (Array.isArray(value)) return "数组";
+	if (isRecord(value)) return "对象";
 	return "scalar";
 }
 
@@ -269,7 +269,7 @@ export function renderJsonTreeLines(
 		getChildren: item => {
 			if (item.children) return item.children;
 			let children: readonly JsonTreeNode[] = [];
-			if (item.kind === "array" && Array.isArray(item.value)) {
+			if (item.kind === "数组" && Array.isArray(item.value)) {
 				const values = item.value;
 				if (values.length === 0) {
 					children = [];
@@ -287,7 +287,7 @@ export function renderJsonTreeLines(
 				} else {
 					children = values.map((child, index) => node(child, `[${index}]`, item.depth + 1));
 				}
-			} else if (item.kind === "object" && isRecord(item.value)) {
+			} else if (item.kind === "对象" && isRecord(item.value)) {
 				const record = item.value;
 				const keys = Object.keys(record);
 				if (item.depth >= maxDepth) {
@@ -329,13 +329,13 @@ export function renderJsonTreeLines(
 			const displayKey = item.key ? sanitize(item.key) : undefined;
 			const label = theme.fg(
 				"muted",
-				displayKey || (item.kind === "array" ? "array" : item.kind === "object" ? "object" : "value"),
+				displayKey || (item.kind === "数组" ? "数组" : item.kind === "对象" ? "对象" : "值"),
 			);
 			if (item.kind === "placeholder") {
 				body = [theme.fg("dim", item.placeholder ?? "…")];
-			} else if (item.kind === "array") {
+			} else if (item.kind === "数组") {
 				body = [`${label} ${theme.fg("dim", `[${(item.value as unknown[]).length}]`)}`];
-			} else if (item.kind === "object") {
+			} else if (item.kind === "对象") {
 				body = [`${label} ${theme.fg("dim", `{${Object.keys(item.value as object).length}}`)}`];
 			} else if (typeof item.value === "string") {
 				const sanitized = sanitize(item.value);
@@ -357,7 +357,7 @@ export function renderJsonTreeLines(
 					if (sourceLines.length > displayedCount) {
 						scalarTruncated = true;
 						scalarLines.push(
-							`${indent}${theme.fg("dim", `…(${sourceLines.length - displayedCount} more lines)"`)}`,
+							`${indent}${theme.fg("dim", `…(还有 ${sourceLines.length - displayedCount} 行)"`)}`,
 						);
 					} else {
 						const lastIndex = scalarLines.length - 1;
diff --git a/pi-tui/src/tools/memory.ts b/pi-tui-zh/src/tools/memory.ts
index 0db475a..1b75bb8 100644
--- a/pi-tui/src/tools/memory.ts
+++ b/pi-tui-zh/src/tools/memory.ts
@@ -79,7 +79,7 @@ function retainComponent(contents: string[], header: string, getExpanded: () =>
 		}
 		const remaining = contents.length - shown.length;
 		if (remaining > 0) {
-			lines.push(`  ${theme.fg("dim", `… ${remaining} more`)} ${formatExpandHint(theme, expanded, true)}`);
+			lines.push(`  ${theme.fg("dim", `… 还有 ${remaining} 个`)} ${formatExpandHint(theme, expanded, true)}`);
 		}
 		return lines.map(line => truncateToWidth(line, width, Ellipsis.Omit));
 	});
@@ -91,7 +91,7 @@ export const retainToolRenderer = {
 	mergeCallAndResult: true,
 	renderCall(args: RetainRenderArgs, options: RenderResultOptions, theme: Theme): Component {
 		const contents = retainContents(args);
-		const header = renderStatusLine({ icon: "pending", title: "Retain" }, theme);
+		const header = renderStatusLine({ icon: "pending", title: "保留" }, theme);
 		return retainComponent(contents, header, () => options.expanded, theme);
 	},
 	renderResult(
@@ -101,7 +101,7 @@ export const retainToolRenderer = {
 		args?: RetainRenderArgs,
 	): Component {
 		if (result.isError) {
-			return new Text(formatErrorMessage(resultText(result) || "Retain failed", theme), 0, 0);
+			return new Text(formatErrorMessage(resultText(result) || "保留失败", theme), 0, 0);
 		}
 		const contents = retainContents(args);
 		// `summary` is the tool's own "N memories stored/queued." line; drop the
@@ -110,7 +110,7 @@ export const retainToolRenderer = {
 		const header = renderStatusLine(
 			{
 				iconOverride: theme.styledSymbol("tool.memory", "accent"),
-				title: "Retain",
+				title: "保留",
 				meta: summary ? [summary] : undefined,
 			},
 			theme,
@@ -124,7 +124,7 @@ export const recallToolRenderer = {
 	inline: true,
 	mergeCallAndResult: true,
 	renderCall(args: QueryRenderArgs, _options: RenderResultOptions, theme: Theme): Component {
-		return new Text(queryHeader("Recall", args.query, "pending", theme), 0, 0);
+		return new Text(queryHeader("回忆", args.query, "pending", theme), 0, 0);
 	},
 	renderResult(
 		result: { content: Array<{ type: string; text?: string }>; isError?: boolean },
@@ -133,16 +133,16 @@ export const recallToolRenderer = {
 		args?: QueryRenderArgs,
 	): Component {
 		if (result.isError) {
-			return new Text(formatErrorMessage(resultText(result) || "Recall failed", theme), 0, 0);
+			return new Text(formatErrorMessage(resultText(result) || "回忆失败", theme), 0, 0);
 		}
 		const text = resultText(result);
 		const match = text.match(/^Found (\d+) relevant/);
 		const found = match ? Number(match[1]) : 0;
-		const meta = [found > 0 ? `${found} found` : "no matches"];
+		const meta = [found > 0 ? `找到 ${found} 个` : "未找到匹配"];
 		const header =
 			found > 0
-				? queryHeader("Recall", args?.query, "success", theme, meta, theme.styledSymbol("tool.memory", "accent"))
-				: queryHeader("Recall", args?.query, "warning", theme, meta);
+				? queryHeader("回忆", args?.query, "success", theme, meta, theme.styledSymbol("tool.memory", "accent"))
+				: queryHeader("回忆", args?.query, "warning", theme, meta);
 		if (found === 0) {
 			return new Text(header, 0, 0);
 		}
@@ -172,7 +172,7 @@ export const reflectToolRenderer = {
 	inline: true,
 	mergeCallAndResult: true,
 	renderCall(args: QueryRenderArgs, _options: RenderResultOptions, theme: Theme): Component {
-		return new Text(queryHeader("Reflect", args.query, "pending", theme), 0, 0);
+		return new Text(queryHeader("反思", args.query, "pending", theme), 0, 0);
 	},
 	renderResult(
 		result: { content: Array<{ type: string; text?: string }>; isError?: boolean },
@@ -181,10 +181,10 @@ export const reflectToolRenderer = {
 		args?: QueryRenderArgs,
 	): Component {
 		if (result.isError) {
-			return new Text(formatErrorMessage(resultText(result) || "Reflect failed", theme), 0, 0);
+			return new Text(formatErrorMessage(resultText(result) || "反思失败", theme), 0, 0);
 		}
 		const header = queryHeader(
-			"Reflect",
+			"反思",
 			args?.query,
 			"success",
 			theme,
@@ -205,7 +205,7 @@ export const reflectToolRenderer = {
 				const remaining = answerLines.length - shown.length;
 				if (remaining > 0) {
 					lines.push(
-						`  ${theme.fg("dim", `… ${remaining} more lines`)} ${formatExpandHint(theme, expanded, true)}`,
+						`  ${theme.fg("dim", `… 还有 ${remaining} 行`)} ${formatExpandHint(theme, expanded, true)}`,
 					);
 				}
 				return lines.map(line => truncateToWidth(line, width, Ellipsis.Omit));
diff --git a/pi-tui/src/tools/read.ts b/pi-tui-zh/src/tools/read.ts
index 3cb11ea..323cdad 100644
--- a/pi-tui/src/tools/read.ts
+++ b/pi-tui-zh/src/tools/read.ts
@@ -283,7 +283,7 @@ export const readToolRenderer = {
 			pathDisplay += `:${startLine}${endLine ? `-${endLine}` : ""}`;
 		}
 
-		const text = renderStatusLine({ icon: "pending", title: "Read", description: pathDisplay }, uiTheme);
+		const text = renderStatusLine({ icon: "pending", title: "读取", description: pathDisplay }, uiTheme);
 		return new Text(text, 0, 0);
 	},
 
@@ -318,7 +318,7 @@ export const readToolRenderer = {
 					offset: args?.offset,
 					sourcePath: result.details?.displayTarget ?? readSourceFsPath(result.details),
 				}) || shortenPath(rawPath);
-			let title = filePath ? `Read ${filePath}` : "Read";
+			let title = filePath ? `Read ${filePath}` : "读取";
 			if (args?.offset !== undefined || args?.limit !== undefined) {
 				const startLine = args.offset ?? 1;
 				const endLine = args.limit !== undefined ? startLine + args.limit - 1 : "";
@@ -339,7 +339,7 @@ export const readToolRenderer = {
 		// Fall back to the raw text, but strip the LLM-facing notice so it doesn't
 		// echo next to the styled warning line below.
 		const contentText = details?.displayContent?.text ?? stripOutputNotice(rawText, details?.meta);
-		const imageContent = result.content?.find(c => c.type === "image");
+		const imageContent = result.content?.find(c => c.type === "图片");
 		const rawPath =
 			typeof args?.file_path === "string" ? args.file_path : typeof args?.path === "string" ? args.path : "";
 		const renderPath = splitReadRenderPath(rawPath);
@@ -370,11 +370,11 @@ export const readToolRenderer = {
 				resolvedPath: details?.resolvedPath,
 				sourcePath: details?.displayTarget ?? readSourceFsPath(details),
 				suffixResolution: suffix,
-				fallbackLabel: "image",
+				fallbackLabel: "图片",
 			});
 			const correction = suffix ? ` ${uiTheme.fg("dim", `(corrected from ${shortenPath(suffix.from)})`)}` : "";
 			const header = renderStatusLine(
-				{ icon: suffix ? "warning" : "success", title: "Read", description: `${displayPath}${correction}` },
+				{ icon: suffix ? "warning" : "success", title: "读取", description: `${displayPath}${correction}` },
 				uiTheme,
 			);
 			const detailLines = contentText
@@ -405,7 +405,7 @@ export const readToolRenderer = {
 			offset: args?.offset,
 		});
 		const correction = suffix ? ` ${uiTheme.fg("dim", `(corrected from ${shortenPath(suffix.from)})`)}` : "";
-		let title = displayPath ? `Read ${displayPath}${correction}` : "Read";
+		let title = displayPath ? `Read ${displayPath}${correction}` : "读取";
 		if (args?.offset !== undefined || args?.limit !== undefined) {
 			const startLine = args.offset ?? 1;
 			const endLine = args.limit !== undefined ? startLine + args.limit - 1 : "";
diff --git a/pi-tui/src/tools/report-tool-issue.ts b/pi-tui-zh/src/tools/report-tool-issue.ts
index e99fbc3..a93009c 100644
--- a/pi-tui/src/tools/report-tool-issue.ts
+++ b/pi-tui-zh/src/tools/report-tool-issue.ts
@@ -4,7 +4,7 @@ import { renderDeviceCallPreview } from "./resolve";
 
 /** Call preview for an `xd://report_issue` write. */
 export function renderReportIssueDeviceCall(content: unknown, uiTheme: Theme): Component {
-	return renderDeviceCallPreview("Report Tool Issue", content, uiTheme);
+	return renderDeviceCallPreview("报告工具问题", content, uiTheme);
 }
 
 /** Device name for automatic tool issue reports. */
diff --git a/pi-tui/src/tools/resolve.ts b/pi-tui-zh/src/tools/resolve.ts
index d7919bc..5d56152 100644
--- a/pi-tui/src/tools/resolve.ts
+++ b/pi-tui-zh/src/tools/resolve.ts
@@ -43,7 +43,7 @@ export interface ResolveInvocation {
 
 /** Streaming-safe call preview for a resolution-device write: `Resolve/Reject/Propose: <text>`. */
 export function renderResolutionDeviceCall(device: ResolutionDeviceName, content: unknown, uiTheme: Theme): Component {
-	const title = device === PROPOSE_DEVICE_NAME ? "Propose" : device === REJECT_DEVICE_NAME ? "Reject" : "Resolve";
+	const title = device === PROPOSE_DEVICE_NAME ? "提议" : device === REJECT_DEVICE_NAME ? "拒绝" : "处理";
 	return renderDeviceCallPreview(title, content, uiTheme, Ellipsis.Omit);
 }
 
@@ -74,10 +74,10 @@ export const resolveRenderer = {
 		const text = renderStatusLine(
 			{
 				icon: "pending",
-				title: "Resolve",
+				title: "处理",
 				description: args.action,
 				badge: {
-					label: args.action === "apply" ? "proposed -> resolved" : "proposed -> rejected",
+					label: args.action === "apply" ? "已提议 → 已解决" : "已提议 → 已拒绝",
 					color: args.action === "apply" ? "success" : "warning",
 				},
 				meta: reason ? [uiTheme.fg("muted", reason)] : undefined,
@@ -93,8 +93,8 @@ export const resolveRenderer = {
 		uiTheme: Theme,
 	): Component {
 		const details = result.details;
-		const label = replaceTabs(details?.label ?? "pending action");
-		const reason = replaceTabs(details?.reason?.trim() || "No reason provided");
+		const label = replaceTabs(details?.label ?? "待处理操作");
+		const reason = replaceTabs(details?.reason?.trim() || "未提供原因");
 		const action = details?.action ?? "apply";
 		const isApply = action === "apply" && !result.isError;
 		const isFailedApply = action === "apply" && result.isError;
@@ -103,7 +103,7 @@ export const resolveRenderer = {
 		// reset (styledSymbol/status glyphs carry their own \x1b[39m) would drop the
 		// inverse block back to the default background mid-line.
 		const icon = uiTheme.symbol(isApply ? "tool.resolve" : "status.error");
-		const verb = isApply ? "Accept" : isFailedApply ? "Failed" : "Discard";
+		const verb = isApply ? "接受" : isFailedApply ? "失败" : "放弃";
 		const separator = ": ";
 		const separatorIndex = label.indexOf(separator);
 		const sourceLabel = separatorIndex > 0 ? label.slice(0, separatorIndex).trim() : undefined;
diff --git a/pi-tui/src/tools/todo.ts b/pi-tui-zh/src/tools/todo.ts
index 200809f..66d0b99 100644
--- a/pi-tui/src/tools/todo.ts
+++ b/pi-tui-zh/src/tools/todo.ts
@@ -154,7 +154,7 @@ function selectWithinCap<T extends { status: TodoStatus }>(
 		const hiddenActive = active.length - cap;
 		return {
 			items: active.slice(0, cap),
-			summary: `… ${hiddenActive} more active ${pluralize("todo", hiddenActive)}`,
+			summary: `… 还有 ${hiddenActive} 个进行中的任务`,
 		};
 	}
 
@@ -442,20 +442,20 @@ export const todoToolRenderer = {
 		// `renderStatusLine` only flattens CR/LF and leaves the rest to the caller.
 		const ops =
 			opsList.length === 0
-				? ["update"]
+				? ["更新"]
 				: opsList.map(e => {
-						const parts = [forDisplay(e.op ?? "update")];
+						const parts = [forDisplay(e.op ?? "更新")];
 						if (e.task) parts.push(forDisplay(e.task));
 						if (e.phase) parts.push(forDisplay(e.phase));
 						if (Array.isArray(e.items) && e.items.length) {
-							parts.push(`${e.items.length} item${e.items.length === 1 ? "" : "s"}`);
+							parts.push(`${e.items.length} 个条目`);
 						}
 						return parts.join(" ");
 					});
 		// No body worth boxing while the call streams — a lone status line reads
 		// cleaner than an empty frame. The container renders it without chrome.
 		const header = renderStatusLine(
-			{ icon: "pending", spinnerFrame: options?.spinnerFrame, title: "Todo", meta: ops },
+			{ icon: "pending", spinnerFrame: options?.spinnerFrame, title: "任务", meta: ops },
 			uiTheme,
 		);
 		return new Text(header, 0, 0);
@@ -468,8 +468,8 @@ export const todoToolRenderer = {
 		args?: TodoRenderArgs,
 	): Component {
 		if (result.isError) {
-			const errorText = result.content?.find(content => content.type === "text")?.text ?? "Todo operation failed";
-			const header = renderStatusLine({ icon: "error", title: "Todo" }, uiTheme);
+			const errorText = result.content?.find(content => content.type === "text")?.text ?? "任务操作失败";
+			const header = renderStatusLine({ icon: "error", title: "任务" }, uiTheme);
 			return framedToolCard(uiTheme, () => ({
 				header,
 				sections: [{ content: formatErrorDetail(errorText, uiTheme).split("\n") }],
@@ -493,8 +493,8 @@ export const todoToolRenderer = {
 		const header = renderStatusLine(
 			{
 				iconOverride: uiTheme.styledSymbol("tool.todo", "accent"),
-				title: "Todo",
-				meta: [`${allTasks.length} tasks`],
+				title: "任务",
+				meta: [`${allTasks.length} 个任务`],
 			},
 			uiTheme,
 		);
@@ -502,7 +502,7 @@ export const todoToolRenderer = {
 			// Provider text on the Cursor path (the todo summary or a refusal note),
 			// so sanitize like every other label. The error branch above already
 			// goes through `formatErrorDetail`.
-			const fallback = forDisplay(result.content?.find(content => content.type === "text")?.text ?? "No todos");
+			const fallback = forDisplay(result.content?.find(content => content.type === "text")?.text ?? "没有任务");
 			return new Text(`${header}\n  ${uiTheme.fg("dim", fallback)}`, 0, 0);
 		}
 
diff --git a/pi-tui/src/tools/vibe.ts b/pi-tui-zh/src/tools/vibe.ts
index ca145ad..cdbda20 100644
--- a/pi-tui/src/tools/vibe.ts
+++ b/pi-tui-zh/src/tools/vibe.ts
@@ -138,6 +138,32 @@ function stateToColor(state: VibeSessionState): ToolUIColor {
 	}
 }
 
+/** Displayed copy of a session state (the semantic value stays in `screen.state`). */
+function sessionStateLabel(state: VibeSessionState): string {
+	switch (state) {
+		case "starting":
+			return "启动中";
+		case "running":
+			return "运行中";
+		case "idle":
+			return "空闲";
+		case "dead":
+			return "已退出";
+	}
+}
+
+/** Displayed copy of a settled turn status (the semantic value stays in the event). */
+function settledStatusLabel(status: "completed" | "failed" | "cancelled"): string {
+	switch (status) {
+		case "completed":
+			return "已完成";
+		case "failed":
+			return "失败";
+		case "cancelled":
+			return "已取消";
+	}
+}
+
 interface VibeRenderArgs {
 	cli?: VibeCli;
 	prompt?: string;
@@ -209,7 +235,12 @@ function tvScreen(
 		live && options.spinnerFrame !== undefined && shimmerEnabled()
 			? shimmerText(screen.id, uiTheme)
 			: uiTheme.fg(live ? "accent" : "toolOutput", screen.id);
-	const headParts = [icon, badge, idText, uiTheme.fg("dim", settledStatus ?? screen.state)];
+	const headParts = [
+		icon,
+		badge,
+		idText,
+		uiTheme.fg("dim", settledStatus ? settledStatusLabel(settledStatus) : sessionStateLabel(screen.state)),
+	];
 	const turnsLabel = `${screen.turns}t${screen.queued > 0 ? `+${screen.queued}q` : ""}`;
 	headParts.push(uiTheme.fg("muted", turnsLabel));
 	if (screen.turnStartedAt !== undefined) {
@@ -249,7 +280,7 @@ function tvScreen(
 	const footer = settledStatus
 		? uiTheme.fg(
 				settledStatus === "completed" ? "success" : settledStatus === "failed" ? "error" : "warning",
-				`turn ${settledStatus} — result delivered`,
+				`回合 ${settledStatusLabel(settledStatus)} — 结果已送达`,
 			)
 		: undefined;
 	return miniFrame(uiTheme, headParts.join(" "), body, footer);
@@ -281,8 +312,8 @@ function describeCall(op: VibeOp, args: VibeRenderArgs | undefined): string {
 			return `send → ${args?.session ? frameText(args.session, 40) : "?"}`;
 		case "wait":
 			return args?.sessions?.length
-				? `wait on ${frameText(args.sessions.join(", "), 60)}`
-				: "wait on running sessions";
+				? `等待 ${frameText(args.sessions.join(", "), 60)}`
+				: "等待运行中的会话";
 		case "kill":
 			return `kill ${args?.session ? frameText(args.session, 40) : "?"}`;
 		case "list":
@@ -309,7 +340,7 @@ export function createVibeToolRenderer(op: VibeOp) {
 						uiTheme,
 						title,
 						composerRows(uiTheme, message, { cursor: cursorOn, expanded: options.expanded }),
-						uiTheme.fg("dim", op === "spawn" ? "booting CLI…" : "delivering…"),
+						uiTheme.fg("dim", op === "spawn" ? "正在启动 CLI…" : "正在投递…"),
 					);
 				});
 			}
@@ -343,14 +374,14 @@ export function createVibeToolRenderer(op: VibeOp) {
 						: `${uiTheme.fg("muted", "vibe send →")} ${uiTheme.fg("accent", frameText(args?.session ?? "?", 40))}`;
 				const ack =
 					op === "spawn"
-						? uiTheme.fg("success", `turn started${details.spawned ? ` (job ${details.spawned.jobId})` : ""}`)
+						? uiTheme.fg("success", `回合已启动${details.spawned ? ` (任务 ${details.spawned.jobId})` : ""}`)
 						: details.send?.mode === "steered"
-							? uiTheme.fg("success", "steered into the running turn")
+							? uiTheme.fg("success", "已切入正在进行的回合")
 							: details.send?.mode === "queued"
-								? uiTheme.fg("warning", "mid-turn — queued as the next turn")
+								? uiTheme.fg("warning", "回合进行中 — 已排队为下一回合")
 								: uiTheme.fg(
 										"success",
-										`turn started${details.send?.jobId ? ` (job ${details.send.jobId})` : ""}`,
+										`回合已启动${details.send?.jobId ? ` (任务 ${details.send.jobId})` : ""}`,
 									);
 				const lines = miniFrame(
 					uiTheme,
@@ -362,7 +393,7 @@ export function createVibeToolRenderer(op: VibeOp) {
 			}
 
 			if (op === "kill") {
-				const killedNote = details.killed?.cancelledTurn ? " (in-flight turn cancelled)" : "";
+				const killedNote = details.killed?.cancelledTurn ? " (正在进行的回合已取消)" : "";
 				const header = renderStatusLine(
 					{
 						icon: "done",
@@ -376,7 +407,7 @@ export function createVibeToolRenderer(op: VibeOp) {
 			// wait/list: the TV wall.
 			const screens = details.screens;
 			if (screens.length === 0) {
-				const fallback = result.content.find(part => part.type === "text")?.text ?? "no sessions";
+				const fallback = result.content.find(part => part.type === "text")?.text ?? "没有会话";
 				return new Text(
 					renderStatusLine(
 						{ icon: "warning", title: `vibe ${op}`, meta: [uiTheme.fg("dim", frameText(fallback, 60))] },
@@ -391,15 +422,15 @@ export function createVibeToolRenderer(op: VibeOp) {
 			return linesComponent(() => {
 				const running = screens.filter(screen => screen.state === "running" || screen.state === "starting").length;
 				const meta: string[] = [];
-				if (running > 0) meta.push(uiTheme.fg("accent", `${running} on air`));
-				if (settledById.size > 0) meta.push(uiTheme.fg("success", `${settledById.size} settled`));
-				if (details.wait?.timedOut) meta.push(uiTheme.fg("warning", "timed out"));
+				if (running > 0) meta.push(uiTheme.fg("accent", `${running} 个运行中`));
+				if (settledById.size > 0) meta.push(uiTheme.fg("success", `${settledById.size} 个已结束`));
+				if (details.wait?.timedOut) meta.push(uiTheme.fg("warning", "超时"));
 				const title =
 					op === "wait"
 						? waiting
-							? "vibe wait — watching the wall"
+							? "vibe wait — 正在观看电视墙"
 							: "vibe wait"
-						: `vibe sessions (${screens.length})`;
+						: `vibe 会话 (${screens.length})`;
 				const header = renderStatusLine(
 					{
 						icon: details.wait?.timedOut ? "warning" : running > 0 ? "info" : "done",
diff --git a/pi-tui/src/tools/web-search.ts b/pi-tui-zh/src/tools/web-search.ts
index 8fcfdab..40ed27f 100644
--- a/pi-tui/src/tools/web-search.ts
+++ b/pi-tui-zh/src/tools/web-search.ts
@@ -293,7 +293,7 @@ export const SEARCH_PROVIDER_OPTIONS = [
 	{
 		value: "openrouter",
 		label: "OpenRouter",
-		description: "OpenRouter plugins-based web search with model-selected grounding",
+		description: "基于 OpenRouter 插件、由模型选择 grounding 的网页搜索",
 	},
 	{ value: "zai", label: "Z.AI", description: "Calls Z.AI webSearchPrime MCP" },
 	{ value: "exa", label: "Exa", description: "API via /login exa or EXA_API_KEY; explicit keyless fallback via MCP" },
diff --git a/pi-tui/src/tools/write.ts b/pi-tui-zh/src/tools/write.ts
index 346ed85..4667ea8 100644
--- a/pi-tui/src/tools/write.ts
+++ b/pi-tui-zh/src/tools/write.ts
@@ -81,7 +81,7 @@ function writeContentOf(args: unknown): string {
 
 function formatLineCountSuffix(lineCount: number, uiTheme: Theme): string {
 	if (lineCount <= 0) return "";
-	return uiTheme.fg("dim", ` · ${lineCount} line${lineCount === 1 ? "" : "s"}`);
+	return uiTheme.fg("dim", ` · ${lineCount} 行`);
 }
 
 function normalizeDisplayText(text: unknown): string {
@@ -257,7 +257,7 @@ function formatStreamingContent(
 
 		let text = "\n\n";
 		if (hidden > 0) {
-			text += `${uiTheme.fg("dim", `… (${hidden} earlier line${hidden === 1 ? "" : "s"})`)}\n`;
+			text += `${uiTheme.fg("dim", `… (之前还有 ${hidden} 行)`)}\n`;
 		}
 		for (let i = 0; i < visibleLines.length; i++) {
 			const lineNum = startIndex + i + 1;
@@ -273,7 +273,7 @@ function formatStreamingContent(
 	// the native-scrollback commit boundary at the top of the block, so a long
 	// expanded preview could never scroll-append mid-stream.
 	const spinner = spinnerFrame !== undefined ? `${formatStatusIcon("running", uiTheme, spinnerFrame)} ` : "";
-	return `${bodyText}${spinner}${uiTheme.fg("dim", `… (streaming)`)}`;
+	return `${bodyText}${spinner}${uiTheme.fg("dim", `… (流式传输中)`)}`;
 }
 
 function renderContentPreview(
@@ -325,13 +325,13 @@ export const writeToolRenderer = {
 				: typeof writeArgs.path === "string"
 					? writeArgs.path
 					: "";
-		if (!rawPath) return { label: "Write" };
+		if (!rawPath) return { label: "写入" };
 		const xdev = parseXdUrl(rawPath);
 		if (xdev?.name) {
 			const resolveMounted = (context.renderContext as WriteRenderContext | undefined)?.resolveXdevMounted;
 			return xdevActivitySummary(xdev.name, writeArgs.content, resolveMounted);
 		}
-		return { label: "Write", detail: shortenPath(rawPath) };
+		return { label: "写入", detail: shortenPath(rawPath) };
 	},
 
 	renderCall(
@@ -366,7 +366,7 @@ export const writeToolRenderer = {
 		// noise. The liveness cue rides the trailing "(streaming)" line instead.
 		const header = renderStatusLine(
 			{
-				title: "Write",
+				title: "写入",
 				description: `${langIcon} ${pathDisplay}`,
 			},
 			uiTheme,
@@ -436,7 +436,7 @@ export const writeToolRenderer = {
 		if (result.isError) {
 			const errorText = result.content?.find(c => c.type === "text")?.text ?? "";
 			const header = renderStatusLine(
-				{ icon: "error", title: "Write", description: `${langIcon} ${pathDisplay}` },
+				{ icon: "error", title: "写入", description: `${langIcon} ${pathDisplay}` },
 				uiTheme,
 			);
 			return framedToolCard(uiTheme, () => ({
@@ -453,14 +453,14 @@ export const writeToolRenderer = {
 		const lineSuffix = formatLineCountSuffix(lineCount, uiTheme);
 		const execSuffix =
 			!isPartial && result.details?.madeExecutable
-				? `${uiTheme.fg("dim", " · ")}${uiTheme.fg("success", "made executable!")}`
+				? `${uiTheme.fg("dim", " · ")}${uiTheme.fg("success", "已设为可执行!")}`
 				: "";
 		const header = renderStatusLine(
 			{
 				icon: isPartial ? "running" : undefined,
 				iconOverride: isPartial ? undefined : uiTheme.styledSymbol("tool.write", "accent"),
 				spinnerFrame: options.spinnerFrame,
-				title: "Write",
+				title: "写入",
 				description: `${langIcon} ${pathDisplay}${lineSuffix}${execSuffix}`,
 			},
 			uiTheme,
